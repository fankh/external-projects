spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/kyra_security
    username: ${DB_USERNAME:kyra}
    password: ${DB_PASSWORD:kyra}

  data:
    redis:
      host: redis
      port: 6379
