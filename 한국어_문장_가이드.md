# 한국어 문장 가이드 — 화면 문구

이 저장소의 제품(전사 IT·보안 거버넌스 포털, SEEKERSLAB ITAM 등)에서 **화면에 보이는 한국어**를
어떻게 쓰는지 정한다. 코드에 박히는 문자열, 안내 문구, 오류 메시지, 빈 화면 문구가 대상이다.

이 문서 자체는 개발 문서이므로 평서형(`-한다`)으로 쓴다. 규칙은 "무조건 공손하게"가 아니라
**누가 누구에게 하는 말인가**로 갈린다.

---

## 1. 한 줄 원칙

> 화면 문장은 시스템이 사용자에게 건네는 말이다. 말을 거는 쪽이 존댓말을 쓴다.

`-한다`, `-된다`, `-이다`로 끝나는 문장은 설명문이지 대화가 아니다. 사용자에게는 통보처럼 읽힌다.

---

## 2. 어디에 무엇을 쓰나

| 대상 | 문체 | 예 |
| --- | --- | --- |
| 안내·도움말, 툴팁, 콜아웃 | 존댓말(합쇼체) | 승인되면 폐기확정으로 집계**됩니다**. |
| 오류·경고·확인 메시지 | 존댓말(합쇼체) | 이미 열려 있는 기간이 있어 추가할 수 **없습니다**. |
| 사용자에게 행동을 요청 | `-하세요` / `-해 주세요` | 종료일자를 입력**해 주세요**. |
| 버튼·탭·컬럼 머리글 | 명사형 그대로 | 저장 · 닫기 · 대상자 관리 |
| 빈 상태(empty state) | 존댓말 | 처리 대기 중인 자산이 **없습니다**. |
| **사용자가 스스로 확인·서약하는 문항** | **평서형 유지** | 업무용 PC 화면잠금을 설정**했다**. |
| **코드 주석·설계 문서·커밋 메시지** | **평서형 유지** | 조회 권한과 라우트 역할을 함께 **본다**. |

마지막 두 줄이 중요하다. 점검 문항은 사용자가 *자기 자신에 대해* 진술하는 문장이라
`-했다`가 맞다(`awareness/remote/page.tsx`의 `ITEMS`). 이것을 존댓말로 바꾸면 뜻이 틀어진다.
주석도 개발자끼리 읽는 글이므로 지금처럼 둔다.

---

## 3. 종결 어미

- 기본은 **합쇼체**(`-합니다`, `-됩니다`, `-없습니다`). 제품 화면의 표준이다.
- 행동을 요청할 때만 `-하세요`, 부탁의 뉘앙스가 필요하면 `-해 주세요`.
- `-해요`체는 쓰지 않는다. 사내 업무 시스템에서는 가볍게 읽힌다.
- 명사로 끊는 개조식 종결(`… 처리.`, `… 등록.`)은 화면에서 쓰지 않는다. 문장을 끝맺는다.

---

## 4. 고쳐 쓰기

실제 문구다. 왼쪽이 현재, 오른쪽이 고친 문장이다.

**`awareness/remote/page.tsx:239`**

> 시작일자를 넣으면 신규 등록(기존 열린 기간은 전일로 마감), 종료일자만 넣으면 진행중 대상의
> 재택 종료 처리. 업로드는 CSV 한 줄에 `이름,시작일자[,종료일자]` — 같은 규칙으로 행 단위 반영된다.

고친 문장:

> 시작일자를 입력하면 새로 등록합니다. 이때 이미 열려 있는 기간은 전날로 마감합니다.
> 종료일자만 입력하면 진행 중인 대상의 재택근무를 종료합니다.
> CSV로 올릴 때는 한 줄에 `이름,시작일자[,종료일자]` 형식으로 적어 주세요.
> 각 줄에 같은 규칙이 그대로 적용됩니다.

무엇을 바꿨는지:

1. 개조식 종결(`… 처리.`)과 평서형(`반영된다`)을 합쇼체로 바꿨다.
2. 괄호 안에 숨어 있던 조건(`기존 열린 기간은 전일로 마감`)을 문장으로 꺼냈다.
   괄호는 부연이지 규칙을 담는 자리가 아니다.
3. 대시(`—`)로 이어 붙인 두 문장을 나눴다. 대시는 문장을 잇는 접착제가 아니다.
4. `넣으면` → `입력하면`, `전일` → `전날`처럼 화면에서 쓰는 말로 바꿨다.
5. 한 문장에 규칙 두 개를 담지 않고 줄을 나눴다.

**`awareness/prints/page.tsx:212`**

> 결재상신한다(본인 자료만 상신 가능). 승인되면 폐기확정으로 집계된다.

> 결재를 상신합니다. 본인 자료만 상신할 수 있습니다. 승인되면 폐기확정으로 집계됩니다.

**`awareness/remote/page.tsx:157`**

> 대상 명단은 일자별로 관리한다.

> 대상 명단은 일자별로 관리합니다.

---

## 5. 흔한 실수

- **개조식 종결** — `… 처리.` / `… 등록.` 명사로 끊으면 보고서 문체가 된다.
- **대시로 문장 잇기** — `A — B` 로 두 문장을 붙이지 않는다. 마침표로 끊는다.
- **괄호에 규칙 숨기기** — 괄호 안 내용이 동작을 좌우한다면 본문으로 꺼낸다.
- **명사 나열** — `진행중 대상의 재택 종료 처리` 같은 명사 사슬은 동사로 푼다.
- **한 문장에 규칙 여럿** — 조건이 둘이면 문장도 둘로 쓴다.
- **용어 흔들림** — 같은 것을 `시작일자`/`시작일`, `재택`/`재택근무`로 섞어 쓰지 않는다.
  화면 하나 안에서는 한 낱말로 통일한다.

---

## 6. 오류·확인 메시지

세 가지를 담는다. **무엇이 일어났는지 → 왜 → 다음에 무엇을 할지.**

> 이미 열려 있는 재택 기간이 있어 추가하지 않았습니다. 기간이 겹치면 대상자가 중복 집계됩니다.
> 기존 기간을 먼저 종료한 뒤 다시 등록해 주세요.

원인만 적고 끝내거나(`중복입니다.`), 사과만 하고 끝내지 않는다(`오류가 발생했습니다.`).

---

## 7. 검토 체크리스트

화면 문구를 추가하거나 고칠 때 확인한다.

- [ ] 사용자에게 하는 말인가? 그렇다면 `-합니다` / `-됩니다` / `-하세요`로 끝나는가?
- [ ] 사용자가 스스로 진술하는 문항이거나 코드 주석인가? 그렇다면 평서형 그대로 두었는가?
- [ ] 명사로 끊은 문장(`… 처리.`)이 남아 있지 않은가?
- [ ] 괄호 안에 동작 규칙이 숨어 있지 않은가?
- [ ] 대시로 두 문장을 붙이지 않았는가?
- [ ] 오류 메시지에 다음 행동이 적혀 있는가?
- [ ] 같은 대상을 같은 낱말로 부르는가?
