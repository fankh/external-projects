# 전사 IT·보안 거버넌스 포털 — portal-web

`전사_IT보안_거버넌스포털_제품안내서.pdf`(16p)를 구현 스코프로 한 웹 포털.
IT 투자/비용 · IT Request(SR) · 인프라 운영 · 프로젝트 · 보안 컴플라이언스를
공통 전자결재·권한관리 기반 위의 단일 포털로 통합한다.

seekerslab-itam/itam-web 의 셸 패턴(도메인 메뉴바 + MDI 탭 + 좌측 내비 +
서버사이드 권한 가드)을 계승하되, 디자인은 뉴트럴 엔터프라이즈(그레이 기조,
도메인별 액센트 색 차등)로 구분한다.

## 재사용 프레임워크 구조

이 포털은 특정 고객사에 하드코딩되지 않는다. 커스터마이징 지점은 두 곳뿐이다:

1. **`portal.config.ts`** — 고객사 프로필: 브랜딩(제품명·고객사명)과 연동 채널
   바인딩(채널 → 어댑터 id → 기본 활성 상태). 제품안내서 §V의 5대 연동
   (그룹웨어 메일·전자결재·SSO / 인사·근태 / 자산관리 API / 보안·출력물 DB / SMS).
2. **`lib/integrations/`** — 어댑터 계층: `types.ts`가 계약(메시징·HR·자산),
   `mock.ts`가 데모 구현. 고객사 배포 시 mock 어댑터를 고객사 시스템용
   구현(REST API·DB 연계·SAML)으로 교체하고 config 의 adapterId 만 바꾼다.

포털 본체(화면·스토어·결재 엔진)는 어댑터 인터페이스만 의존한다. 채널을 중지하면
발송·조회가 실패로 드러나고(발송 이력·배치 이력), 상태바 채널 카운트가 갱신된다.

## 현재 단계 — v1.1 (요구 복원 트랙)

36개 화면(34개 요구 화면 + 감사 이력 + 통합 검색) 전부 구현, 요구사항 3개 시트
(메뉴체계 84행 · 결재 14건 · 첨부 지점) 소진. 결재는 상신 → 상세 확인 → 승인/사유
반려 → 보완 재상신 → 상신취소(회수)의 전체 생명주기를 갖추고, 고객사 전환은
`PORTAL_PROFILE` 환경변수 한 개로 검증된다. v1.0 봉인 후 v1.0.x 패치 트랙(결함·검증·
문서)이 재감사까지 소진되어, **v1.1 부터는 로드맵 §4 '실구현 시 복원 목록'(재감사에서
확인된 신규 화면·엔터티급 요구)을 오너 지시로 순차 구현한다** — 게이트 3종 통과 규칙은
동일하다.

### 버전 이력

| 버전 | 내용 |
|------|-----------|
| v0.1 | 셸 + 로그인 + 10대 도메인 전체 스텁 + 개인별현황 대시보드 |
| v0.2 | 나의 할일 · 결재함 — 승인·반려가 SR 상태로 전파되고 '결재' 할일이 자동 마감 |
| v0.3 | SR 신청→결재 상신→CI 배정→개발·테스트·적용→완료, 지연 재계획, 권한별 데이터 스코핑 |
| v0.4 | 보안서약서 제출→할일 마감→부서 현황 집계→미서약 안내메일(배치 이력), 공지 등록 |
| v0.5 | 연동 어댑터 프레임워크 — portal.config 채널 바인딩 + 연동·인프라 관리 화면(토글·HR 동기화·발송 이력) |
| v0.6 | 자산등록 — 자산관리시스템 API 어댑터 연계 (조회·자산등록번호 취득, 채널 중지 시 차단) |
| v0.7 | IT 투자 폐쇄 루프 — 경영계획→확정→시행/계약→정산품의 상신→결재 승인→지급완료→계획대비실적 |
| v0.8 | 셸 브랜딩을 고객사 프로필로 일원화 (하드코딩 제거) |
| v0.9 | 장애관리(등록→조치→통계 결재상신→향후대책) + 변경관리(계획/결과 2단 상신, SR 적용요청 편입→최종완료 시 SR 완료 전파) |
| v0.10 | 보안점검 ISMS — 기준(Template)→연간 점검계획→결과 결재상신→현황판 집계·기한 경과 추적 |
| v0.11 | fix: 점검계획 폼 오버플로로 버튼이 가려지던 문제 (Playwright 검출) |
| v0.12 | 비용 관리 — 경영계획→계약→속보(월별 지불 예상)→정산품의, 기준금액 정산>계약>계획 우선순위 |
| v0.13 | 보안교육 — 연간계획→결과·명단 등록→이수현황 집계, 명단 등록 시 '보안교육' 할일 자동 마감 |
| v0.14 | 출력물 개인정보관리(보안·출력물 어댑터 일배치 이관→본인 폐기 등록→결재→폐기확정) + 재택근무 체크리스트(월 주기 자가점검→현황 취합) |
| v0.15 | 보안위반 관리 — 등록→확인서 안내메일(메일 어댑터)→위반자 본인 확인서 결재→완료 |
| v0.16 | 환경설정 사용자·그룹·결재선 — 8개 상신 액션의 하드코딩 결재자를 스토어 기반 기본 결재선(공통 draftApproval)으로 대체, 자기 결재 방지 |
| v0.17 | 프로젝트 관리 3화면 — 계약 연동 등록·진척(100%→완료)·인력, 산출물 점검·이슈 추적, 회의록·주간보고 |
| v0.18 | 인프라 시스템·서버 현황(장애 이력 연계·디스크 경고) + 배치·인터페이스·디스크(재실행→상태바) + 전사 서약 현황·양식관리(개정→전원 재서약 재산출·스캔본·보안담당자) + QnA |
| v0.19 | 환경설정 완성 — 공통코드(장애등급 셀렉트가 코드그룹을 직접 소비)·메뉴/기능·권한 매트릭스·엑셀양식(버전 관리). 34개 화면 전부 구현 |
| v0.20 | 파일 기반 영속화(PORTAL_DATA_FILE) + 공통 첨부 기반(실파일 업로드 메타, SR·장애·계약 장착, 결재함 첨부 표시) |
| v0.21 | 엑셀 다운로드 4종(BOM CSV — 계획대비실적·이수현황·서약 현황·재택 현황, 권한·부서 스코핑) + 특별서약서(보안담당자) + 협력업체서약서(징구·첨부→선택 상신→승인 완료) |
| v0.22 | SR 유형별 단계 분기(데이터·계정/권한은 처리→완료 직행, '처리중' 표기) + 변경·점검 증적 첨부 + Dockerfile |
| v0.23 | 개인별현황 포틀릿 완성(관리대상 현황 2행 + 전사 운영 스냅샷) + 제품안내서 부록 슬라이드(실화면 4컷, PDF 17p) |
| v0.24 | 감사 이력 — 결재 처리·결재선/채널/코드 변경·양식 개정·배치 실행 기록(append-only) + 환경설정 조회 화면 (35번째 화면) |
| v0.25 | client-health 스크립트(전 화면 브라우저 크래시 검사) + docker-compose/nginx 배포 예시 + 고객사 프로필 예시(profiles/) |
| v0.26 | docs — 구축 요약·데모 시나리오 |
| v0.27 | 일일 알림 배치(미서약·점검 경과·SR 지연·재택 미제출 → 메일 어댑터) + 결재 문서 상세 패널(참조 스냅샷 8종·첨부 목록·상세 승인/반려, 본인 문서만 열람) |
| v0.28 | 통합 검색(/search) — SR·공지·QnA·계약 + 담당 전용 장애·변경·프로젝트, 화면과 동일 스코핑, 타이틀바 연결 |
| v0.29 | 로그 상한 하드닝 — 발송·배치 이력 200건 캡, 배치 기록 recordBatch 헬퍼 일원화 |
| v0.30 | 모바일 대응 — 타이틀바 레이아웃 수정, client-health 모바일 뷰포트 패스(가로 오버플로 검출) |
| v0.31 | 런타임 견고성 — 브랜디드 404·오류 바운더리(화면/전역) + ChunkReload(재배포 청크 크래시 자동 복구, itam 검증 패턴) |
| v0.32 | 세션 쿠키 HMAC 서명(SESSION_SECRET, 평문·변조·권한 상승 거부 — 스모크 부정 검증) + 감사 이력 CSV |
| v0.33 | 세션 만료(12시간 exp 서명 포함, 만료 쿠키 거부) + 보안 응답 헤더(X-Frame-Options·nosniff·Referrer-Policy) |
| v0.34 | 결재 반려 사유 필수(기안자 상신함·상세 표시, 감사 기록 포함) + 알림 배치 자동 스케줄러(instrumentation, PORTAL_NOTIFY_INTERVAL_MS — Docker 기본 24h) |
| v0.35 | 반려→재상신 폐쇄 루프 — SR 반려 시 기안자 '재상신' 할일 자동 생성(사유 포함), 신청내역 재상신 버튼([재상신] 재결재, 할일 마감) |
| v0.36 | 정산품의 반려 재상신(투자·비용 공통 액션, 기안자 본인·중복 상신 가드) — 전 문서 유형 재상신 경로 정합 완성 |
| v0.37 | E2E 스위트 저장소 편입(scripts/e2e_suite.py, 9시나리오 · 시나리오별 독립 서버) — 상설 게이트 3종 완성(npm run smoke/health/e2e) |
| v0.38 | QnA 담당자 지정(요구사항 소항목 잔여분) + 문서 정합(데모 시나리오에 상세·반려/재상신·검색·스케줄러 반영, 구축 요약 36화면) |
| v0.39 | 런타임 프로필 스위칭 — PORTAL_PROFILE 환경변수로 고객사 전환(빌드 불변), 제조업 예시 프로필 실동작화 + e2e 자동 검증(10번째 시나리오) |
| v1.0 | 마일스톤 선언 — 36화면·요구사항 소진·프로필 스위칭 검증, 게이트 3종 인증 |
| v1.0.1 | docs — v1.0 이후 백로그(실고객 어댑터·SAML·MS-SQL·피드백, 착수 조건·완료 기준) |
| v1.0.2 | fix — 상신 묶음 번호(장애 IR·출력물 PD·서약 CPB)를 결재 이력에서 채번(반려 시 행 참조 초기화로 번호가 재사용되던 결함) + 결재 상세 참조 번호 표시 + e2e 11번째 시나리오(batchref) |
| v1.0.3 | 반려 → '재상신' 할일을 전 문서 유형 공통으로 일원화(SR 한정이던 것을 9종 전체로) — 재상신 시 draftApproval 이 참조 번호(묶음 문서는 유형 태그)로 자동 마감, 할일 바로가기를 문서 유형별 화면으로 연결 |
| v1.0.4 | 일일 알림 배치 5번째 유형 '반려 방치' — 반려 후 재상신하지 않은 기안자에게 안내메일(재상신하면 할일이 닫혀 자동 제외), e2e 검증 포함 |
| v1.0.5 | 결재 문서 상세에 이전 회차 결재 이력 — 같은 참조로 상신된 과거 문서(반려 사유 포함)를 표로 표시, 재상신 심사 시 보완 근거 확인 |
| v1.0.6 | 감사 이력에 '결재 상신' 기록 — 상신 단일 경로(draftApproval)에서 전 문서 유형 공통, 상신→승인/반려 결재 생명주기 전체가 감사 추적 |
| v1.0.7 | 스모크 부정 검증 보강 — 무관한 계정의 결재 문서 상세(sel=) 직접 접근 차단 2건 (스모크 458체크) |
| v1.0.8 | fix(integrations): 채널 토글을 화면 표시(isEnabled)와 같은 기준으로 반전 — 구버전 데이터 파일에 키가 없는 기본 가동 채널의 첫 '중지' 클릭 무동작 해소 + e2e 12번째 시나리오(persist — 부분 파일 시드 머지·채널 기본값 폴백) |
| v1.0.9 | 스모크 CSV 부정 검증 보강 — 부서담당 서약 CSV 타부서 인원 미포함·USER 재택 현황 403 (스모크 461체크) |
| v1.0.10 | 세션 쿠키 Secure 옵션(PORTAL_COOKIE_SECURE=1, HTTPS 종단 배포용) + e2e 세션 검증(쿠키 HttpOnly·SameSite=Lax, 로그아웃 후 보호 화면 차단) |
| v1.0.11 | docs 현행화 — 구축 요약(게이트 3종 461/46/12·감사 결재 상신·영속화 머지·반려 순환)·데모 시나리오(이전 회차 이력·반려 방치 알림) |
| v1.0.12 | fix(education): 유효 이수자 0명인 명단 등록이 과정을 '완료'로 확정하지 않도록 서버 가드 — 조작 요청의 빈 완료 과정이 이수율 집계를 왜곡하던 경로 차단 |
| v1.0.13 | docs(roadmap): v1.0.x 패치 트랙 공식화(결함·검증·문서는 봉인 예외, 신규 기능은 실고객 접점) + 운영 수칙 보강(재발 방지 체크 동반 커밋, 실배포 환경변수 4종) |
| v1.0.14 | 기동·종료 하드닝 — 프로세스 종료 시 영속화 동기 플러시(디바운스 300ms 창의 마지막 뮤테이션 유실 방지), 알림 스케줄러 중복 기동 가드(HMR 재등록 시 중복 발송 방지) |
| v1.0.15 | Dockerfile 하드닝 — 비루트(node) 실행(쓰기 경로 /data·.next 만 소유 이전) + HEALTHCHECK(로그인 화면 프로브) — 실컨테이너 기동·헬시 검증 |
| v1.0.16 | npm run gates — 빌드+게이트 3종 통합 러너 (커밋 전 기본 명령, 운영 수칙·테스트 절 갱신) |
| v1.0.17 | chore: e2e 임시 파일(scripts/.e2e-*) gitignore — 스위트 중단 시 잔류물의 오커밋 방지 |
| v1.0.18 | 데이터 파일 일일 백업 로테이션 — 스케줄러 틱이 일자별 스냅샷(.bak, 보존 7개)을 남겨 논리적 손상의 복구 지점 확보 (배치 이력 기록, e2e persist 검증) |
| v1.0.19 | docs: 백업 복구 절차 — 스냅샷을 원본 자리에 복사 후 재기동하는 볼륨 조작 예시 (README 배포 절) |
| v1.0.20 | 의존성 보안 — npm audit high 3건(next 전이 postcss·sharp)을 overrides 로 패치 강제, 취약점 0건 (게이트 3종 + Docker 빌드·기동 재검증) |
| v1.0.21 | docs(roadmap): 운영 수칙에 주기적 npm audit 점검 편입 (overrides 우선·의존성 변경 시 게이트+Docker 재검증) |
| v1.0.22 | Content-Security-Policy 헤더 — self 한정·object/frame/base/form-action 봉쇄 (인라인 스크립트는 Next 하이드레이션 제약으로 허용, dev 만 unsafe-eval) + 스모크 CSP 검증 2건 (463체크) |
| v1.0.23 | docs: 구축 요약 게이트 수치·보안 헤더 4종 현행화 (CSP 반영) |
| v1.0.24 | Permissions-Policy 헤더 — 미사용 브라우저 기능(카메라·마이크·위치·결제) 명시 봉쇄, 보안 헤더 5종 체계 (스모크 464체크) |
| v1.0.25 | 결재 시트 11번 구현 — '부서서약 현황 상신' (부서담당 현황 전체 상신, 결재 이력 채번 DPS, 상세 스냅샷·재상신 루프 편입). 요구사항 재감사에서 발견한 잔여 결재 유형 마감 — 결재 14건 전량 커버 (스모크 465·e2e pledge 확장) |
| v1.0.26 | 첨부 지점 장착 1차 (재감사 후속) — 비용 계약 부속서류·정산품의 증빙(투자/비용)·SR BA 검토 증적·SR 처리 결과 증적 (SR 은 pk 하나로 신청·BA·결과 첨부 공유 — 요구 명세 그대로) + e2e 첨부 뱃지 검증 2건 |
| v1.0.27 | 첨부 지점 장착 2차 — 프로젝트 5지점(인력투입계획·산출물·이슈 근거·회의록·주간보고 원본) 등록 폼 첨부 + 목록 뱃지 |
| v1.0.28 | 첨부 지점 장착 3차 — 교육(연간계획·결과 명단 증빙, 과정 pk 공유)·공지(게시/공지 이력 원본)·보안위반 증빙(결재제외자 스캔)·점검 연간계획(상세점검계획표) |
| v1.0.29 | 첨부 지점 장착 4차 — QnA 문의 첨부 + 메뉴관리 화면 정의서(화면당 1첨부·교체형, pages) — 첨부 시트 32지점 실장착 완료 |
| v1.0.30 | fix — 묶음(회전 참조) 문서 4종의 '이전 회차 결재' 이력 복원 (v1.0.2 참조 회전과 v1.0.5 참조 매칭의 충돌 — 문서유형·기안자로 회차 연결, 이력에 참조 컬럼) + 장애보고 상신 서버측 조치완료 재검증(조작 요청 차단, v1.0.12 동형) + e2e batchref 확장 |
| v1.0.31 | 일일 알림 배치 6번째 유형 '확인서 미제출' — 보안위반 사실확인서 징구중 방치 인원 안내메일(제출하면 결재중 전환으로 자동 제외, e2e 검증) + 감사 이력 화면 현행화 — '결재 처리' 통계를 승인·반려만 집계(상신 포함 과다 집계 결함), 상신·알림 배치 실행 톤·콜아웃 반영 + README 스모크 수치 정합(461→465) |
| v1.0.32 | 게이트 환경 내성 + 의존성 보안 — health·e2e 를 `scripts/pyrun.mjs` 경유로 전환(PATH 의 python 이 playwright 없는 무관 venv 로 바뀌어도 PORTAL_PYTHON→py→python3→python 순으로 자동 해석, 실패 시 안내 메시지), npm audit high 1건(next 전이 nanoid GHSA-2v37-7h3g-55p8)을 overrides `^3.3.17` 로 마감 — 취약점 0건, 스모크 서버 spawn 의 DEP0190 경고 제거 (게이트 3종 + Docker 빌드·기동 재검증) |
| v1.0.33 | 상신취소(회수) — 요구사항 결재 시트가 명시한 전이의 잔여 공백 마감. 기안자 본인이 결재 대기 문서를 상신함 상세에서 회수(서버측 기안자·대기·유형 3중 가드), 참조 업무를 상신 이전 상태로 복원 — SR → 작성중(임시저장, '상신' 버튼으로 재상신), 변경 계획/결과 → 작업등록/작업등록승인, 확인서 → 징구중. 결재자 '결재' 할일 자동 마감 + 감사 '결재 회수' 기록. 반려(재상신 할일 순환)와 달리 할일 없이 업무 화면 경로로 복귀 (e2e withdraw 시나리오 — 13시나리오) |
| v1.0.34 | 엑셀양식 자동첨부 실장착 — 결재 시트 8~10번 '첨부파일자동생성 o'가 제목 문구로만 있던 것을 실제 첨부로 구현. 장애보고·변경 계획/결과 상신 시 문서 유형에 매핑된 엑셀양식(XT)으로 첨부 레코드 생성(`registerGenerated` — 유형 미매핑은 '공통' 폴백, 같은 참조·양식 중복 방지), 변경결과 양식 시드(XT-05) 추가. 결재함 뱃지·문서 상세 첨부에 표시 (e2e batchref 검증 2건 추가) |
| v1.0.35 | 요구사항 재감사(메뉴 시트 84행 전수) 기존 화면 공백 4건 마감 — ① 관리책임자·재택근무 서약 제출 카드(45행 — 4종 양식 중 미서명 2종이 접근 불가였던 결함, 관리자 권한 서버 재검증, e2e pledge 확장) ② SR 관리 공수(MD) 누적 입력·작업기간 표시(26행) ③ SR 신청내역 유형별 분할 조회 필터(22~24행) ④ 비용 계획대비실적 카드 + expense-actual 엑셀(17행 — 투자와 동형화). 신설 화면·엔터티급 잔여는 로드맵 §4 '실구현 시 복원 목록'으로 문서화 (스모크 465→474) |
| v1.0.36 | 재감사 후속 — 기존 화면 액션 공백 2건 마감. ① 공통코드 사용기간·값 추가/삭제(73행): until 종료일(지나면 선택지 제외·기존 데이터 유지), 그룹별 값 추가·삭제, `isCodeActive` 단일 판정 — 장애등급 소비처 반영, 전 변경 감사 기록 ② 게시판 삭제(6행): 공지(작성자 본인·Admin)·QnA(미답변 본인·Admin), 첨부 동반 정리 + 감사 '게시물 삭제' 액션 신설 (스모크 474→476, e2e codes 확장 + board 시나리오 — 14시나리오) |
| v1.0.37 | e2e devchain 시나리오 — 제품 최장 폐쇄 루프(결재 시트 4·5·10번 연계)의 게이트 공백 마감. 시스템개발 SR 신청→승인→CI 배정→테스트·적용요청(공수 3+2 누적)→변경관리 편입→계획 상신취소(작업등록 복원)→재상신(자동첨부 1건 유지 — 회수 재상신 중복 방지 회귀 검증)→승인→결과 상신·승인→최종완료→SR 완료 전파. 코드 무변경, 검증만 추가 (15시나리오) |
| v1.1.0 | 요구 복원 트랙 개시 — 재택근무 대상자 명단 (54행, 재감사 hard-gap). `remoteTargets` 엔터티(일자별 기간): 시작일자 입력=신규 등록(기존 열린 기간 전일 마감), 종료일자만=종료 처리, CSV(`이름,시작[,종료]`) 업로드 행 단위 반영, 기간별(월) 조회 + export period 파라미터. 체크리스트 제출·현황·대시보드 스탯·미제출 알림 배치가 전부 명단 기준으로 전환(명단 밖 인원 제출 차단·안내 제외), 감사 '재택 대상자 변경' 기록 (스모크 476→482, e2e remote 시나리오 — 16시나리오) |
| v1.1.1 | 결재 없는 CI SR 접수 (25행, 재감사 hard-gap) — `ciSrs` 엔터티: 전화·메일 경로의 결재 불요 건을 보안/업무 구분으로 직접 접수(요청자 수기·사외 포함, 접수 근거 첨부 — 첨부 시트 CI SR관리 지점), 접수 → 처리중 → 완료(처리 내용 기록) 이력 관리. CI SR 관리 화면에 접수 폼·이력 테이블·진행 스탯 (스모크 482→486, e2e sr 시나리오 확장) |
| v1.1.2 | 보안점검 기준관리 CRUD·엑셀 (62행, 재감사 hard-gap) — 기준(Template) 등록(ISMS 대분류·중분류·통제항목·주기·구분, 중복 통제 항목 금지)·삭제(계획 참조 중이면 '사용중' 가드)·CSV 업로드(`대분류,통제항목,주기,구분[,중분류]` 행 단위, 무효 주기 건너뜀)·inspection-items 엑셀 다운로드. 주기 선택지는 공통코드 INSPECT_CYCLE 소비(`isCodeActive` 2번째 소비처), 전 변경 감사 '점검 기준 변경' 기록 (스모크 486→492, e2e criteria 시나리오 — 17시나리오) |
| v1.1.3 | 특별서약서(프로젝트) (46행, 재감사 hard-gap) — PledgeKind '프로젝트' + `projectRef`: 사내인력이 진행중 프로젝트를 지정해 참여 보안수칙에 동의(프로젝트별 1회, 양식 개정 시 재서약), 제출 현황 칩·내 서약 이력에 프로젝트명 표시. 프로젝트 진행현황에 '참여 서약' 집계 컬럼 — 서약과 프로젝트 도메인이 잇긴다 (스모크 492→495, e2e pledge 확장) |
| v1.1.4 | 엑셀 다운로드 전면 확대 (15·22~27·36~44·55·56·63행 엑셀 ◎) — 9종 신설: 속보(비용, 기준금액 우선순위 포함)·SR 신청내역(본인/부서/전사 스코핑)·CI SR 이력·장애 대장·변경 대장·프로젝트 현황(참여 서약 포함)·출력물 폐기현황(본인 스코핑)·보안위반 대장(본인 스코핑)·점검계획. 총 16종, 전부 화면 동일 권한·스코핑 서버 재검증 + 10개 화면 다운로드 버튼 (스모크 495→507 — 스윕 9 + 스코핑 2 + 차단 1) |
| v1.1.5 | 랙·H/W 관리 + 랙구성도 (28~31행, 재감사 최대 hard-gap) — 신규 화면 `/infra/racks` (menus.ts 등록으로 내비·가드·매트릭스 자동): 랙(위치·사이즈U·자산번호) CRUD, H/W(물리서버·네트워크장비·스토리지) CRUD — 장착·탑재 중이면 '사용중' 삭제 가드, racks·hardware 엑셀(18종). 랙구성도: 랙 → H/W → 서버 → 시스템 → 배치·인터페이스 사슬 도식(등록 데이터 실렌더). 서버관리에 CPU·메모리 컬럼(31행), 감사 '인프라 자산 변경' 기록 (스모크 507→524 · health 47화면 · e2e racks — 18시나리오) |
| v1.1.6 | 인프라 잔여 CRUD (31~34행) — 서버 등록(H/W 선택 → 랙 자동)·삭제(시스템 매핑 가드), 시스템 등록(서버 매핑)·삭제(배치·인터페이스 참조 가드), 배치·인터페이스 등록·삭제. servers·systems·batches·interfaces 엑셀(22종), 전 변경 감사 기록. 인프라 8종(28~35행) 전부 관리 가능 — 재감사 인프라 클러스터 마감 (스모크 524→534, e2e infracrud — 19시나리오) |

## 배포 (Docker)

```powershell
cd portal-web
docker build -t ngv-portal .
docker run -d --name ngv-portal -v portal-data:/data -p 3400:3400 `
  -e SESSION_SECRET="교체-필수-랜덤-키" ngv-portal
```

`SESSION_SECRET` 는 세션 쿠키 HMAC 서명 키다 — 미설정 시 개발용 기본 키로 동작하므로
실배포에서는 반드시 랜덤 키로 교체한다. HTTPS 로 서비스하는 배포에서는
`PORTAL_COOKIE_SECURE=1` 을 함께 설정해 세션 쿠키에 Secure 속성을 붙인다.

컨테이너는 KST 시간대로 동작하고, 스토어는 `/data/portal-data.json`(볼륨)에
영속화되어 재시작·재생성 후에도 유지된다. 알림 스케줄러 틱마다 일자별 백업
(`portal-data.json.YYYY-MM-DD.bak`, 보존 7개)이 같은 볼륨에 남는다.

백업으로 되돌리려면 컨테이너를 멈추고 원하는 일자의 스냅샷을 원본 자리에
복사한 뒤 재기동한다 (기동 시 파일을 다시 읽는다):

```powershell
docker stop ngv-portal
docker run --rm -v portal-data:/data alpine `
  cp /data/portal-data.json.2026-08-03.bak /data/portal-data.json
docker start ngv-portal
```

nginx 리버스 프록시를 포함한 구성은 `deploy/docker-compose.yml` 을 쓴다:

```powershell
docker compose -f deploy/docker-compose.yml up -d --build
```

고객사 전환은 **빌드 없이 환경변수로** 한다 — 한 이미지가 여러 고객사 프로필을 담는다:

```powershell
$env:PORTAL_PROFILE = "manufacturer"; npm run start   # 제조업 예시 프로필로 기동
```

새 고객사는 `profiles/` 에 구성 파일을 추가하고 `portal.config.ts` 선택지에 올린 뒤,
adapterId 가 가리키는 고객사 어댑터를 `lib/integrations/` 에 등록한다.
프로필 스위칭은 e2e 스위트의 `profile` 시나리오가 자동 검증한다.

미구현 화면은 캐치올 스텁(`app/(app)/[...stub]/page.tsx`)이 `lib/screens.ts`
카탈로그로 렌더 — 실제 구현 시 해당 경로에 page.tsx 를 만들면 정적 라우트가
캐치올보다 우선한다.

## 실행

```powershell
cd portal-web
npm install
npm run dev        # http://localhost:3000
```

기본은 인메모리(기동 시 시드 초기화)이고, `PORTAL_DATA_FILE` 환경변수를 주면
파일 기반으로 영속화되어 서버 재시작 후에도 데이터가 유지된다:

```powershell
$env:PORTAL_DATA_FILE = "C:\data\portal-data.json"; npm run start
```

로그인 화면에서 권한그룹별 목업 계정(SSO 대체) 4종 중 선택:
**사용자**(김현우) · **부서담당**(이수진) · **업무담당**(박정호) · **Admin**(시스템관리자).
권한그룹에 따라 메뉴바·좌측 내비 노출이 다르며, 모든 화면은 `lib/authz.ts`의
서버사이드 가드로 직접 URL 진입도 차단한다.

## 테스트

```powershell
npm run gates    # 빌드 + 아래 3종을 순서대로 전부 실행 (커밋 전 기본 러너)

npm run build
npm run smoke    # 프로덕션 서버 기동 → 권한 매트릭스·세션 보안·SSR 본문 검증 (534체크) → 종료
npm run health   # 실제 브라우저로 47화면(데스크톱+모바일) 로드 → 크래시·가로 오버플로 검사
npm run e2e      # 핵심 폐쇄 루프 19시나리오 — 결재 전파·어댑터·재상신·상신취소·개발변경 사슬·게시판·재택 명단·점검 기준·랙/H·W·인프라 CRUD·스케줄러·런타임 복구·영속화
```

`smoke` 는 서버 렌더 HTML만, `health` 는 로드만 본다 — 동작(전파·업로드·자동 발화)은
`e2e` 가 검증한다. 배포 전 셋 다 돌린다. (health·e2e 는 Python + playwright 필요)

## 구조

상세 문서: [docs/구축_요약.md](docs/구축_요약.md) (아키텍처·루프 카탈로그·커스터마이징),
[docs/데모_시나리오.md](docs/데모_시나리오.md) (15분 시연 대본),
[docs/로드맵.md](docs/로드맵.md) (v1.0 이후 백로그 — 착수 조건·완료 기준).

| 경로 | 역할 |
|------|------|
| `portal.config.ts` | 고객사 프로필 — 브랜딩·연동 채널 바인딩 (커스터마이징 단일 지점) |
| `lib/integrations/` | 연동 어댑터 계층 — 계약(types) · 목업(mock) · 레지스트리(registry) |
| `components/chrome/menus.ts` | 메뉴 체계(10대 도메인) × 권한그룹 — 권한의 단일 원천 |
| `components/chrome/AppShell.tsx` | 셸 — 타이틀바 · 메뉴바(LV1) · MDI 탭 · 좌측 내비(LV2) · 상태바 |
| `lib/screens.ts` | 스텁 화면 카탈로그(제품안내서 LV3 화면·기능) |
| `lib/store.ts` | in-memory 스토어(globalThis 싱글턴) — 실서비스에서는 MS-SQL 대체 |
| `lib/authz.ts` | 화면 단위 서버사이드 권한 게이트 |
| `scripts/smoke.mjs` | 라우트 × 권한 스모크 스위트 |
