# 로드맵 — v1.0 이후

v1.0(태그 `portal-web-v1.0`)은 데모·제안 범위의 봉인 지점이다. 요구사항 3개 시트가
소진됐고 게이트 3종이 녹색이므로, **신규 기능**은 아래 실고객 접점에서만 시작한다.
각 항목은 착수 조건(외부 입력)과 완료 기준을 함께 적는다.

예외로 **v1.0.x 패치 트랙**은 봉인과 무관하게 허용된다 — 결함 수정, 기존 폐쇄 루프의
공백 보강, 게이트(스모크·e2e) 검증 추가, 문서 정합. 새 화면·새 요구사항은 여기에
속하지 않는다. (v1.0.2~v1.0.12: 묶음 채번 결함, 반려→재상신 할일 일원화·방치 알림·
이전 회차 이력, 감사 '결재 상신', 영속화 구버전 머지·채널 토글, 세션 Secure 옵션,
접근 통제·CSV 부정 검증, 교육 완료 가드 — 상세는 README 단계 표.)

**강화·검증 트랙 (v1.1~v1.5, 봉인 예외):** v1.1.x 요구 복원(§4), v1.2 권한 아키텍처·보안선,
v1.3 제품안내서 대조, v1.4 다단 결재, v1.5 프레임워크 재사용성 강화(자가진단·프로필 단일원천·
접근성·배포 하드닝). **v1.5.14~42: 9대 핵심 서브시스템 적대 감사(결재·영속화·SR/변경·컴플라이언스·
재무·보안의식·인프라·게시판설정 + 델타) — 게이트가 못 잡는 로직·집계·스코핑·내구성 결함
전수 수정, 재현 가능 건 e2e 회귀 테스트(22→31시나리오). 감사가 남긴 저심각·잠재 항목 5종은
전부 조치 완료(#1 SR todo·#2 서약 타임스탬프·#3 year 필터·#4 비-일반 재서약·#5 폐기 취합 마감).
상세·핸드오프는 `docs/감사_후속조치.md`. v1.5.38~41: 확정·잠재 findings 전건에 재현 회귀
테스트 확보(크래프트 PORTAL_DATA_FILE 로 UI 미도달 상태까지 재현) — 이 과정에서 자가진단
경로 어댑터 호출이 withTimeout 누락이던 실결함 적발·수정(v1.5.41). v1.5.42: 재무 리포트
(전사 집계·계약·정산·속보·계획대비실적) 관리자급 잠금 — 일반 사용자에겐 본인 경영계획만,
화면·export·검색 3면 동시 스코핑(정산 상신 주체 DEPT_MGR 은 접근 유지).**
Docker 영속화 내구성(SIGTERM 플러시) 실컨테이너 검증.

**v1.5.95~120 요구 재복원 + 감사 근거 산출물 (태그 `portal-web-v1.5.120`, 완료):** 원문(sample.xlsx 결재
비고·제품안내서 본문) 재대조로 미구현 요구를 추가 복원 — 재택 주기 config·SR 중지(BA030014)·예산 초과
신호·이수현황 false-100. 이어 제품안내서 VI장 **보안성 검토**를 신규 도메인으로 복원(전 계층 통합:
화면·export·대시보드·배치·검색·감사, e2e sc_security_review). ISMS 감사 근거·경영 보고용 **컴플라이언스/
IT운영 종합 현황 export** 추가(각 지표 소속 화면 단일-원천 술어 재사용, 대시보드 정합 실검증). 교차 결함
수정(감사톤 Record<AuditAction> 강제, EXPORT_MENU/client_health 레지스트리 편입 누락 2건). 게이트 전량
그린: build·smoke 575·a11y 697·emptystate/corrupt 38·keycheck 3·health 48·e2e 68. 상세는
`docs/릴리스노트_v1.5.120.md`·`docs/감사_후속조치.md`. **이 트랙으로 두 요구 소스의 코드 게이트 표면은
재차 완비 확인 — 잔여는 아래 §1~3(외부 입력 의존)과 §4 잔여 2종(사용자그룹·메뉴기능, SSO 동행)뿐.**

**v1.5.127~131 보안성 검토 심각도 등급 (태그 `portal-web-v1.5.131`, 완료):** 사용자 지시로 보안성 검토
발견/조치를 등급별(심각·높음·보통·낮음)로 세분하고, 고위험(심각+높음) 미조치를 우선 조치 신호로 화면·
대시보드·컴플라이언스 종합 export 에 전면 노출(`highSevOpen` 단일원천). findings/fixed 총계 유지로 기존
소비처 무변경, 등급 필드는 평면 numField(손상 내성). 게이트 e2e 68/68·smoke 575·a11y 697·keycheck 3·
health 48. 상세 `docs/릴리스노트_v1.5.131.md`·`docs/감사_후속조치.md`(SV-1~4).

**v1.5.135~139 컴플라이언스 추세 스냅샷 (태그 `portal-web-v1.5.139`, 완료):** 사용자 지시로 컴플라이언스
포스처를 주기(월)별 스냅샷으로 남겨 전기 대비 개선 추이(ISMS 감사 근거)를 본다. KPI 산출을 `lib/compliance`
computeComplianceKpis 로 단일원천화(종합 export·스냅샷 공유)한 뒤, 보안점검 화면에 추세 카드(기간별 KPI +
직전 대비 델타)·'현황 스냅샷 기록'(당월 upsert)·compliance-trend export 를 얹었다. 게이트 e2e 69/69·
smoke 577·a11y 698·keycheck 3·health 48. **v1.5.141~142 추세 자동 스냅샷**: upsert 를 lib/compliance
단일경로로 추출 후 일배치(runDailyNotify)가 당월 포스처를 자동 스냅샷(by 스케줄러)해 추세가 사람 손 없이
쌓인다. 상세 `docs/릴리스노트_v1.5.139.md`·`docs/감사_후속조치.md`(CT-1~5).

## 1. 고객사 어댑터 실구현

- **착수 조건**: 고객사 시스템 접속 정보·API 명세(그룹웨어 메일/결재, 인사·근태,
  자산관리, 보안·출력물 DB) 접수.
- **작업**: `lib/integrations/` 에 어댑터 구현 추가, `profiles/<고객사>.config.ts` 작성,
  `portal.config.ts` 선택지 등록. 포털 본체는 무변경이 원칙 — 본체 수정이 필요해지면
  어댑터 계약(`lib/integrations/types.ts`)의 결함이므로 계약을 먼저 고친다.
- **완료 기준**: 어댑터 계약 자가진단(`/platform/integrations` 카드 — 새 프로필의 바인딩
  완결성·계약 적합성)이 전부 적합 + e2e `profile` 시나리오와 동형의 고객사 시나리오 추가
  통과 + 스테이징에서 실 시스템 대상 왕복 1회(메일 수신, 자산번호 취득 등) 확인.
  (v1.5.0: 자가진단 도구가 착수 조건으로 제공됨 — 고객사 어댑터를 registry 에 등록하고
  프로필을 올리면 즉시 구조 결함·계약 위반이 드러난다. v1.5.2: 프로필 구조 검증 추가.
  v1.5.37: 복사용 참조 어댑터 `lib/integrations/examples/rest-adapters.example.ts`.)
- **온램프 문서**: 개발자 절차는 `docs/어댑터_연동_가이드.md`(계약·오류/타임아웃·4단계),
  운영자 배포는 `docs/배포_운영_가이드.md`. 어댑터 예외/타임아웃은 프레임워크가 흡수한다
  (v1.5.16~17) — 어댑터는 실패 시 throw 만 하고 자체 타임아웃을 두지 않는다.

## 2. SAML SSO 연동

- **착수 조건**: 고객사 IdP 메타데이터(엔드포인트·인증서) 접수.
- **작업**: 로그인 화면의 목업 계정 선택을 SAML 어설션 처리로 교체. 세션 발급은
  기존 `signSession()` 을 그대로 쓰고, 어설션의 사번·권한그룹을 `Session` 으로 매핑한다.
  목업 로그인은 `PORTAL_PROFILE` 과 같은 방식의 환경 스위치로 개발용으로 보존한다.
- **완료 기준**: IdP 로그인 → 권한그룹별 메뉴 노출 확인 + 스모크의 세션 부정 검증
  (변조·만료) 4건이 SAML 세션에서도 통과.

## 3. MS-SQL 전환

- **착수 조건**: 운영 DB 인프라 확정.
- **작업**: `lib/store.ts` 의 컬렉션을 테이블로 이관(요구사항의 테이블 힌트:
  service_requests·failures·modifications·company_pledges·printout_discards·violations·
  excel_templates·pages·boards). 뮤테이션이 전부 서버 액션의 `getStore()` 경유이므로
  저장 계층 교체 지점이 한 곳이다. 개인정보 컬럼 암호화(제품안내서 §VI) 포함.
- **완료 기준**: 게이트 3종을 DB 백엔드로 전부 통과 + 동시 사용자 부하 기본 검증
  (`node scripts/load_probe.mjs` — 인메모리 기준선 2026-08-13: 동시 25 × 4라운드
  100요청 오류 0건, p50 182ms · p95 270ms. DB 전환 후 이 수치와 비교).

## 4. 요구사항 재감사 잔여 — 실구현 시 복원 목록

v1.0.35 재감사(메뉴 시트 84행 전수 대조)에서 확인된, **데모 스코프 통합으로 의도적으로
접은 화면·엔터티**다. 패치 트랙 소진 후 오너 지시로 **v1.1.x 요구 복원 트랙**을 열어
순차 구현한다 (완료 항목은 목록에서 제거, 게이트 3종 규칙 동일).

- ~~랙관리·랙구성도·H/W관리 + 서버 cpu·메모리~~ — v1.1.5 완료
- ~~인프라 잔여 CRUD (서버·시스템·배치·인터페이스)~~ — v1.1.6 완료
- ~~재택근무 대상자 명단~~ — v1.1.0 완료
- ~~보안점검 기준관리 CRUD·엑셀·중분류~~ — v1.1.2 완료
- ~~특별서약서(프로젝트)~~ — v1.1.3 완료
- ~~CI SR 결재 없는 접수~~ — v1.1.1 완료
- ~~메뉴권한 할당 (72행)~~ — v1.2.0 완료 (런타임 제한 오버레이 — 축소 전용·ADMIN 잠금 방지)
- 환경설정 잔여 2종: 사용자그룹 등록·메뉴기능 정의 (67·70행 — 그룹·기능 엔터티와
  사용자 매핑은 SSO 어설션 매핑·MS-SQL 전환과 함께 설계)
- ~~엑셀 다운로드 전면 확대~~ — v1.1.4 완료 (16종)

**제품안내서 17p 대조 감사(v1.3.0)에서 확인된 잔여** — 데모 스코프 단순화로 가이드보다
얕게 구현된 지점. ~~적용요청서 결재~~·~~양식 파일 업로드~~ 는 v1.3.0 마감, 나머지는
실구현 시 우선순위 합의 대상:

- ~~경영계획 중간 단계(취합·효율화)~~ v1.3.4 · ~~지급 일정~~ v1.3.1 · ~~초과 표시~~ v1.3.2 ·
  ~~속보 이력~~ v1.3.3 — III장 잔여: 계약 품의(결재)는 결재 시트 14건에 없음(가이드-시트
  불일치, 실구현 시 합의)
- 전 모듈 임시저장(SR v1.3.1·정산 v1.3.3 완료 — 나머지 모듈은 등록/상신 분리 구조라 해당 없음) (II장)
  — ~~프로젝트 투입 인력 명단~~ v1.3.3 (참여 서약 대상 기준 연계)
  — ~~장애 항목·조치기준 코드화~~·~~주기별 장애 통계표~~ v1.3.1
- 입사·복직 판별 미서약 메일(근태 연동 필요), ~~재택 주기 설정(현재 월 고정)~~ — v1.5.95 완료
  (RemoteCycle 매일·월·분기·반기 · remotePeriodKey/Bounds · 대상자관리 화면 주기 select · 기본 월 무회귀),
  ~~점검 2단 결재~~ — v1.4.0 (다단 결재 엔진, 결재선 2차 지정)
  — ~~출력물 미등록 안내메일~~·~~점검 대상 필드~~ v1.3.1 · ~~이관 배치 자동화~~·~~교육 게시이력~~ v1.3.2
- ~~SR 중지(BA030014) 진행상태~~ — v1.5.96 완료 (결재 시트 4·6·7 '진행상태 SR중지 시 반영 안함':
  SrStatus '중지'·suspendedFrom, suspend/resume 서버액션(결재 대기 없는 활성단계만·재개 시 직전상태 복원),
  지연목록·SR지연 알림·대시보드·replan 배제 가드, e2e sc_sr_suspend)
- ~~보안성검토 화면~~ — v1.5.102~105 완료 (제품안내서 VI장 '시큐어코딩·취약점 점검·자료 제출':
  SecurityReview 엔터티·시드, 보안 컴플라이언스 그룹 화면(계획→진행→조치중→완료, 발견/조치·증적 기록,
  조치율 div-zero·false-100 방어, 완료는 발견 전건 조치 후만), 엑셀 export, 빈/손상 스토어 38/38, e2e sc_security_review,
  대시보드 '미조치 취약점' 운영 스냅샷 신호(canSee 게이트·openFindings 단일원천, v1.5.107),
  일일 배치 '착수 예정일 경과' 안내메일(검토자 재직 교집합, v1.5.108) — 전 계층 통합. 게이트 68/68)
- 객체관리(74행 — 화면 라벨은 v1.3.0 에서 '공통코드'로 정직화), 서버세팅 화면,
  양식관리 기반 xlsx 출력(현재 고정 컬럼 CSV), 기능(버튼) 단위 권한 편집 (V장)
- 가이드 정오: 출력물 상신 주체는 시트 변경 협의대로 '본인'(가이드의 '부서' 문구 구버전),
  화면 수 70+ 는 데모 통합 전 기준(현행 37), 결재 14건은 시트 행 기준(문서 유형 12종이 커버)

## 5. 데모 피드백 반영

- **착수 조건**: 시연(docs/데모_시나리오.md) 후 고객 피드백 접수.
- **작업**: 피드백을 요구사항 시트 형식으로 정리해 우선순위 합의 후, 기존 사이클
  방식(폐쇄 루프 단위 + 게이트 + 커밋)으로 반영.

## 운영 수칙

- 어떤 변경이든 커밋 전 `npm run gates` (빌드 + 스모크 + 헬스 + e2e) 를 돌린다.
- 새 화면·권한은 `components/chrome/menus.ts` 한 곳에서 시작한다(내비·가드·매트릭스·
  스모크가 따라온다). 새 결재 유형은 `ApprovalDocType` 추가 → `draftApproval` 상신 →
  `work/approvals/actions.ts` 전파 순서다.
- 결함을 고치면 같은 커밋에 재발 방지 체크(스모크 니들 또는 e2e 시나리오)를 함께 넣는다.
- 주기적으로 `npm audit` 를 확인한다 — 전이 의존성 취약점은 프레임워크 다운그레이드
  대신 `overrides` 로 패치 버전을 강제하고, 의존성이 바뀌면 게이트 전체와 Docker
  빌드·기동을 재검증한다 (선례: v1.0.20).
- 실배포 환경변수: `SESSION_SECRET`(랜덤 키 필수), HTTPS 종단이면 `PORTAL_COOKIE_SECURE=1`,
  영속화는 `PORTAL_DATA_FILE`, 알림 배치 주기는 `PORTAL_NOTIFY_INTERVAL_MS`.
- 병렬 세션과 같은 저장소를 쓸 때 테스트 포트는 3500번대(이 프로젝트) 를 유지한다.
