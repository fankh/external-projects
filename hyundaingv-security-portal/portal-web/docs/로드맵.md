# 로드맵 — v1.0 이후

v1.0(태그 `portal-web-v1.0`)은 데모·제안 범위의 봉인 지점이다. 요구사항 3개 시트가
소진됐고 게이트 3종이 녹색이므로, **신규 기능**은 아래 실고객 접점에서만 시작한다.
각 항목은 착수 조건(외부 입력)과 완료 기준을 함께 적는다.

예외로 **v1.0.x 패치 트랙**은 봉인과 무관하게 허용된다 — 결함 수정, 기존 폐쇄 루프의
공백 보강, 게이트(스모크·e2e) 검증 추가, 문서 정합. 새 화면·새 요구사항은 여기에
속하지 않는다. (v1.0.2~v1.0.12: 묶음 채번 결함, 반려→재상신 할일 일원화·방치 알림·
이전 회차 이력, 감사 '결재 상신', 영속화 구버전 머지·채널 토글, 세션 Secure 옵션,
접근 통제·CSV 부정 검증, 교육 완료 가드 — 상세는 README 단계 표.)

## 1. 고객사 어댑터 실구현

- **착수 조건**: 고객사 시스템 접속 정보·API 명세(그룹웨어 메일/결재, 인사·근태,
  자산관리, 보안·출력물 DB) 접수.
- **작업**: `lib/integrations/` 에 어댑터 구현 추가, `profiles/<고객사>.config.ts` 작성,
  `portal.config.ts` 선택지 등록. 포털 본체는 무변경이 원칙 — 본체 수정이 필요해지면
  어댑터 계약(`lib/integrations/types.ts`)의 결함이므로 계약을 먼저 고친다.
- **완료 기준**: e2e `profile` 시나리오와 동형의 고객사 시나리오 추가 통과 +
  스테이징에서 실 시스템 대상 왕복 1회(메일 수신, 자산번호 취득 등) 확인.

## 2. SAML SSO 연동

- **착수 조건**: 고객사 IdP 메타데이터(엔드포인트·인증서) 접수.
- **작업**: 로그인 화면의 목업 계정 선택을 SAML 어설션 처리로 교체. 세션 발급은
  기존 `signSession()` 을 그대로 쓰고, 어설션의 사번·권한그룹을 `Session` 으로 매핑한다.
  목업 로그인은 `PORTAL_PROFILE` 과 같은 방식의 환경 스위치로 개발용으로 보존한다.
- **완료 기준**: IdP 로그인 → 권한그룹별 메뉴 노출 확인 + 스모크의 세션 부정 검증
  (변조·만료) 4건이 SAML 세션에서도 통과.

## 3. MS-SQL 전환

- **착수 조건**: 운영 DB 인프라 확정.
- **작업**: `lib/store.ts` 의 컬렉션을 테이블로 이관(요구사항의 테이블 힌트:
  service_requests·failures·modifications·company_pledges·printout_discards·violations·
  excel_templates·pages·boards). 뮤테이션이 전부 서버 액션의 `getStore()` 경유이므로
  저장 계층 교체 지점이 한 곳이다. 개인정보 컬럼 암호화(제품안내서 §VI) 포함.
- **완료 기준**: 게이트 3종을 DB 백엔드로 전부 통과 + 동시 사용자 부하 기본 검증.

## 4. 요구사항 재감사 잔여 — 실구현 시 복원 목록

v1.0.35 재감사(메뉴 시트 84행 전수 대조)에서 확인된, **데모 스코프 통합으로 의도적으로
접은 화면·엔터티**다. 패치 트랙 소진 후 오너 지시로 **v1.1.x 요구 복원 트랙**을 열어
순차 구현한다 (완료 항목은 목록에서 제거, 게이트 3종 규칙 동일).

- ~~랙관리·랙구성도·H/W관리 + 서버 cpu·메모리~~ — v1.1.5 완료
- ~~인프라 잔여 CRUD (서버·시스템·배치·인터페이스)~~ — v1.1.6 완료
- ~~재택근무 대상자 명단~~ — v1.1.0 완료
- ~~보안점검 기준관리 CRUD·엑셀·중분류~~ — v1.1.2 완료
- ~~특별서약서(프로젝트)~~ — v1.1.3 완료
- ~~CI SR 결재 없는 접수~~ — v1.1.1 완료
- ~~메뉴권한 할당 (72행)~~ — v1.2.0 완료 (런타임 제한 오버레이 — 축소 전용·ADMIN 잠금 방지)
- 환경설정 잔여 2종: 사용자그룹 등록·메뉴기능 정의 (67·70행 — 그룹·기능 엔터티와
  사용자 매핑은 SSO 어설션 매핑·MS-SQL 전환과 함께 설계)
- ~~엑셀 다운로드 전면 확대~~ — v1.1.4 완료 (16종)

**제품안내서 17p 대조 감사(v1.3.0)에서 확인된 잔여** — 데모 스코프 단순화로 가이드보다
얕게 구현된 지점. ~~적용요청서 결재~~·~~양식 파일 업로드~~ 는 v1.3.0 마감, 나머지는
실구현 시 우선순위 합의 대상:

- 경영계획 중간 단계(취합·효율화), 계약 다건·초과 검증, 속보 이력 (III장) — ~~지급 일정~~ v1.3.1
- 전 모듈 임시저장(SR 은 v1.3.1 완료, 잔여: 정산 등), 프로젝트 투입 인력 명단 (II·III장)
  — ~~장애 항목·조치기준 코드화~~·~~주기별 장애 통계표~~ v1.3.1
- 입사·복직 판별 미서약 메일(근태 연동 필요), 재택 주기 설정(현재 월 고정),
  이관 배치 자동화, 점검 2단 결재, 교육 게시이력 전용 화면 (IV장)
  — ~~출력물 미등록 안내메일~~·~~점검 대상 필드~~ v1.3.1
- 객체관리(74행 — 화면 라벨은 v1.3.0 에서 '공통코드'로 정직화), 서버세팅·보안성검토
  화면, 양식관리 기반 xlsx 출력(현재 고정 컬럼 CSV), 기능(버튼) 단위 권한 편집 (V장)
- 가이드 정오: 출력물 상신 주체는 시트 변경 협의대로 '본인'(가이드의 '부서' 문구 구버전),
  화면 수 70+ 는 데모 통합 전 기준(현행 37), 결재 14건은 시트 행 기준(문서 유형 12종이 커버)

## 5. 데모 피드백 반영

- **착수 조건**: 시연(docs/데모_시나리오.md) 후 고객 피드백 접수.
- **작업**: 피드백을 요구사항 시트 형식으로 정리해 우선순위 합의 후, 기존 사이클
  방식(폐쇄 루프 단위 + 게이트 + 커밋)으로 반영.

## 운영 수칙

- 어떤 변경이든 커밋 전 `npm run gates` (빌드 + 스모크 + 헬스 + e2e) 를 돌린다.
- 새 화면·권한은 `components/chrome/menus.ts` 한 곳에서 시작한다(내비·가드·매트릭스·
  스모크가 따라온다). 새 결재 유형은 `ApprovalDocType` 추가 → `draftApproval` 상신 →
  `work/approvals/actions.ts` 전파 순서다.
- 결함을 고치면 같은 커밋에 재발 방지 체크(스모크 니들 또는 e2e 시나리오)를 함께 넣는다.
- 주기적으로 `npm audit` 를 확인한다 — 전이 의존성 취약점은 프레임워크 다운그레이드
  대신 `overrides` 로 패치 버전을 강제하고, 의존성이 바뀌면 게이트 전체와 Docker
  빌드·기동을 재검증한다 (선례: v1.0.20).
- 실배포 환경변수: `SESSION_SECRET`(랜덤 키 필수), HTTPS 종단이면 `PORTAL_COOKIE_SECURE=1`,
  영속화는 `PORTAL_DATA_FILE`, 알림 배치 주기는 `PORTAL_NOTIFY_INTERVAL_MS`.
- 병렬 세션과 같은 저장소를 쓸 때 테스트 포트는 3500번대(이 프로젝트) 를 유지한다.
