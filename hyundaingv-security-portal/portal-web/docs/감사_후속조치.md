# 적대 감사 후속조치 (미조치 잔여 항목)

v1.5.x 에서 9개 핵심 서브시스템(결재·영속화·SR/변경·컴플라이언스·재무·보안의식·인프라·
게시판/설정, 그리고 직전 델타)을 적대적으로 감사해 확정 결함을 전수 수정했다. 아래는 감사가
찾은 저심각·잠재 항목의 처리 현황이다. 전부 상태 불일치·데이터 손상이 아니라 알림/가시성/
정합성 성격이며, portal-web 은 본 트랙 단독 소유다(병렬 v1.24x~ 트랙은 별개 프로젝트
`seekerslab-itam/itam-web` 소속으로 portal-web 파일을 건드리지 않는다).

**#1~#5 전부 조치 완료(v1.5.31·v1.5.33·v1.5.32·v1.5.35·v1.5.36).** #2 타임스탬프 승격은
v1.5.24 프로젝트 과다계수 회귀 테스트까지 가능케 해 실증했고(v1.5.34), #4 는 비-일반 양식
개정 재서약 통지를 일반과 동일하게 확장했다. #5 는 개인 폐기 상신이 부서 취합('취합') 할일을
닫지 않도록 방어(휴면이나 실데이터 대비). **적대 감사 9종 + 후속조치 5종 전부 종결.**

| # | 상태 | 위치 | 내용 | 심각도 | 비고 / 권장 조치 |
|---|:---:|------|------|:---:|-----------|
| 1 | ✅ v1.5.31 | `work/approvals/actions.ts` | SR 신청 승인이 `'SR 처리'` 할일을 안 만들어 `assignCi` 마감 루프가 반쪽이었음. | 저 | 승인 시 업무담당(BIZ_MGR)에게 할일 생성(시드 TD-104 형태), assignCi 가 마감. e2e sr 회귀테스트 추가. |
| 2 | ✅ v1.5.33 | 서약 상태 판정 전 화면 | 서약 유효 판정 `signedAt >= revisedAt` 가 날짜 단위라 같은 날 개정 전 서명이 유효로 남던 문제. | 저~중 | `nowStampSec()`(초 KST)로 서약·개정 시각 타임스탬프 승격, 개정=현재시각 확정. 표시는 date. e2e revision 재현(당일 서명→개정→무효화 8명) + 프로젝트 과다계수 회귀(v1.5.34) 실증. |
| 3 | ✅ v1.5.32 | `dashboard`·`work/approvals`·`notify` | 일반 서약 집계 3곳에 `year` 필터 누락(다른 뷰는 있음) — 레거시 데이터 파일에서만 갈리는 잠재 불일치. | 저(latent) | 세 곳에 `p.year === '2026'` 추가해 전 뷰 정합. |
| 4 | ✅ v1.5.35 | `pledge/manage`·`pledge/my`·`notify` | 비-일반 양식(관리책임자·재택·특별·프로젝트) 개정이 서명 무효화만 하고 재서약 할일·안내·리마인더가 없던 문제. | 저 | 사용자 결정(기본값)으로 일반과 동일하게 확장: reSignTargets 로 유형별 의무자(관리책임자=비-USER 계정·재택=당월 대상자·특별=보안담당자·프로젝트=진행중 참여인력) 산정, 개정 시 유형 명시 재서약 할일+안내메일, notify 일배치 리마인더, 서명 시 **유형별** 마감(교차마감 방지). e2e pledge_multikind 로 교차마감 방지 실증. |
| 5 | ✅ v1.5.36 | `awareness/prints` (`submitDiscards`) | 개인 폐기 제출이 부서 취합 할일(시드 TD-103)까지 닫던 문제. | 매우 저(휴면) | 개인 상신 마감에서 부서 취합('취합' 포함 제목) 할일을 제외해 조기 마감 방지. printouts 시드가 비어 게이트 미실행(early return)이라 무회귀·코드 검토 검증. 개인 폐기 전용 todo 모델은 데이터 경로 구축 시 별도 확장. |

> 참고: 확정 결함(수정 완료)과 감사 범위는 `docs/구축_요약.md`·커밋 이력(v1.5.14~30) 참조.
> 재사용 프레임워크 온보딩은 `docs/어댑터_연동_가이드.md`·`docs/배포_운영_가이드.md`.

## 결재 엔진 최대정밀 재감사 (v1.5.46~47)

5개 적대 렌즈(다단 큐·전파·재상신/재번들·회수·인가/멱등) 발견 → 렌즈별 반증 검증(13 에이전트)으로
확정 8건. 상태 손상·인가 우회·스톨은 없음(전 전파 블록 상태가드 정확, 회수는 forward 의 정확한
역전이, 자기결재 대체·큐 전진 건전). 확정분은 이력/할일 매칭 정밀성과 표현/추적성.

**수정 완료:**

| # | 상태 | 위치 | 내용 | 심각도 |
|---|:---:|------|------|:---:|
| A1 | ✅ v1.5.47 | `work/approvals/page.tsx` | 비회전 '이전 회차' 이력이 `ref` 만으로 묶여, 같은 SR 번호를 ref 로 공유하는 SR 신청↔적용요청·변경계획↔변경결과가 교차 노출(적용요청 결재자=시스템관리자가 SR 신청 반려사유 열람 = 비당사자 유출). `docType` 조건 추가. | 중(접근통제) |
| A2 | ✅ v1.5.47 | `lib/approvals.ts` | 재상신 할일 닫기가 `title.includes(ref)` 부분문자열이라 (a) 반려 사유에 인용된 타 건 ref 로 무관 할일 오마감, (b) 같은 ref 공유 문서 교차마감. 유형+ref 선두 고정(`startsWith`) 매칭. e2e `reject_reason_collision` 회귀(stash 실측 1 vs 0). | 중 |
| A3 | ✅ v1.5.47 | `work/approvals/actions.ts` | ref 없는 결재(시드 AP-2026-0709) 반려 시 ap.id 로 키된 재상신 할일이 닫기키(엔티티 ref)와 달라 영구 미마감·방치알림. ref 있을 때만 재상신 할일 생성. | 저 |

**미조치(저심각·데이터모델/표현 성격, 실데이터·시연 무해):**

| # | 위치 | 내용 | 심각도 | 권장 조치 |
|---|------|------|:---:|-----------|
| B1 | `work/approvals/page.tsx`·`actions.ts` | 다단 중간 결재자가 승인 후 자기 결재 UI 흔적을 전부 잃음(inbox·상세 접근이 현재 approver 스칼라 기준, 할일 마감). 감사 로그에는 남음. 점검결과 상신(유일 다단)만 해당. | 저(추적성) | Approval 에 결재자 이력 배열 추가 후 inbox/상세를 '과거 결재자 포함'으로 확장(데이터모델 변경). |
| B2 | `actions.ts`(반려 전파)·`page.tsx`(refSummary) | 묶음(장애·출력물·서약) 반려가 역링크(reportRef/approvalRef)를 지워, 반려된 과거 결재 상세가 'N건'→0건으로 빈다(제목엔 N건). | 저(표현/감사완결) | 상신 시 묶음 건수/스냅샷을 결재에 저장해 상세가 역링크 없이도 재구성. |
| B3 | `page.tsx`(회전 이력) | 회전 이력이 같은 유형·기안자의 모든 과거 배치를 포집해, 무관한 월별 배치가 '이전 회차'로 표기(동일 당사자, 유출 없음). | 저(표현) | 헤더 문구를 '이전 배치'로 명확화하거나 회차 계보 링크 도입. |

> 재감사 결론: 결재 엔진의 상태·인가·전파 코어는 건전하며, 확정분은 문자열 매칭 정밀성(A1~A3, 수정)과
> 다단/묶음의 표현·추적성(B1~B3, 저심각 보고)에 국한된다. B1~B3 는 데이터모델·표현 변경이라 실고객
> 결재 UX 피드백 시 함께 반영 권장.

## 영속화/스토어 엔진 최대정밀 재감사 (v1.5.49)

5개 적대 렌즈(로드 머지·저장 내구성·라이프사이클·싱글턴/캡·참조 무결성) 발견 13건 → 반증 검증(14
에이전트)으로 확정 3건, 반증 6건. 코어(디바운스 저장·SIGTERM 플러시·백업 순서·로그 3종 캡)는 건전
재확인. 확정분은 손상 파일 처리(손실·요소/중첩 검증).

**수정 완료(전부 v1.5.49):**

| # | 상태 | 위치 | 내용 | 심각도 |
|---|:---:|------|------|:---:|
| D1 | ✅ | `lib/store.ts loadFromFile catch` | 파싱 불가 DATA_FILE → seed 폴백 → 읽기 트리거 저장(scheduleSave)이 300ms 뒤 손상 원본을 빈 시드로 덮어써 복구 불가 손실(스케줄러 off 배포는 .bak 도 없어 전손). catch 에서 원본을 `.corrupt.<시각>` 격리 보존 후 seed 기동. e2e emptystate quarantine 프로브. | 높음 |
| D2 | ✅ | `lib/store.ts loadFromFile` | v1.5.14 타입 안전 머지가 상위 타입만 봐, 요소가 객체여도 시드에서 배열/숫자인 중첩 필드(codeGroups[].values·systems[].serverIds·investPlan[].amount)가 어긋나면 소비처 `.length/.filter/.toLocaleString` 가 전 화면 500. 요소 필터(원시값 제거) + 시드 형판 기준 배열→[]·숫자→0 정규화. corruptfile 프로브에 values 누락 그룹. | 높음 |
| D3 | ✅ | `lib/store.ts nextNo` | id 없는 요소가 남으면 existing 에 undefined 섞여 `undefined.startsWith` 가 상신 서버액션 500. `typeof n === 'string'` 가드. | 중 |

**반증(비결함, 참고):** null 요소 크래시 — 요소 필터가 제거(반증); 배치 ref 교차링크 — incidents.reportRef 와
approvals 는 원자 공동저장이라 부분 파일에서 갈리지 않음(반증); dangling ref — 의도된 per-key graceful
degradation, 소비처가 .find/.filter 로 미스 허용(반증); 외부 파일 삭제 — 스케줄러 틱의 audit 뮤테이션+백업
saveNow 로 자가치유(반증, existsSync 가드는 값싼 하드닝 여지); SIGTERM drain 선점 — 뮤테이션이 await 전
선적용돼 플러시가 보전(반증, store 의 SIGTERM 핸들러는 Next 가 이미 처리해 중복); 업무 컬렉션 무한증가 —
로그(cap)와 달리 승인/할일/첨부는 system-of-record, 캡이 되레 감사추적 손실(반증). **워크플로 반증 패스가
진행중이던 내 요소필터 수정을 검증하고 부족분(중첩 필드)을 정확히 지목 — 선제수정+반증검증의 이상적 협업.**

## SR/변경 전파 엔진 최대정밀 재감사 (v1.5.50)

5개 적대 렌즈(SR 상태전이·변경 2단·교차 전파·동시성/멱등·직접접수/할일) 발견 9건 → 반증 검증(14
에이전트)으로 확정 2건, 반증 7건. 코어(상태전이·kind 분기·SR 완료 전파·2단 변경·큐 전진)는 건전 재확인.

**수정 완료(전부 v1.5.50):**

| # | 상태 | 위치 | 내용 | 심각도 |
|---|:---:|------|------|:---:|
| E1 | ✅ | `infra/changes submitPlan/submitResult` | 변경은 공유 워크스페이스(담당·Admin)라 재상신자가 원 기안자와 다를 수 있는데, draftApproval 의 기안자-매칭 닫기에만 의존해 타 담당 재상신 시 원 기안자 재상신 할일이 고아 + 방치 알림 무한 반복. `closeChangeResignTodo`(유형+변경번호, 소유자 무관)로 닫음 — SR 재상신(requester 바인딩)과 동등. e2e change_resign_orphan. | 중 |
| E2 | ✅ | `sr/new createSr` | 멱등 가드 부재 — 상신 폼 이중 클릭이 SR·결재·할일을 두 벌 생성(타 전이는 상태 게이트). 같은 신청자·시스템·제목의 결재중 SR 존재 시 중복 무시. | 중 |
| E0 | ✅ | `sr/ci assignCi/baReject` | (선제) 'SR 처리' 할일 누수 2경로: assignCi 가 owner==me.name 로 닫아 ADMIN 배정 시 방치 + baReject 미닫음. 유형+SR번호로 양 경로 닫기. **워크플로 반증 패스가 이 수정을 확인(고쳐진 코드 읽고 finding 반증).** | 저 |

**반증(비결함, 참고):** SR 상태전이 스톡 — 각 상태에 유효 액션 존재(반증); SR 완료 전파 조건(sr.status==='적용요청')
— 변경 사슬 동안 SR 동결로 정확(반증); 2단 변경 중복 상신 — 상태 가드(작업등록/작업등록승인)로 재상신 차단(반증);
직접접수 CI(receiveCiSr) — srRequests 와 격리(반증); withdraw 역전이 — 정확한 forward 역(반증); advance 이중
호출 — nextOf(테스트)=undefined 안전(반증); addDevChange 중복 링크 — srNo dedup(반증). **E0(내 선제수정)를
워크플로가 읽고 반증 — 3연속 재감사(알림·영속화·SR/변경)에서 선제수정+반증검증 협업 패턴 확립.**

## 알림/스케줄러 엔진 최대정밀 재감사 (v1.5.48)

5개 적대 렌즈(타깃팅·스케줄러 동시성·멱등·실패 관측성·기록/백업) 발견 17건 → 반증 검증(14 에이전트)으로
확정 3건, 반증 14건. 코어(어댑터 예외/타임아웃 내성 v1.5.16~17, batch0 dedup, SR지연 CI 스코프, 채널
중지 실패기록)는 건전 재확인. 확정분은 연도 리터럴·백업 관측성/커버리지.

**수정 완료(전부 v1.5.48):**

| # | 상태 | 위치 | 내용 | 심각도 |
|---|:---:|------|------|:---:|
| C1 | ✅ | `lib/notify.ts` 외 6곳 | 미서약 안내가 `year === '2026'` 리터럴로 굳어(다른 날짜는 today() 파생), 연도 전환 후 연차 서약 리마인더가 조용히 멈춤(2027 이후 미서약자 미통지). `currentYear()`(today 파생) 헬퍼로 notify·dashboard·pledge/{dept,manage,my}·export 통일. 2026 불변. | 높음 |
| C2 | ✅ | `lib/store.ts backupDataFile` | 디스크 오류에도 null 반환(스킵과 동일)이라 틱이 성공만 기록 → 지속 백업 실패가 배치 이력·로그에 안 보임(광고된 '복구 지점' 부재를 운영자가 모름). catch 에서 '실패' 배치 기록. | 중 |
| C3 | ✅ | `platform/integrations`·`store.ts` | backupDataFile 이 스케줄러 틱에서만 호출 → 외부 스케줄러 사용(NOTIFY_INTERVAL 미설정)+영속화 켬 배포에선 스냅샷 전무. 수동 '알림 배치 실행'도 백업 남기게 배선. e2e `manual_backup` 회귀. | 중 |
| C0 | ✅ | `instrumentation.ts` | (선제) async tick 이 인터벌보다 길 때 setInterval 이 다음 tick 을 발화해 겹침 → 중복 발송·동시 뮤테이션. `__ngvNotifyTicking` 재진입 가드. 워크플로 반증 패스가 이 가드 존재를 확인. | 저(방어) |

**반증(비결함, 참고):** batch0 성공 마스킹 — batch0 은 자체 실패를 배치 이력에 기록(반증); SR지연이 '적용요청결재중'
누락 — 제외 축이 CI 배정 유무라 정상, 제외하면 되레 지연 알림 소실(반증); 수동+스케줄 동시 이중발송 — 목 어댑터가
마이크로태스크로 직렬화돼 재현 불가(실 async 어댑터 배포 시 저심각 잠재만, 수동 경로는 __ngvNotifyTicking 미공유가
잠재 갭). 매 틱 재발송은 의도된 일일 리마인더(반증). year 리터럴은 앱 전역 데모 상수였으나 notify 의 무관측 특성이 높음.

## 컴플라이언스/ISMS 엔진 최대정밀 재감사 (v1.5.51)

5개 적대 렌즈(교육 이수율·점검 생애·ISMS 기준 CRUD·인가/검증·컴플라이언스 정합) 발견 12건 → 반증
검증(14 에이전트)으로 확정 2건(교육 target 무시는 5 finder 중복), 반증 다수. 점검(2단 결재·상태전이·
referential 무결성·CSV 검증)과 이수율 재직자 스코프(v1.5.24)는 건전 재확인.

**수정 완료(전부 v1.5.51):**

| # | 상태 | 위치 | 내용 | 심각도 |
|---|:---:|------|------|:---:|
| F1 | ✅ | `compliance/education`·`store`·`export` | 이수율이 과정 대상(전임직원/개발자/보안담당자)을 무시하고 완료 과정 전부를 전 재직자에게 요구 → 개발자 전용 과정을 개발자가 100% 이수해도 비개발자 6명이 '미이수', 전사 이수율 88%→56% 급락(감사 지표 오표기 + 개인 허위 비준수). `eligibleForCourse`(개발자=부서 '개발', 보안담당자=securityOfficers, 그 외=전 재직자) 단일 원천으로 이수율·개인상태·미이수 명단·N/M·USER 화면·엑셀 전부 대상 스코프. e2e education_target. | 높음 |
| F2 | ✅ | `compliance/education registerAttendees` | '보안교육' 할일을 유형만으로 닫아, 다른 과정 이수 시 무관한 과정의 교육 의무 할일이 닫혀 실제 미이수가 대시보드에서 숨음(할일-이수 괴리). 할일 제목의 과정명으로 좁혀 그 과정 할일만 닫음. | 중 |
| F0 | ✅ | `compliance/education` (과정별 이수 인원 N/M) | (선제) recordsOf(c.id).length 가 퇴사자 기록까지 세어 '이수 인원 > 대상'(6/5) 표기 → 재직자+대상 스코프로 통합. **워크플로 반증 패스가 이 수정을 확인(고쳐진 코드 읽고 orphan finding 반증).** | 저 |

**반증(비결함, 참고):** 과정 완료=첫 기록 시 확정(의도된 '세션 개최' 의미); inspection 상태전이·2단 점검결과 전파
(완료는 최종 승인만)·referential(deleteItem 이 plan 참조 시 삭제 차단)·CSV 무효 주기 건너뜀·addPlan inspector 검증;
addCourse/registerAttendees 무감사(개인 submission 아닌 관리행위지만 도메인 레코드가 추적 — 감사정책 §5 경계).
**F0(내 선제수정)를 워크플로가 읽고 반증 — 5연속 재감사에서 선제수정+반증검증 협업 패턴 일관 확립.**

## 재무/자금 엔진 최대강도 재감사 (v1.5.52)

5개 적대 렌즈(집행률·계약/투자·속보 수정·인가/스코프·감사 정합) → 반증 검증. 자금 코어(집행률 확정
스코프 v1.5.25·크로스-kind export v1.5.25·연도 필터)는 건전 재확인, 표시·감사 정합 2건만 확정. optimizePlan
가설은 상태 가드로 자기 반증.

**수정 완료(전부 v1.5.52):**

| # | 상태 | 위치 | 내용 | 심각도 |
|---|:---:|------|------|:---:|
| M1 | ✅ | `finance/invest` | 계약 체결 stat 의 건수 note 가 종류(자본/비용) 무관하게 전체 `investContracts` 를 세어, 자본 화면에 비용 계약까지 합산된 교차집계 누수 → 해당 종류 `kindContracts` 건수만 표기(비용 화면은 이미 정상). | 저 |
| M2 | ✅ | `finance/expense`·`lib/audit` | 비용 속보 예상액 수정 감사가 '공통코드 변경' 액션으로 오기록 → 신설 controlled-vocabulary '재무 속보 수정' 으로 정정. | 저 |

**반증(비결함):** 집행률 확정 스코프·크로스-kind export·연도 필터·optimizePlan(상태 가드) 모두 건전.

## 임직원 의식제고 엔진 최대강도 재감사 (v1.5.53)

5개 적대 렌즈(재택 명단 생애·출력물 폐기 폐쇄루프·서약/교육 연계·인가/스코프·집계 정합) → 반증 검증.
재택 명단 경계월 이중 집계(중)와 부서 취합 할일 고아(저) 확정, 내 선제 '역순 등록 거부' 수정은 워크플로가
읽고 반증(확인). importDaily 중복·서약 시각정밀은 반증.

**수정 완료(전부 v1.5.53):**

| # | 상태 | 위치 | 내용 | 심각도 |
|---|:---:|------|------|:---:|
| A1 | ✅ | `awareness/remote`·`export` | 한 사람이 인접한 두 재택 기간(경계월 전환)으로 당월 명단·통계·엑셀에 둘로 잡히던 이중 집계 → (a) 역순/열린 기간 중복 등록 거부, (b) 구 기간을 신규 시작 **전일**로 마감(off-by-one: 당일 마감 시 그 달에 구·신 함께 걸림), (c) 화면·export 이름 기준 중복 제거. e2e remote_overlap. | 중 |
| A2 | ✅ | `awareness/prints` | 부서 출력물 폐기 '취합' 할일(TD-103)이 개인 폐기 상신으로는 닫히지 않아(v1.5.36 조기마감 차단) 닫을 경로 없이 영구 방치(기한 경과) → 부서담당 '부서 취합 완료' 버튼으로 명시적 마감 경로 추가. | 저 |

**반증(비결함):** 역순 두-열린-기간(선제수정이 처리 → 반증으로 확인); importDaily 일배치 중복 방지; 서약 당일
개정 시각정밀(v1.5.33). **6·7연속 재감사에서 선제수정+반증검증 협업 패턴 유지.**

## 권한/접근통제 엔진 최대강도 재감사 (v1.5.54)

5개 적대 렌즈(메뉴권한 write·서버액션 게이트 우회·nav-gate 발산·effectiveRoles 엣지·신원 게이트) → 반증
검증(7 에이전트). 코어 전량 건전 재확인: effectiveRoles 페일클로즈(미등록 href→[]→리다이렉트)·권한 상승/
ADMIN 잠금 원천 차단·requireMenuRole 교집합·nav-gate 락스텝(hiddenHrefs=effectiveRoles 파생)·결재 승인/
회수 신원 게이트(approver/drafter===me). 확정 0, PARTIAL 1(수정), 반증 1.

**수정 완료(v1.5.54):**

| # | 상태 | 위치 | 내용 | 심각도 |
|---|:---:|------|------|:---:|
| G1 | ✅ | `work/approvals/page.tsx` | 결재 상세 '이전 회차 이력'이 회전 문서(장애보고·출력물폐기·서약 현황·부서서약 현황)에서 유형+기안자만으로 매칭 → `selected` 는 신원 게이트(결재자/기안자 본인)되나 이력 쿼리는 재적용 안 함. 결재선 변경·다단 회부로 결재자가 바뀐 뒤엔 내가 관여 안 한 같은 기안자의 별개 묶음 반려사유·처리이력 노출. 이력 행에도 `approver===me||drafter===me` 재적용(기안자 조회는 본인 전 회차 유지). e2e approval_history_identity. **PARTIAL 판정**(결재자는 (기안자,유형)당 결정적이라 안정 구성에선 미교차, 결재선 재구성·큐 진행 시 도달 — 반증 패스가 정확히 지목, 권고 수정과 일치). | 저 |

**반증(비결함):** nav 가시성 이중 파생(AppShell.roles+hiddenHrefs vs 게이트 effectiveRoles) — 현재 수학적
등가(effectiveRoles⊆item.roles)이고 nav 는 서버 가드 아래 비하중(cosmetic)이라 접근 경계 불변. 유지보수 노트.
**교훈: 확정 0건이어도 PARTIAL(조건부 도달) 방어심화는 반증 패스의 권고와 일치하면 수정 가치 — 신원 게이트는
selected 뿐 아니라 파생 조회(history·첨부·요약)에도 재적용해야 일관.**

## 대시보드/집계 엔진 최대강도 재감사 (v1.5.55)

5개 적대 렌즈(집계 단일원천·스코프 누출·수치 정합·엣지/빈값·신선도) → 반증 검증(10 에이전트). 개인/전사
스냅샷 스코핑(canManage 게이트)·배지(todos·approvals) 정합·지연 SR 경계는 건전 재확인. 확정 2(수정)·PARTIAL 1·반증 3.

**수정 완료(전부 v1.5.55):**

| # | 상태 | 위치 | 내용 | 심각도 |
|---|:---:|------|------|:---:|
| D1 | ✅ | `dashboard/page.tsx` (myEduMissing) | '보안교육 미이수' 포틀릿이 과정 대상(eligibleForCourse)을 무시하고 완료 과정 전부를 세어 → 개발자 전용 과정이 비개발자 대시보드에 '미이수'로 오표기(v1.5.51 F1 클래스가 대시보드에 잔존). 교육 화면과 동일한 대상 스코프 단일 원천 적용. e2e dashboard_edu_scope. (선제 발견·수정, 반증 패스가 확인) | 중 |
| D2 | ✅ | `dashboard/page.tsx` (pledgeTodo) | '일반 서약' 타일이 kind '보안서약서' 할일 유무로 판정 — 이 kind 는 관리책임자·재택·특별·프로젝트 재서약이 공유(제목만 상이)라, 무관한 유형 개정 시 일반 서약 유효자가 '미제출'로 오표기되고 일반 서약 제출로는 안 닫혀(sign 은 '일반' 제목 할일만·유효 시 early return) 방치. 일반 서약 유효성(validSign) 단일 원천으로 판정(전사 미서약 집계와 정합). e2e dashboard_pledge_general. **워크플로 반증 패스가 확정(내 독립 패스는 놓친 finding — 워크플로가 보강).** | 중 |

**PARTIAL(무해):** 공지 정렬 `Number(pinned)` 이 손상 파일의 문자열 pinned("true")에 NaN → 정렬 미이동(크래시·
오수치 없음, 순서만). 타입안전 머지는 크래시 유발 필드(배열/숫자)만 정규화하는 의도적 범위라 불리언은 밖. 미조치.
**반증(비결함):** '점검 미등록·경과' 합성 지표는 라벨이 '미등록'을 포함해 전월 경과+당월 결과미등록을 함께
세는 의도된 집계(전용 화면 '기한 경과' 순수 overdue 와 다른 지표); 지연 SR strict-< 경계는 전용 화면과 동일;
미래월 결과미등록은 도달성 낮음. **교훈: 대시보드 집계는 도메인 화면의 단일 원천 헬퍼(eligibleForCourse·
validSign)를 반드시 재사용 — 재유도하면 원천이 갱신돼도 대시보드만 옛 규칙으로 남아 지표가 갈린다.**

## 통합 검색 엔진 최대강도 재감사 (v1.5.56)

5개 적대 렌즈(스코프 정합·필드 누출·쿼리 처리·결과 정합·엣지) → 반증 검증(16 에이전트). 확정 6·부분 5·반증 0.

**수정 완료(전부 v1.5.56):**

| # | 상태 | 위치 | 내용 | 심각도 |
|---|:---:|------|------|:---:|
| S1 | ✅ | `search/page.tsx` (도메인 게이트) | 검색이 도메인 노출을 me.role 리터럴(canManage·canViewFinance)로 판정 → 화면 가드는 requireMenu→effectiveRoles 라 런타임 메뉴권한(menuOverrides) 제한을 반영하는데 검색은 무시. ADMIN 이 특정 화면을 역할에서 제한해도 그 역할이 검색으로 SR·계약·장애·변경·프로젝트·게시판 데이터 열람(요구 72행 통제 우회). 각 도메인을 소속 화면 href 의 effectiveRoles 로 게이트(재무는 투자/비용 종류별 화면 분리 + canViewFinance 유지). e2e search_menu_override. (반증 패스가 5도메인 전수 확정) | 높음 |
| S2 | ✅ | `search/page.tsx` (has 매처) | `f?.includes(query)` 가 손상 파일의 비문자열 필드(숫자)에 TypeError 500 → `String(f ?? '').includes` 로 방어. | 중 |

**부분(무해/UX):** 과대 부분매칭(ID·연도 substring)·대소문자 구분 zero-result·React key 접두 관례 의존·무한 렌더
(페이지네이션 없음) — 저심각 UX/성능 노트. **교훈: 검색·리포트 등 집계 화면은 반드시 effectiveRoles(런타임 권한)로
게이트 — 정적 menus.ts 기본 역할 비교(내 초기 독립 패스가 이걸로 오판)로는 menuOverrides 런타임 제한을 못 잡는다.**

## 첨부/파일 업로드 엔진 최대강도 재감사 (v1.5.57)

5개 적대 렌즈(refId 인가·가시성 스코프·파일 검증·집계/dedup·엣지) → 반증 검증(23 에이전트). 보안 코어 건전
재확인: refId 는 호출부가 자기 신규 id 또는 검증된 기존 레코드(+역할 게이트)로만 전달(교차문서 오염 없음);
파일은 크기 상한·경로 정제·메타데이터 전용(디스크 미기록→트래버설·실행 없음)·CSV 미유출·React 이스케이프.
확정 3(집계/dedup)·부분 다수·반증 다수.

**수정 완료(v1.5.57):**

| # | 상태 | 위치 | 내용 | 심각도 |
|---|:---:|------|------|:---:|
| T1 | ✅ | `lib/attachments.ts` registerGenerated | 자동첨부 dedup 키가 파일명에 today() 를 포함해 당일만 방어 → 변경 계획/결과(안정 refId=cw.id)가 반려·회수 후 다른 날 재상신되면 같은 양식 자동첨부가 날짜만 달라 중복 누적, attachCount 부풀림 → 날짜 뺀 접두(양식명+버전) 기준 dedup(버전 상승 시 정상 재생성). e2e attach_generated_dedup. | 중 |

**잔여 저심각(문서화·미조치):** submitResult 업로드가 registerUpload 무-dedup 이라 반려·회수 재상신마다 결과파일
중복 누적(저, 선택 첨부·관리 워크스페이스); 변경 계획/결과 결재가 ref=cw.id 공유라 배지·상세 첨부가 단계 교차
합산(저, 상세는 신원 게이트·동일 변경 작업). 둘 다 count/표시 이슈로 보안 경계 무관 — 첨부에 docType 태깅 시 정리 가능.
**교훈: 자동생성 산출물 dedup 키에 날짜를 넣으면 '당일만' 방어가 된다 — 안정 식별자(refId+양식버전)로 dedup.**

## 알림/스케줄러 엔진 최대강도 재감사 2차 (v1.5.58)

5개 적대 렌즈(발송 폴트·스케줄러 타이밍·대상 정합·배치 기록·엣지) → 반증 검증(18 에이전트). 1차(v1.5.48)
이후 확정 3·부분 1·반증 9. 코어(sendVia withTimeout+catch 폴트격리·스케줄러 인터벌/finally/HMR·대상 정합) 건전.

**수정 완료(전부 v1.5.58):**

| # | 상태 | 위치 | 내용 | 심각도 |
|---|:---:|------|------|:---:|
| N1 | ✅ | `lib/notify.ts` (재서약 필터) | title 누락(undefined) 손상 할일에 `x.title.includes` 가 TypeError → 배치 전체 중단·후속 유형 전부 누락·실패기록 없음(수동 실행은 500) → `String(x.title ?? '')` 강제. e2e notify_corrupt_todo. | 높음 |
| N2 | ✅ | `pledge/{my,manage,dept}` | `const YEAR = currentYear()` 를 모듈 스코프에서 1회 평가·고정 → 프로세스가 연도 경계를 넘으면 서약 유효 판정·신규 레코드 year 가 전년에 묶여 재서약 사이클 미시작 + 호출시점 평가하는 notify/dashboard/export 와 영구 불일치 → 렌더/액션 호출 시점 평가로 이동. | 중 |
| N3 | ✅ | `lib/notify.ts`·`instrumentation.ts`·`platform/integrations` | 재진입 가드(__ngvNotifyTicking)가 스케줄러 틱 전용이라 수동 실행이 우회 → 겹침 시 중복 발송 → runDailyNotifyOnce 공유 래퍼로 두 경로 직렬화(가드를 래퍼 안으로 이동, 틱은 중복 가드 제거). | 저 |

**반증(비결함):** 미정의 수신자(ci/requester/inspector 부재)는 실무상 항상 정의; 빈배치 성공기록은 정상.
**교훈: 재진입 가드는 한 진입점(스케줄러)만 감싸면 다른 진입점(수동)이 우회한다 — 공유 래퍼로 전 경로 직렬화.**

## 세션/인증 엔진 최대강도 재감사 (v1.5.59)

5개 적대 렌즈(위조·로그인 무결성·쿠키/CSRF·생애주기·파싱) → 반증 검증(12 에이전트). 확정 0·부분 다수·반증 다수.
코드 레벨 코어 건전 재확인: 서명(HMAC-SHA256+timingSafeEqual 길이 가드)·만료(exp)·로그인(서버 ACCOUNTS
매핑, 클라이언트 역할 주입 불가)·쿠키(httpOnly·sameSite lax·secure(prod))·파싱(try/catch·null 가드)·로그아웃(delete).

**하드닝 완료(v1.5.59):**

| # | 상태 | 위치 | 내용 | 심각도 |
|---|:---:|------|------|:---:|
| SS1 | ✅ | `next.config.mjs` | HSTS(Strict-Transport-Security) 헤더 부재 — 어떤 env 로도 안 켜지던 잔여 → max-age 2년·includeSubDomains 추가(HTTPS 배포에서 쿠키 평문 유출 방어, http 는 브라우저가 무시해 무영향). 스모크 검증. | 저 |
| SS2 | ✅ | `lib/readiness.ts` | 'HTTPS 쿠키' 준비상태가 프로덕션에서도 info 라 세션 키 경고(warn)보다 낮게 노출 → 프로덕션 && Secure 미설정 시 warn 승격(동등 수준). | 저 |

**부분·미조치(배포 구성, 문서화):** SESSION_SECRET 미설정 시 공개 개발키로 세션 위조 가능 — 기동 경고·준비상태
카드·docker-compose(`:?` 강제) 3중 노출된 구성 리스크. getSession fail-closed(프로덕션+개발키 → 세션 전면 거부)는
정당한 방어심화이나, 게이트·http 데모가 개발키를 프로덕션 모드에서 쓰므로(스모크/e2e/health 전부 개발키로 쿠키
서명) 도입 시 게이트 전면 붕괴 + 신규 env 플래그로 데모 기동 의미 변경 → 별도 트랙(게이트가 SESSION_SECRET 을
쓰도록 선행 개편 필요). **교훈: 0-확정 감사에서도 잔여 하드닝(HSTS·경고 승격)은 가치 — 단 게이트 인프라가
특정 구성(개발키)에 의존하면 fail-closed 도입 전 인프라부터 정리.**

## SR/변경 전파 엔진 최대강도 재감사 2차 (v1.5.60)

5개 적대 렌즈(상태기계·전파·멱등성·인가·고아루프) → 반증 검증(13 에이전트). 1차(v1.5.50) 이후 확정 2·부분 2.

| # | 상태 | 위치 | 내용 | 심각도 |
|---|:---:|------|------|:---:|
| SR1 | ✅ | `sr/manage` submitApply | 적용요청 상신이 반려 재상신 할일을 소유자 무관하게 닫지 않아, 공유 워크스페이스에서 재상신자≠원 기안자면 재상신 할일 고아+'반려 방치' 알림 무한 반복(v1.5.50 변경엔 closeChangeResignTodo 로 고쳤으나 적용요청 누락) → 유형+SR번호 선두로 소유자 무관 마감. e2e apply_resign_orphan. | 중 |
| SR2 | ✅ | `sr/ci` receiveCiSr | 결재없는 CI 직접접수에 중복 가드 부재 → 더블클릭 이중 접수 → 같은 제목+요청자+접수 중복 무시. | 저 |

**부분(저·미조치):** addInfraChange 중복(인프라 변경은 자연키 없어 보류)·변경결과 반려 시 cw.result 잔류(재상신 시 덮임).
**교훈: 폐쇄루프 수정은 유형별로 흩어진 전 상신 경로에 적용 — 변경만 고치고 적용요청을 놓친 v1.5.50 형제 결함.**

## 컴플라이언스/ISMS 엔진 최대강도 재감사 2차 (v1.5.61)

5개 적대 렌즈(점검 상태기계·이수율·referential·CSV·인가/고아) → 반증 검증(17 에이전트). 확정 2·부분/반증 다수.

| # | 상태 | 위치 | 내용 | 심각도 |
|---|:---:|------|------|:---:|
| C1 | ✅ | `compliance/education` registerAttendees | 명단 이름을 과정 대상(eligibleForCourse)으로 재검증하지 않아, 임의 POST 로 비대상자를 이수 기록에 넣으면 그 기록이 과정을 '완료'로 확정하면서도 이수율 집계엔 안 잡혀 전사 이수율 왜곡(화면은 대상자만 노출, 서버 미검증) → 이름 대상 스코프 재검증 + '완료' 확정도 대상 이수자 기준(단일 원천). | 중 |
| C2 | ✅ | `lib/csv.ts` esc() | 인용 판정이 내부 CR(\r)을 빠뜨려 업로드 필드 중간 단독 CR 이 export 시 비인용→일부 파서 행 깨짐 → 인용 대상에 \r 추가. | 저 |

**반증:** 이수율 단일 원천(>100% 불가·0나눗셈 가드)·점검 상태전이·referential 삭제 가드·인가 건전. C1 은 서버측
가드(UI 미도달 크래프트 POST)라 코드 검토 확인, education_target 정상 경로 회귀 유지. **교훈: 화면이 대상자만
노출해도 서버액션은 직접 POST 가능 — 목록 렌더 필터와 동일 스코프를 쓰기 액션에도 재적용(export 재적용 원칙과 동일).**

## 재무/자금 엔진 최대강도 재감사 2차 (v1.5.62)

5개 적대 렌즈(집행률·정산전파·속보기준·종류격리·인가) → 반증 검증(18 에이전트). 확정 3(HIGH×2·MED)·부분 다수.

| # | 상태 | 위치 | 내용 | 심각도 |
|---|:---:|------|------|:---:|
| M1 | ✅ | `finance/invest`·`finance/expense` addContract | requireMenuRole 에 USER 포함 + /finance/* 메뉴 roles:ALL → USER 가 게이트 통과. 계약 카드는 canViewFinance(비-USER) 섹션이라 화면엔 안 보이나 서버액션 직접 POST 로 USER 가 전사 계약 신규 생성(write-where-read-denied, BAC) → 섹션 가드와 동일 DEPT_MGR+ 로 정정. | 높음 |
| M2 | ✅ | `finance/expense` editFlash | 동일 BAC — USER 가 전사 속보 기준금액 변조(감사기록까지 남아 정당해 보임) → DEPT_MGR+. | 높음 |
| M3 | ✅ | `finance/expense` addFlash | 동일 BAC — USER 가 전사 속보 신규 생성(관리자 전용 뷰 오염) → DEPT_MGR+. | 중 |

**반증:** 집행률(확정계획 스코프 ratio-of-sums, 내 독립 패스도 정합 확인)·종류 격리(kind 필터)·정산 전파(status
guard)·>100%(실 초과집행+계약 초과 플래그) 건전. 경영계획 액션은 본인 스코프라 USER 유지(정상). **핵심 교훈:
v1.5.42/43 이 재무 섹션(UI)·정산 상신은 잠갔으나 계약·속보 쓰기 4개 액션을 놓쳤다 — 섹션 가드(canViewFinance)를
바꿀 때 그 섹션의 전 쓰기 액션 가드도 동일 역할로 맞출 것. 내 독립 패스가 놓친 걸 워크플로가 적발(이번 런
검색·알림에 이어 3번째) — 집계 수학만 보고 액션 인가를 교차검증 안 한 사각.**

## 영속화/스토어 엔진 최대강도 재감사 2차 (v1.5.63)

5개 적대 렌즈(저장/플러시·로드/머지·백업 로테이션·nextNo/기록·정합) → 반증 검증(18 에이전트). 확정 다수가
동일 클래스로 수렴. 저장(원자 tmp→rename·비굶음 디바운스·SIGTERM 플러시)·격리(파싱+읽기 오류)·백업·nextNo
(NaN 안전·연도 키)·recordBatch 캡 건전 재확인.

| # | 상태 | 위치 | 내용 | 심각도 |
|---|:---:|------|------|:---:|
| ST1 | ✅ | `lib/store.ts` 요소 정규화 | 타입 안전 머지가 배열(→[])·숫자(→0)만 복구하고 문자열 필드는 방치 → 손상 파일이 문자열 필드를 숫자/객체로 남기면 소비처 .slice/.includes/.localeCompare 가 평범한 GET 에서 500(예: incidents.occurredAt 숫자→/infra/incidents 500). strFields 정규화 추가(존재하는 비문자열만 String 강제, 누락 undefined 는 옵셔널 보존). 알림 title.includes(v1.5.58)와 동일 클래스 체계적 마감. emptystate 요소 오염 프로브. | 높음 |

**미조치(문서):** 빈 객체 요소(누락 필수필드)는 옵셔널 보존과 상충해 머지 일괄 강제 불가 — 핫스팟은 호출부 String 방어.
**교훈: 요소 정규화는 array/number(→.map/.length 크래시)뿐 아니라 string(→.slice/.includes 크래시)도 대칭 커버.**

## 인프라 CRUD 엔진 최대강도 재감사 2차 (v1.5.64)

5개 적대 렌즈(참조삭제·참조생성·입력검증·인가·렌더견고성) → 반증 검증(13 에이전트). 확정 1·부분 2·반증 다수.
참조 무결성 삭제 가드(랙←HW/서버·서버←시스템·시스템←배치/IF·HW←서버) 전량·생성측 부모 검증·인가·IP
옥텟(v1.5.27) 건전 재확인 — v1.5.27 CLEAN 유지.

| # | 상태 | 위치 | 내용 | 심각도 |
|---|:---:|------|------|:---:|
| IN1 | ✅ | `infra/systems` incidentsOf | systemName.replace 무방비 호출 → 손상 파일이 system.name 누락(undefined)·비문자열이면 전 페이지 500. 머지 strFields(v1.5.63)가 비문자열은 강제하나 누락은 옵셔널 보존 위해 두므로 호출부 String(name ?? '')로 방어. emptystate systems.name 누락 프로브. | 중 |

**부분(저·미조치):** deleteSystem 이 incident.system(자유텍스트, 이미 create 측 허용) 미가드 → 소프트 dangling
(무크래시·표시만); 인터페이스 from/to 이름 기반 가드 우연 일치 과차단(rename 액션 없어 역방향 orphan 불가).
**교훈: 삭제 참조 가드는 완비됐으나 렌더 견고성(손상 파일 문자열 필드)은 별도 축 — 문자열 메서드 호출부 방어.**

## 게시판/설정 엔진 최대강도 재감사 2차 (v1.5.65)

5개 적대 렌즈(공통코드 전파·결재선 편집·board CRUD·엑셀양식·설정 견고성) → 반증 검증(15 에이전트). 확정 1·부분 다수.
공통코드 6그룹 전파(activeCodes·enabledGrades·activeItems 모두 isCodeActive)·결재선 편집 검증(비-USER·2차 구분)·
설정 ADMIN 게이트·board 인가 건전 재확인(v1.5.28/29 유지).

| # | 상태 | 위치 | 내용 | 심각도 |
|---|:---:|------|------|:---:|
| BS1 | ✅ | `settings/audit` | 설정변경 집계가 l.action.includes 무방비 호출 → 손상 파일이 감사 로그 action 을 누락(undefined)·비문자열로 남기면 전 감사화면 500(머지 strFields 는 비문자열만 강제, 누락은 옵셔널 보존). 호출부 String(l.action ?? '') 방어. emptystate 프로브에 action 누락 로그 추가. (선제 수정, 워크플로 반증이 확인) | 중 |

**부분(저·미조치):** resubmitSr 가 SR_KIND 활성 재검증 생략(은퇴 유형 초안이 결재 진입 — 반증이 grandfathering
으로 판정); addCodeValue 그룹당 상한 없음(ADMIN 1건/요청, 벌크 경로 없음); sr/requests KIND_TABS 하드코딩(읽기
필터라 신규 코드가 탭엔 없으나 전체 조회 정상). 엑셀양식 중복 docType(unshift+find 로 최신 우선)은 반증.
**교훈: 손상 파일 문자열 필드 방어는 머지 strFields(비문자열)+핫스팟 호출부 String(누락 undefined) 이중이 패턴 —
audit/incidents/systems/notify 전 화면 동일 처리. 이로써 렌더 견고성 축 체계적 마감.**

## 의식제고/서약 엔진 최대강도 재감사 2차 (v1.5.67)

5개 적대 렌즈(재서약 대상·서명 시각정밀·스코핑·출력물 폐쇄루프·위반/견고성) → 반증 검증(16 에이전트). 확정 3·부분 다수.
pledge/dept·export 스코핑(DEPT_MGR 부서한정+effectiveRoles)·서명 시각정밀(signedAt>=revisedAt)·프로젝트/특별 대상·
위반 self-only 건전 재확인.

| # | 상태 | 위치 | 내용 | 심각도 |
|---|:---:|------|------|:---:|
| AP1 | ✅ | `pledge/manage` reSignTargets '재택근무' | remoteTargets 를 이름 dedup 없이 매핑 → 인접 두 기간(경계월, v1.5.53) 보유자에게 재서약 안내메일 중복+sendLog to.length 부풀림(할일은 line63 별도 dedup) → Set dedup. (선제 수정, 반증이 확인) | 중 |
| AP2 | ✅ | `awareness/prints` (myCollation·submitDiscards·completeCollation) | '취합' 할일 조회가 title.includes 무방비 → 손상 파일의 title 누락 '출력물 폐기확인' 할일이 있으면 평문 GET·액션 500 → String(t.title ?? '') 방어(3곳). emptystate 프로브. | 중 |
| AP3 | ✅ | `awareness/remote` applyTarget | CSV/폼 날짜가 DATE 정규식만 통과(2026-13-45·2026-02-30 등 달력 무효)해도 dayBefore 의 new Date(NaN).toISOString() 가 RangeError 로 업로드 액션 전체 500 → realDate(UTC 파싱 왕복) 가드로 무효 행 거부(v1.5.53 dayBefore 가 도입한 크래시 벡터 마감). e2e remote CSV 무효행. | 중 |

**부분(저·미조치):** 특별 재서약이 validSign 일반 선행 미충족자까지 통지(순서 강제로 해소 가능 — 반증이 dead-todo
주장 반증); signProject 비멤버·signKind 재택 비대상·관리책임자 ACCOUNTS 기준·pledgeForm 누락 시 전원 유효 등 —
반증이 방어가능/저영향 판정. **교훈: 이름 dedup·문자열 필드 방어·달력 유효성은 각 엔진에 반복 적용해야 하는
횡단 클래스 — 특히 dayBefore 처럼 날짜 산술을 도입할 땐 정규식만으론 부족(실 달력 검증 필수).**

## 프로젝트 엔진 최대강도 재감사 (v1.5.68)

5개 적대 렌즈(생애주기·참조/첨부·입력검증·인가·렌더견고성) → 반증 검증(14 에이전트). 확정 1·부분 다수.
진척 범위검증(0-100·NaN)·projectOf 옵셔널체이닝·날짜 렉시컬 비교(크래시 클래스 없음)·멤버 실인원 검증·인가 건전.

| # | 상태 | 위치 | 내용 | 심각도 |
|---|:---:|------|------|:---:|
| PR1 | ✅ | `projects/status` updateProgress | 현재 상태 미확인 → 화면은 완료 프로젝트에 폼 숨기나 서버액션 직접 POST 로 완료(100%)→진행중(진척 하향) 되돌리기(불변식 위반) → `pj.status==='완료'` 서버 가드. | 중 |
| PR2 | ✅ | `pledge/manage` reSignTargets 프로젝트 | pj.members 를 s.people 교집합 없이 대상화 → 실 HR 어댑터가 퇴사자 제거해도 members 잔존(재조정 없음), 프로젝트 서약 개정 시 떠난 인원에 재서약 할일·메일(유령) → 현재 재직자만 대상화. | 저 |

**부분(저):** toggleDeliverable 대칭 토글(설계). **교훈: members 처럼 외부 동기화로 변하는 참조는 사용 시점에 현
디렉터리와 교집합(생성 시점 검증만으론 stale).**

## 연동/어댑터 엔진 최대강도 재감사 2차 (v1.5.69)

5개 적대 렌즈(채널게이팅·폴트내성·자가진단 이빨·프로필 단일원천·계약/타입) → 반증 검증(16 에이전트). 확정 2·부분 다수.
withTimeout(타이머 클린업)·sendVia 폴트격리·프로필 단일원천·전 어댑터 호출부 try/catch+withTimeout 건전 재확인.

| # | 상태 | 위치 | 내용 | 심각도 |
|---|:---:|------|------|:---:|
| IG1 | ✅ | `platform/integrations` syncHr | adapter.fetchPeople() 결과를 형태 검증 없이 s.people 대입 → 실 고객사 HR 어댑터가 계약(Person[]) 어겨 비배열({data,total})·필드 누락을 resolve 로 반환하면(withTimeout·try/catch 는 throw·hang 만 잡음) 스토어 오염→전 화면 s.people.filter/.map·p.dept.includes 500(앱 전역 브릭) → isPerson 형태 검증을 수집 경로에 적용, 위반 시 기존 명단 보존. e2e adapter_malformed. | **높음** |
| IG2 | ✅ | `lib/integrations/conformance.ts` asset | 자가진단 asset 계약 검사가 searchAssets 행 형태 미검증(hr·secdata 는 검증) → 등록자산 없는 그린필드서 malformed 어댑터가 '적합' 통과(false-clean) → ExternalAsset{serial,model,category,holder} 행 형태 검증. | 중 |

**부분(저):** hrAdapter isEnabled 리졸버 미게이트(호출부 syncHr 재검사, 잠재). **핵심 교훈: 프레임워크의 어댑터
경계 방어는 throw·hang(v1.5.16/17)뿐 아니라 resolve 된 오형(계약 위반)도 수집 경로에서 검증해야 완결 — 자가진단
검증기(isPerson)와 동일 기준을 실 수집 write 에도 적용. 목은 항상 정상이라 게이트가 못 잡음(실 어댑터 전용 결함).**

## 결재/다단(2차)·영속화(3차) 최대강도 재감사 (v1.5.70)

두 워크플로 동시 완료 — 같은 클래스(손상 파일 요소 정규화 사각)의 렌더 500 을 각각 적발, 머지에서 함께 마감.

**결재 다단 (5렌즈, 확정 2·반증 4):** 셀프결재 방지(resolve 가 기안자 미산출)·다단 큐 전진(중간 승인 early-return
으로 propagation 미발화)·앵커 재상신 닫기·신원 게이트(v1.5.47/54) 건전. 확정 2(둘 다 손상 파일 React child):
결재의 옵셔널 문자열 decidedAt·rejectReason 이 객체로 오염되면 {a.x ?? '-'} 가 React child 로 렌더돼 /work/approvals
평문 GET 500. rejectReason 은 시드 어느 행에도 없어(반려 시드 없음) strFields(v1.5.63)로 못 잡던 사각.

**영속화 3차 (5렌즈, 확정 1·부분 다수):** 저장·격리·nextNo·백업 로테이션 건전 재확인(내 독립 패스와 일치).
확정 1[높음]: codeGroups[].values 안의 null·원시 원소를 상위 요소 필터가 못 걸러 isCodeActive(null) 이 .enabled
접근으로 전 업무화면(장애·SR·공통코드) 500(중첩 배열 원소 미검증). 부분(저): .corrupt 파일 무제한 누적(포렌식
아티팩트라 방어적 설계)·격리 실패 시 다음 saveNow 가 원본 덮어씀(Windows 파일락 엣지).

| # | 상태 | 위치 | 내용 | 심각도 |
|---|:---:|------|------|:---:|
| AP-E | ✅ | `store.ts` 머지 요소 정규화 | 결재 옵셔널 문자열(rejectReason·decidedAt) 객체 오염 → React child 500. strFields 를 전 시드 요소 합집합으로 확장 + 요소 필드의 순수 객체값 일괄 제거(시드에 없는 필드까지 보편 방어). | 중 |
| ST3-1 | ✅ | `store.ts` 머지 중첩 배열 | codeGroups[].values 의 null·원시 원소 미검증 → isCodeActive(null) 전 화면 500 → 시드 형판 원소 타입 기준 중첩 원소 필터(v1.5.49 상위 필터의 중첩 확장). | 높음 |

**교훈: 시드 첫 요소만 형판으로 쓰면 later-row-only 옵셔널 필드가 샌다(합집합). 요소 필드에 순수 객체는
불변식 위반이라 일괄 제거가 시드 무관 보편 방어. 손상 파일 방어 3층 완성: 상위요소(v1.5.49)·필드값 타입
(v1.5.63/66)·중첩원소·객체값 제거(v1.5.70).**

## 알림/스케줄러(3차)·통합 검색(2차) 최대강도 재감사 (v1.5.71)

**알림 3차 (5렌즈, 확정 0·부분 다수):** 코어(재진입 가드 finally·스케줄러·대상 정합·sendVia 폴트격리) 건전.
부분 2건을 기존 횡단 클래스와 정합화(저심각):
- type-0 일배치 이관이 fetchPrintouts 행을 형태 검증 없이 push → 실 어댑터 계약 위반(name 누락) 시 undefined
  수신자·집계 오염 → 행 형태 검증(syncHr v1.5.69 와 동일 수집 경로 계약 검증).
- 비일반 재서약 notify(1-b)가 재서약 할일 소유자를 재직 명단 교집합 없이 통지 → 퇴사자 유령 통지 → 재직자
  교집합(reSignTargets v1.5.67·미서약·재택 미제출 타 유형과 동일). 미조치(무해): 배치 '명' 은 유형별 합(라벨 의미).

**검색 2차 (5렌즈, 확정 0):** v1.5.56(effectiveRoles 게이트)·행 스코핑(SR 부서·재무 USER 제외+canAccess) 보존,
쿼리 반향 React 이스케이프·숫자/키 렌더 안전(v1.5.70) 전량 건전 재확인 — CLEAN. 부분(저·미조치): 손상 파일
중복 id 시 React key 경고(무크래시)·무제한 렌더(데모 데이터셋 한정 perf). **교훈: 어댑터 수집 계약 검증·재직자
교집합은 전 수집/집계 경로에 반복 적용할 횡단 클래스 — 한 경로(syncHr·reSignTargets) 고쳐도 형제 경로(fetchPrintouts·
notify 1-b)에 남는다. 검색은 v1.5.56 이후 견고(2차 확정 0).**

## 컴플라이언스/ISMS(3차)·재무/자금(3차) 최대강도 재감사 (v1.5.72)

두 워크플로 동시 완료 — 둘 다 같은 클래스(v1.5.70 손상 파일 정규화의 **'시드에 없는/누락된 필드'** 사각)의
렌더 500 을 각각 적발, 스토어 머지 + 렌더 가드로 마감. 코어 로직(정산 생명주기·기준금액 우선순위·집행률
스코프·kind 격리·교육 이수율·점검 월 비교)은 전량 반증 — 3차 재감사가 남긴 건 손상 파일 엣지 2건뿐.

**재무 3차 (5렌즈, 확정 1·부분 다수·반증 다수):** 정산 이중지급(status==='결재중' 가드로 1회 전이)·재상신
중복결재(대기 결재 가드)·기준금액 basisOf 우선순위(정산>계약>계획, month×vendor 집계)·집행률 확정계획 스코프·
kind 격리(CT 채번 전역 유일)·paidOf 전역참조(현 데이터 무누수) 전부 반증. 확정 1[중]: **ExpenseFlash.history**
(옵셔널 배열, 시드 3행 모두 부재)가 비배열 원시값('x')으로 오염되면 머지 arrFields(시드 형판 기반)가 못 잡아
`f.history.length>0`(문자열 length) 통과 후 `.map` TypeError → /finance/expense 전 화면 500. 원소 오염
(null·비숫자 expected)도 fmt(undefined).toLocaleString 로 동일 크래시.

**컴플라이언스 3차 (5렌즈, 확정 1·부분 1·반증 3):** 교육 이수율 라이브 분모·이수 완료 후 신규 대상·점검
월 비교(어휘 비교, Date 무연산) 전부 반증/부분. 확정 1[중]: **person.dept 필드 누락**(값 오염이 아니라 키
부재) — 머지 strFields 는 옵셔널 보존 위해 undefined 를 안 건드려 살아남고, eligibleForCourse('개발자')의
`p.dept.includes('개발')` 가 undefined 에서 500(/compliance/education 평문 GET, 시드 ED-2026-03 개발자 과정).
부분 1[저]: addPlan 월 정규식이 2026-13 등 달력 무효월 통과(어휘 비교라 무크래시, 데이터 정합만).

| # | 상태 | 위치 | 내용 | 심각도 |
|---|:---:|------|------|:---:|
| FE3-1 | ✅ | `finance/expense/page.tsx` history 렌더 | 시드 무존재 옵셔널 배열 history 비배열/원소 오염 → .length/.map/toLocaleString 500. 스토어가 시드 형판으로 못 잡으므로 렌더에서 `Array.isArray` + 원소(`h?.at`·비숫자 expected→0) 방어. | 중 |
| CE3-1 | ✅ | `store.ts` 머지 reqStrFields | 전 시드 행 공통 필수 문자열 필드가 손상 파일에서 통째 누락(키 부재) → undefined 에서 .includes/.slice 500. 옵셔널(일부 행) 보존과 별개로, 필수(전 행) 필드는 누락도 ''로 채움(정상 데이터 무영향). | 중 |

**교훈: v1.5.70 정규화는 '값 오염'(wrong-type)을 잡지만 두 사각이 남았다 — (a) 시드 어느 행에도 없는 옵셔널
필드(history)는 형판 부재라 스토어가 원천적으로 못 잡아 소비 렌더가 방어해야 하고, (b) 전 행 공통 '필수'
필드의 키 부재(dept)는 undefined-보존 규칙에 걸려 살아남으니 필수/옵셔널을 구분해 필수는 누락도 기본값으로
채워야 한다. 손상 파일 방어 4층: 상위요소(v1.5.49)·필드값 타입(v1.5.63/66)·중첩원소·객체값(v1.5.70)·필수필드
누락채움+시드무존재 필드 렌더가드(v1.5.72). 코어 재무·컴플라이언스는 3차에서 확정 0(손상 파일 엣지만).**

## 시드무존재 필드(FE3-1) 전수 스윕 + 동시성/TOCTOU 감사 (v1.5.73)

**FE3-1 클래스 폐쇄(코드변경 없음):** 시드 어느 행에도 없는 옵셔널 배열/객체 필드는 정확히 2개
(ExpenseFlash.history·Approval.queue)뿐임을 전수 확인 — history 는 v1.5.72 렌더가드, queue 는 store.ts:405
F2 가드로 둘 다 방어. 확정 0, 클래스 구조적 폐쇄. queue 소비처(approvals/page.tsx:146 truthiness 가드)는
store 상류 정합으로 무해 → 과수정 지양 원칙 적용(미변경).

**동시성/TOCTOU 감사(5렌즈, 확정 0·부분 다수):** 공유 globalThis 인메모리 스토어의 await 갭 경합·재진입·
ID 충돌·영속화 인터리브를 전수 감사. 핵심 모델: JS 단일스레드라 동기 read-modify-write 본문은 원자적 —
경합은 read-check 와 mutation 사이 await 가 있을 때만. 전부 반증/무해 부분: (a) asset-reg 취득·출력물 이관·
nextNo 삽입은 await 후 dedup+삽입이 동기라 원자적(둘째 요청은 dedup 에서 스킵); (b) saveNow 는 완전 동기
(stringify→write→rename)라 프로세스 내 무경합, 크로스-프로세스 torn-write 는 인메모리 싱글턴이 미지원
토폴로지; (c) 디바운스 저장 드롭은 다음 틱 recordBatch 재무장으로 복구. 확정 1[저·조치]:

| # | 상태 | 위치 | 내용 | 심각도 |
|---|:---:|------|------|:---:|
| CC-1 | ✅ | `notify.ts` 명단 스냅샷 | runDailyNotify 가 s.people 을 3구간(미서약·재서약·재택)에서 sendVia await 사이로 읽는데 syncHr 가 배치 도중 `s.people = new`(참조 교체)하면 앞뒤 구간이 옛/새 명단으로 갈려 배치 내부 불일치 → 배치 시작 시 `[...s.people]` 얕은복사로 1회 포착, 전 구간 반복읽기 일관성(참조교체·in-place 양쪽 불변). | 저 |

**교훈: 인메모리 스토어 동시성은 단일스레드 원자성이 대부분 방어 — 실 경합면은 (1)비멱등 외부 어댑터를
await 하는 사이드이펙트(자산번호 취득, 현 목 어댑터는 멱등이라 무해)·(2)참조 교체(s.people=new)로 갱신되는
배열을 배치가 여러 구간 걸쳐 읽을 때의 read-skew뿐. 후자는 배치 시작 스냅샷으로 마감. __ngvNotifyTicking(notify-
vs-notify) + people 스냅샷(notify-vs-syncHr) 로 배치 일관성 이중화. 동시성 감사 확정 0(스냅샷 1건 선제 조치).**

## 연동/어댑터 계약 완전성 감사 (v1.5.74)

제품 핵심 차별점(재사용 프레임워크·클린 어댑터 계층)을 대상으로 폴트 내성(throw/hang/malformed, v1.5.16/17/69)이
아닌 **계약 완전성**을 5렌즈(연동지점 전수·수집 검증 패리티·컨포먼스 커버리지·폴트 랩 전수·프로필 격리)로
감사. 확정 다수 — **수집 검증 패리티 사각**이 핵심 테마: 자동 경로(syncHr v1.5.69·notify type-0 v1.5.71)는
어댑터 resolve 오형을 형태검증하나 형제 경로(자산·수동 출력물·메일·컨포먼스)가 누락돼 있었다.

| # | 상태 | 위치 | 내용 | 심각도 |
|---|:---:|------|------|:---:|
| AD-1 | ✅ | `asset-reg/page.tsx` searchAssets | `.catch(()=>[])` 는 reject 만 흡수 — 실 어댑터가 비배열({data,total} 봉투) resolve 시 assets.filter/.map 전 화면 500 → Array.isArray+행 형태검증(conformance 와 동일 엄격도). e2e adapter_asset_malformed(스태시검증). | 높음 |
| AD-2 | ✅ | `asset-reg/page.tsx` acquireAssetNo | 빈/비문자열 assetNo 무검증 저장 → 빈 취득번호 이력 + serial dedup 이 재시도 영구차단(복구불가) → 비어있지 않은 문자열일 때만 기록. | 중 |
| AD-3 | ✅ | `prints/page.tsx` importDaily | 수동 '전일자 이관'이 fetchPrintouts 행을 무검증 push(자동 notify.ts:40 은 검증) → name·document·printedAt 형태검증 패리티. | 중 |
| AD-4 | ✅ | `registry.ts` sendVia | 실 메일 어댑터가 undefined resolve 시 result.ok 접근이 try 밖에서 500 → resolve 값 SendResult 형태검증·정규화. | 중 |
| AD-5 | ✅ | `conformance.ts` secdata | 자가진단이 dept·personalInfo(필수, 부서 스코핑·개인정보 표시에 소비) 미검증 → 이를 뺀 고객사 어댑터가 녹색 통과 후 실배포 오작동 → 전 필수 필드 검증. | 중 |
| AD-6 | ✅ | `registry.ts` hrAdapter | 유일하게 isEnabled 게이트 없이 어댑터 반환(asset·secdata 리졸버는 게이트) → 채널 토글 계약 위반 잠재 트랩 → isEnabled('hr-sync') 자체 게이트로 대칭화. | 저 |
| AD-7 | ✅ | `store.ts` 프로필 스탬프 | PORTAL_DATA_FILE 에 프로필 식별자 미기록 → 같은 파일로 PORTAL_PROFILE 전환 시 프로필A channelStates 잔존. **v1.5.188~189 종결**(아래). | 완료 |

**교훈: 수집 검증(어댑터 resolve 오형 형태검증)은 전 어댑터 write 경로에 적용할 횡단 계약 — 자동 경로만
굳히면 자산·수동·메일·컨포먼스 형제가 샌다. 목 어댑터는 항상 정상이라 게이트가 실 어댑터 결함을 못 잡으므로
PORTAL_FAULT_{HR,ASSET,SECDATA,MAIL}=malformed 주입 훅으로 각 경로를 결정적 검증. 어댑터 계약 4폴트(throw·
hang·malformed-resolve·조회봉투) × 전 write 경로 = 재사용 프레임워크 무결성 축 완성. e2e 45→46.**

## 엑셀/CSV 익스포트 보안·정합 감사 (v1.5.75)

익스포트 계층(app/api/export·lib/csv)을 5렌즈(CSV 수식인젝션·구조 이스케이프·인가·스코프 누수·손상필드)로
감사 — **확정 0·부분 1(저)**. 핵심 보안(수식인젝션·구조)은 이미 견고: lib/csv.ts esc() 가 사용자 문자열 셀의
수식 트리거(=+-@ 탭 CR)를 작은따옴표로 무력화(CWE-1236)하고 RFC-4180(따옴표·콤마·CR·LF 인용)로 이스케이프,
단일 choke point(csvResponse)라 전 익스포트가 상속. 인가(USER 403·kind·dept 스코프)·스코프 누수 전량 반증.

| # | 상태 | 위치 | 내용 | 심각도 |
|---|:---:|------|------|:---:|
| EX-1 | ✅ | `store.ts` 빈시드 컬렉션 null 원소 | printouts 는 유일한 '빈 시드 + 익스포트' 컬렉션이라 형판이 없어 요소 정규화가 스킵된다(sample===undefined→raw 수용). 손상 파일 {"printouts":[null]} → /awareness/prints·엑셀 익스포트가 p.name/p.id 접근에서 전 역할 500. null/undefined 는 어느 컬렉션(객체·문자열 배열)에도 무효라 빈시드 분기에서 제거(형태 판별 불가해도 null 제거는 안전). emptystate 게이트에 null printout 픽스처(스태시검증). | 저 |

**교훈: 익스포트 CSV 인젝션·이스케이프는 이미 성숙(단일 choke point). 잔여는 손상파일 방어의 마지막 사각 —
'빈 시드 컬렉션'은 형판이 없어 요소 형태검증을 못 하나, null 원소만은 전 컬렉션 공통 무효라 제거 가능(형태는
못 잡아도 null 은 잡는다). 손상 파일 방어: 상위요소(v1.5.49)·필드값타입(v1.5.63/66)·중첩원소+객체(v1.5.70)·
필수필드+시드무존재렌더가드(v1.5.72)·빈시드 null원소(v1.5.75). 6축 수렴(+익스포트 보안 확정 0).**

## SR/변경 전파 엔진 최대강도 재감사 (3차, v1.5.76)

SR/변경 전파를 5렌즈(생명주기·변경링크·지연알림·인가·렌더견고성)로 3차 재감사 — **확정 2(저)·부분 다수·반증
다수**. 코어(전파 멱등성·이중생성 방지·인가) 전량 견고 재확인: 모든 폐쇄루프가 대상 현재상태를 가드(sr.status
==='결재중' 등)해 멱등, addDevChange 중복가드, assignCi 의 ci∈BIZ_MGR/ADMIN 검증(v1.5.60), decide ap.status
==='대기' 가드. 반증: SR지연 ghost(ci 는 ACCOUNTS 명이라 로스터 무관·dueDate 있으면 ci 항상 존재→requester
분기 도달불가), 적용요청결재중 제외(dueDate 축으로 반증), 인가 전파(설계상 의도).

확정 2 — v1.5.70 객체드롭의 **배열 사각**(같은 클래스 일반화):

| # | 상태 | 위치 | 내용 | 심각도 |
|---|:---:|------|------|:---:|
| SR3-1 | ✅ | `store.ts` 객체드롭→배열 확장 | v1.5.70 객체드롭은 순수객체만 제거하고 '객체 담은 배열'은 통과 → 시드 어느 행에도 없어 arrFields 로 못 잡는 스칼라 필드(ChangeWork.srNo·SrRequest.completedAt)가 손상 파일에서 `[{}]` 이면 {c.srNo} 직접 렌더가 React child 500(/infra/changes·/sr/manage). arrFields(정상 배열, 원소 정규화 완료)만 예외로 두고 그 외 '객체 담은 배열' 제거. 유일한 시드무존재 배열-객체 필드 history 는 시드에 []추가로 arrFields 편입해 보존(queue 는 문자열 배열이라 무해). emptystate srNo:[{}] 픽스처(스태시검증). | 저 |

**교훈: 손상파일 방어의 마지막 사각 — 객체 제거는 '순수 객체'뿐 아니라 '객체를 담은 배열'까지 봐야 완결
(둘 다 {field} 직접 렌더 시 React child 500). 시드무존재 배열-객체 필드는 history 하나뿐이라 시드 편입으로
예외 처리, 나머지 스칼라 필드의 배열-객체 오염은 제거. 손상파일 방어 6층(+빈시드 null v1.5.75·배열-객체드롭
v1.5.76). SR/변경 코어 3차 확정 0(렌더 견고성 사각만).**

## 재무/자금 엔진 최대강도 재감사 (4차, v1.5.77)

재무를 5렌즈(계획 생명주기·금액연산·참조무결성·인가·렌더)로 4차 재감사 — **코어 확정 0·부분 다수·렌더 확정 1**.
코어 전량 반증/부분: 계획 생명주기(작성중→취합→효율화→확정) 스킵불가(confirmPlan 은 효율화 필수)·submitPlanDraft
소유자 스코프·optimize/confirm BIZ_MGR — invest/expense 완전 대칭. 금액연산: 전 입력 Number.isFinite+≤0+1e9,
집행률 planTotal===0 가드. 참조무결성: planId·contract kind 검증, confirmedIds 스코프. 부분(저·미조치): 집행률
>100% 주석(과집행 의도 기능이라 표시는 정직·주석만 낡음), invest 정산항목 하드코딩 vs expense activeCodes(투자=
마일스톤 착수/중도/잔금, 월정산은 비용 전용이라 의도적 비대칭일 개연 — 미변경).

확정 1 — 빈 시드 컬렉션의 객체값 필드(v1.5.75 null필터의 사각):

| # | 상태 | 위치 | 내용 | 심각도 |
|---|:---:|------|------|:---:|
| FN4-1 | ✅ | `store.ts` 빈시드 객체필드 제거 | assetAcquisitions 는 빈 시드([]) 라 형판 기반 필드 정규화 루프(v1.5.70/76 객체드롭 포함)를 스킵한다(expected.length===0 continue). v1.5.75 는 null 원소만 필터 → 객체값 필드(at={$date} 등, Mongo/ISO export)가 살아남아 {a.at} 직접 렌더 React child 500(/finance/asset-reg). 빈시드 분기에 순수객체·객체담은배열 필드 제거 추가(형판 없이도 적용 — 이 값들은 어느 타입이든 렌더 크래시). emptystate at={} 픽스처(스태시검증). | 중 |

**교훈: v1.5.75 빈시드 방어는 null '원소'만 잡고 객체값 '필드'는 놓쳤다 — 빈시드 컬렉션은 형판 기반 정규화
전체(타입 강제+객체드롭)를 스킵하므로, null 필터에 더해 객체값·객체배열 필드 제거까지 빈시드 분기에 넣어야
완결(assetAcquisitions·printouts 등 전 빈시드 컬렉션 공통). 손상파일 방어 7층(+빈시드 객체필드 v1.5.77). 재무
코어 4차 확정 0 — 남은 건 빈시드 렌더 사각 하나뿐이었고 마감.**

## 알림/스케줄러 엔진 최대강도 재감사 (4차, v1.5.78)

알림/스케줄러를 5렌즈(발화기전·배치필터·로스터/dedup·기록성장·손상/동시성)로 4차 재감사 — **확정 2(높음 1·저 1)·
부분 다수·반증 다수**. batchRuns 200 캡·재진입 가드(__ngvNotifyTicking) finally·per-day 무가드는 인터벌 시맨틱상
의도·점검경과 inspector(ACCOUNTS 안정) 반증. 확정 2:

| # | 상태 | 위치 | 내용 | 심각도 |
|---|:---:|------|------|:---:|
| NS4-1 | ✅ | `instrumentation.ts` 기동 따라잡기 | setInterval 이 부팅에 앵커돼 인터벌보다 잦은 재시작(야간 배포·오토스케일·유휴 축출)이 매번 카운트다운 리셋 → 24h 주기면 틱이 영영 안 와 미서약·SR지연·재택·확인서 컴플라이언스 메일 조용히 영구 중단. 오늘자 일일 배치 기록 없으면 기동 즉시 1회 발화(재진입 가드가 중복 방지, recordBatch 가 오늘 실행 표시 → 하루 1회 수렴). e2e scheduler_bootcatchup(1h 인터벌, 스태시검증). | 높음 |
| NS4-2 | ✅ | `notify.ts` 반려방치·확인서 재직교집합 | 반려 방치(재상신 todo.owner)·확인서 미제출(violation.name)이 재직 명단 교집합 없이 발송 → 퇴사자 유령 독촉·집계 부풀림. 비일반 재서약(v1.5.71)의 구조적 쌍둥이 사각(형제 경로) → people 스냅샷 교집합 적용(떠난 인원 제외, 위반/할일 기록 자체는 화면에 유지). | 저 |

**교훈: (1) 인터벌 스케줄러는 부팅 앵커라 '인터벌보다 잦은 재시작'에 취약 — 기동 시 '오늘 안 돌았으면 따라잡기'
1회 발화로 무발송 마감(cron 없이도 하루 1회 수렴). (2) 재직 명단 교집합은 전 person 발송 경로 횡단 클래스 —
v1.5.71 이 재서약 고쳐도 형제(반려방치·확인서)에 남았다(v1.5.73 교훈 재확인). 알림 코어(재진입·필터·기록 캡)는
4차에도 견고, 신규 확정은 발화기전(운영)·형제 경로(로스터)뿐.**

## 컴플라이언스/ISMS 엔진 최대강도 재감사 (4차, v1.5.79)

컴플라이언스를 5렌즈(이수율연산·점검생명주기·대상적격·인가·렌더)로 4차 재감사 — **확정 1(중)·부분 다수**.
코어(이수율 분자⊆분모라 ≤100%·div가드·완료 flip eligibleNames.size>0·점검 생명주기 상태가드·기준 사용중 삭제
차단·결재 멱등) 전량 견고 재확인. 확정 1 + 조치한 부분 2:

| # | 상태 | 위치 | 내용 | 심각도 |
|---|:---:|------|------|:---:|
| CI4-1 | ✅ | `inspection` registerResult | 점검결과 재상신을 원 상신자와 다른 관리자(BIZ_MGR·ADMIN 공유 화면)가 하면 draftApproval 의 기안자-매칭 닫기(owner 게이트)가 놓쳐 원 상신자 '재상신' 할일 고아 → '반려 방치' 무한 독촉. SR submitApply·변경 closeChangeResignTodo(v1.5.50) 와 동일 결함 → 유형+계획번호 선두 고정 소유자무관 닫기. e2e inspection_resign_orphan(스태시검증). | 중 |
| CI4-2 | ✅ | `education`·export 이수율 | 전사 이수율이 Math.round 라 99.5% 를 100 으로 올려 미이수자 남은 컴플라이언스를 거짓 완주로 표시(governance 오신호) → 전원 이수(분자≥분모)일 때만 100, 아니면 99 캡(화면·엑셀 동일). | 저 |
| CI4-3 | ✅ | `inspection`·`education` 월 검증 | /^\d{4}-\d{2}$/ 가 2026-13·2026-00 통과 → 어휘 비교(경과·notify)에서 영구경과(2026-00)·경과누락(2026-13) 오분류(임의 POST) → 달력유효월 /^\d{4}-(0[1-9]\|1[0-2])$/ 로 강화(addPlan·addCourse). | 저 |

**미조치(문서화): namesake 이름-키(전 앱 공통, add-person 경로 없음)·registerResult SoD(등록≠승인은 강제됨,
동일신뢰 role)·반려 결과텍스트 export 잔존(상태열이 결과미등록 표기). 교훈: 교차-상신자 고아 할일은 전 공유
워크스페이스(SR·변경·이제 점검) 횡단 클래스 — draftApproval owner 게이트를 페이지가 소유자무관 닫기로 보완해야
완결(v1.5.50 이 SR/변경 고쳐도 점검 registerResult 에 남음). 컴플라이언스 코어 4차 확정 0(생명주기·이수율 견고).**

## 영속화/스토어 엔진 최대강도 재감사 (4차, v1.5.80)

영속화를 5렌즈(저장내구성·격리복구·백업로테이션·구버전머지·객체필드)로 4차 재감사 — **확정 3(높음1·중2)+선제1**.
이전 3패스는 손상 파일 '렌더' 방어(7층) 위주였고, 저장 내구성·복구 내부는 이번에 처음 심층 감사. 코어(saveNow
try/catch·tmp→rename 원자성·exit 플러시·백업 로테이션 slice(len-keep)·seed-driven 머지 구버전호환) 견고 재확인.
확정 3 + 선제 1:

| # | 상태 | 위치 | 내용 | 심각도 |
|---|:---:|------|------|:---:|
| PS4-1 | ✅ | `store.ts` 격리 실패 no-save | 손상 파일 격리 renameSync 가 throw(Windows AV·백업 파일락 EPERM)하면 catch 가 삼키고 seed 폴백 → scheduleSave→saveNow 가 원본을 빈 시드로 덮어써 격리 사본조차 없는 유일 손상 원본까지 파괴(복구지점 상실). 격리 실패 시 __ngvPortalNoSave 로 저장 비활성화(읽기전용 인메모리)해 원본 보존. | 높음 |
| PS4-2 | ✅ | `store.ts` 손상 후 백업 보류 | 손상→격리→seed 기동 후 catch-up 틱의 backupDataFile 이 seed 를 그날의 양호한 .bak(같은 날짜 스탬프) 위에 덮어써 유일 복구지점 파괴. __ngvPortalQuarantined 면 백업 보류(실패 기록)해 기존 복구지점 보존. | 중 |
| PS4-3 | ✅ | `store.ts` channelStates 검증 | menuOverrides·queue 와 달리 channelStates 값 무검증 → 비불리언 값({})이 isEnabled 의 `st[id] ?? default`(?? 는 null 만 폴백)를 통과, {} truthy 로 중지 채널(security-db) 오활성·발송. menuOverrides 와 동일 불리언 값 필터. e2e channelstate_corrupt(4/5 유지, 스태시검증). | 중 |
| PS4-4 | ✅ | `store.ts` __proto__ 가드 | 머지 키 스킵 `k in base` 가 프로토타입 체인을 봐 '__proto__'·'constructor' 통과 → mergedRec['__proto__']=v 가 스토어 프로토타입 조작(CWE-1321) → hasOwnProperty 로 시드 자기 키만 수용(선제 하드닝). | 저 |

**미조치(문서): saveNow 무신호 실패(디스크 지속실패 무경보, 300ms마다라 스로틀 필요)·.corrupt 무한누적(포렌식
설계)·백업 copyFileSync 비원자(외부 리더 엣지)·SIGKILL 300ms 창(불가피). 교훈: 손상→seed 회귀는 '저장·백업이
멀쩡한 복구지점을 덮어쓰는' 2차 손실을 부른다 — 격리 실패는 저장 금지(NoSave), 격리 성공이라도 seed 상태
백업은 보류(Quarantined)해야 복구지점을 지킨다. 렌더 방어(7층)와 별개로 저장 내구성·복구는 별도 축. 영속화
코어 4차: 저장/원자성/머지 견고, 신규 확정은 손상-후-회귀 2차손실·객체필드 검증뿐.**

## SR/변경 전파 엔진 최대강도 재감사 (4차, v1.5.81)

SR/변경을 5렌즈(다단큐×전파·CI직접접수·교차상태·첨부ref·렌더/인가)로 4차 재감사 — **확정 1(중)·부분 다수**.
코어 전량 견고 재확인: 다단 큐 중간승인은 전파 전 return(최종에만 전파)·CI직접접수 상태가드·교차플로 단조+락
(적용요청 승인 후 회수불가→체인지-링크 SR 잠금)·createSr 이중제출 dedup. 부분(저·미조치): CI '진행중' 리스트가
완료 SR 미제외(표시), replan 상태가드 없음(권한자·표시전용), 결재 todo includes(id) 5자리 충돌(>9999 도달불가),
attachCount 라벨 오표기(표시). 확정 1:

| # | 상태 | 위치 | 내용 | 심각도 |
|---|:---:|------|------|:---:|
| SR4-1 | ✅ | `approvals.ts` 회전 닫기 소유자무관 | 회전 문서(장애보고·서약현황·부서서약) 재상신을 원 기안자와 다른 관리자(공유 워크스페이스)가 하면 draftApproval 회전 닫기가 owner 게이트에 막혀 원 기안자 '재상신' 할일 고아 → '반려 방치' 무한 알림. SR submitApply·변경 closeChangeResignTodo(v1.5.50)·점검 registerResult(v1.5.79) 의 회전판 사각. 회전 닫기는 회전 ref 라 참조일치 불가하고 개수 일치(재상신 1회=반려 1건) 불변식이라 owner 무관이 옳다 → 회전 브랜치 owner 게이트 제거(비회전 ref-exact 는 owner 유지, 페이지레벨 닫기 담당). 중앙 1곳 수정으로 3 회전 제출부 동시 마감. e2e rotating_resign_orphan(스태시검증). | 중 |

**교훈: 교차-상신자 고아 할일 클래스 완결 — 비회전(SR·변경·점검)은 페이지레벨 소유자무관 닫기, 회전(장애·서약)은
draftApproval 회전 브랜치 자체를 소유자무관화(개수 불변식이라 안전). owner 게이트는 비회전 ref-exact 정확도용이지
회전엔 부적합했다. 전 orphan e2e(비회전 3 + 회전 1 + rebundle/batchref) 통과로 무회귀 확인. SR/변경 코어 4차 확정 0.**

## 재무/자금 엔진 최대강도 재감사 (5차, v1.5.82)

재무를 5렌즈(정산×결재전파·자산취득·익스포트완전성·연산·렌더/인가)로 5차 재감사 — **확정 1(중)·부분 다수·반증**.
코어 전량 견고 재확인: resubmitSettlement own-scope(정산품의 비회전이라 v1.5.81 무영향, ref-exact owner 일치로
닫힘)·결재중 이중지급 가드·exec-rate div가드·plan 생명주기·kind 격리 — settlement/rate 렌즈 확정 0. 확정 1 + 조치 부분 2:

| # | 상태 | 위치 | 내용 | 심각도 |
|---|:---:|------|------|:---:|
| FN5-1 | ✅ | `asset-reg`·conformance assetNo 검증 | searchAssets 수집 필터(v1.5.74)가 serial/model/category/holder 만 검증하고 렌더 필드 assetNo 는 빠뜨렸다 → 계약 위반 어댑터가 assetNo={} 반환 시 {a.assetNo} 직접 렌더가 React child 500(전 화면). conformance 도 동일 누락이라 녹색 통과 후 실배포 크래시(false-clean). 필터·자가진단에 assetNo 옵셔널-문자열 검증 추가. e2e adapter_asset_badfield(스태시검증). | 중 |
| FN5-2 | ✅ | export route 비용 헤더·flash planId | (a) invest/expense-actual 공유 헤더가 비용을 과제(투자 노운)로 오표기 → kind별 과제/항목 노운. (b) 비용 속보 export 가 planId 를 kind 무스코프로 find → 손상 상태서 투자 과제명 유출 → `&& p.kind==='비용'`(export 자체 원칙 route:45 정합). | 저 |

**미조치(문서): invest 정산항목 하드코딩(월정산 비용전용이라 straight symmetry 불가·교집합 필요, 거버넌스 반쪽)·
재취득 dedup 이 재발번 이력 갱신 안 함(비권위 세션 로그, decommission/re-onboard 엣지). 교훈: v1.5.74 수집
형태검증이 '렌더되는' 옵셔널 필드(assetNo)를 빠뜨렸다 — 검증은 화면이 직접 렌더하는 전 필드를 덮어야 완결
(conformance 도 동조). 재무 코어 5차 확정 0(어댑터 렌더 필드 사각·export 라벨/스코프뿐).**

## 의식제고/서약 엔진 최대강도 재감사 (v1.5.83)

의식제고/서약을 5렌즈(서약생명주기·현황상신·재택출력물·위반확인서·인가렌더)로 재감사 — **확정 2(중·저)·보류 1(높음·경계)**.
코어 견고 재확인: reSignTargets 전 kind ∩ 재직자, 개정 revisedAt 초정밀, submitStatement 위반자 본인 own-scope,
addViolation 감사·검증, dept scope(remindUnsigned·submitDeptStatus), 상태전파 멱등. 조치:

| # | 상태 | 위치 | 내용 | 심각도 |
|---|:---:|------|------|:---:|
| AP3-1 | ✅ | `prints`·`notify` dept/pages 검증 | importDaily·notify type-0 이 fetchPrintouts 행의 name/document/printedAt 만 검증하고 렌더 필드 dept/pages 를 무검증 spread → 계약위반 어댑터가 객체값 반환 시 {p.dept}/{p.pages} 직접 렌더 React child 500. 인메모리 주입이라 loadFromFile 정규화(v1.5.77) 우회. 양 경로에 dept(문자열)·pages(숫자) 검증 추가. e2e adapter_secdata_badfield(스태시검증). | 중 |
| AP3-2 | ✅ | `pledge/manage`·`pledge/dept` 서약률 | 전사·부서 서약률이 Math.round 라 99.5%→100 으로 미서약자 남은 서약 컴플라이언스 거짓 완주 표시 → 전원 서약(미서약 0)일 때만 100, 아니면 99 캡(교육 이수율 v1.5.79 동일). | 저 |
| AP3-3 | ⏳ | `approvals.ts` 회전 닫기(신선 제출 과닫기) | 회전 닫기(count 기반)가 '재상신' 아닌 신선 제출에도 발화 — submitDeptStatus/submitCompanyPledges 는 재상신 전용 액션이 없어 새 부서/새 배치 제출이 무관한 오래된 반려 재상신 할일을 닫는다 → 그 항목 반려방치 알림 유실. 단, 항목은 화면 상시 노출(company=등록 상태·dept=상시 결재상신)이라 워크플로 미정지·데이터 무손실 — 손실은 이메일 넛지뿐. 근본 수정(pledge-status 안정 ref/비회전화)은 테스트된 결재 닫기 루프 재작업이라 별도 트랙 보류. | 높음(경계) |

**교훈: 어댑터 수집 검증은 '화면이 직접 렌더하는' 전 필드를 덮어야 한다 — dept/pages 도 렌더되므로 name/document/
printedAt 만으론 반쪽(FN5-1 assetNo 동일 클래스). 인메모리 주입(importDaily·notify)은 loadFromFile 정규화를 우회하므로
수집 지점 검증이 유일 방어. 서약률은 교육 이수율과 동일 governance 오신호. AP3-3 은 count 기반 회전 닫기가 fresh-vs-
resubmit 를 구분 못 하는 근본 한계 — 항목 상시노출로 harm 이 넛지 유실에 한정(정지·손실 없음)돼, 위험한 close-loop
재작업 대신 안정-ref 재설계를 별도 트랙 보류(정직 기록).**

## 알림/스케줄러 엔진 최대강도 재감사 (5차, v1.5.84) — AP3-3 해소 포함

알림/스케줄러를 5렌즈(재상신-todo/반려방치·발화기전·sendVia기록·필터·동시성)로 5차 재감사 — **확정 3(높음·중·중)+선제,
보류(v1.5.83 AP3-3) 해소**. 코어 견고: 재진입 가드·people 스냅샷(참조교체 방어)·recordBatch 실패기록·필터 예외 없음.

| # | 상태 | 위치 | 내용 | 심각도 |
|---|:---:|------|------|:---:|
| NS5-1 | ✅ | `approvals.ts`·`pledge/dept` 부서서약 비회전 (AP3-3 해소) | 부서서약은 무상태 스냅샷이라 매 상신이 fresh 이고 재상신 전용 액션이 없어, 개수기반 회전 닫기가 무관한 부서의 반려 재상신 할일을 과다마감(반려방치 알림 영구 소실·잔여상태 없어 불가시) + 교차소유자 재상신 시 대상 뒤바뀜. ROTATING 에서 제거 + ref=부서명(안정 키)로 exact 닫기 + 페이지레벨 소유자무관 닫기(SR·변경·점검 패턴). e2e deptpledge_resign(스태시검증). 나머지 3 회전종(장애·출력물·서약현황)은 배치 가변이라 회전 유지·항목 잔여상태로 최소 가시성(문서). | 높음 |
| NS5-2 | ✅ | `notify.ts` 출력물 미등록 재직교집합 | 출력물 미등록 발송이 printout.name 을 재직 명단 교집합 없이 발송 → 퇴사 출력자 유령 독촉. 타 person 경로(미서약·재택·반려방치·확인서) 전부 적용하는 people 스냅샷 교집합을 이 경로만 누락 → 추가(미등록 출력물 자체는 관리자 화면 잔존). | 중 |
| NS5-3 | ✅ | `instrumentation.ts` 부팅 따라잡기 격리 가드 | 손상 파일 격리→시드 폴백 부팅에서 boot-catchup 이 시드 데이터 기준 컴플라이언스 메일을 발화(backupDataFile 은 __ngvPortalQuarantined 로 보류하나 notify 경로는 누락) → 같은 격리 플래그로 boot-catchup 도 보류. | 중 |

**미조치(문서): 회전 3종(장애·출력물·서약현황) fresh 상신 과다마감(항목 잔여상태로 최소 가시성·배치 가변이라 per-item
마커 필요, 별도 트랙)·personalInfo 검증(불리언 강제 렌더라 무크래시, contract 완전성만)·sendLog detail 무한장(성공 경로
slice 없음, 200 count-cap 은 있음). 교훈: 개수기반 회전 닫기는 fresh-vs-resubmit 미구분 근본한계 — 안정 per-item ref
가 있으면(부서=부서명) exact 닫기로 근본 해소, 없으면(가변 배치) 항목 잔여상태 가시성으로 완화. 재직 교집합은 전 person
발송 경로 횡단클래스(출력물로 완결). 격리 가드는 backup·notify 양쪽에 필요. 알림 코어 5차 확정 0(신규 확정은 pledge-status
회전·형제 로스터경로·격리기전).**

## SR/변경 전파 엔진 최대강도 재감사 (5차, v1.5.85)

SR/변경을 5렌즈(닫기루프정합·CI/지연·다단큐·교차플로렌더·인가첨부)로 5차 재감사 — **확정 1(중)·부분 다수·반증 다수**.
코어 견고 재확인: 닫기루프 v1.5.81/84 무회귀(SR docs 는 ref-안정·비회전이라 ref-exact 닫기 정상, 공유 워크스페이스는
페이지레벨 소유자무관 닫기), 다단큐 중간승인 전파전 return, CI접수 상태가드, 교차플로 단조+락. 조치:

| # | 상태 | 위치 | 내용 | 심각도 |
|---|:---:|------|------|:---:|
| SR5-1 | ✅ | `work/todo` RESUBMIT_HREF 적용요청 | 적용요청 상신 재상신 할일의 '재상신하기' 링크가 RESUBMIT_HREF 누락으로 KIND_HREF 폴백(/sr/requests)로 빠짐 → 테스트 상태 SR 엔 재상신 버튼 없어 데드엔드, 반려방치 알림 무한. /sr/manage(submitApply 재상신처) 매핑 추가. e2e apply_resubmit_route(스태시검증). | 중 |
| SR5-2 | ✅ | `sr/delayed` replan 상태·미래일 가드 | replan 이 상태 가드·서버측 미래일 검증 없이 임의 SR 의 dueDate 를 덮어씀(v1.5.76 부터 재지적) → 임의 POST 로 미배정(CI배정)·종결 SR 을 지연목록·SR지연 알림에 오분류. dueDate 보유·비종결 SR + dueDate≥오늘 가드(assignCi 정합). | 저 |
| SR5-3 | ✅ | `approvals`·`sr/ci` 결재/SR처리 todo 앵커 | 결재·SR처리 todo 닫기가 title.includes(id) 비앵커 매칭 → >9999 채번 시 4자리 id 가 5자리 id 의 접두-부분문자열이라 오마감(재상신 닫기는 startsWith 앵커됨). 5곳을 startsWith(id+공백) 앵커로 통일. | 저 |

**미조치(문서): CI '진행중' 리스트 완료SR 미제외·corrupt-but-string 날짜 NaN 렌더(무크래시)·회전 3종 fresh 과닫기
(항목 잔여상태 가시성, 별도 트랙). 교훈: 폐쇄루프는 '할일 생성·상태전파'뿐 아니라 '재상신 화면 라우팅'까지 완결해야
데드엔드가 없다(RESUBMIT_HREF 전 재상신 docType 커버). 비앵커 substring 매칭은 채번 롤오버 시 충돌 — 앵커 통일.
SR/변경 코어 5차 확정 0(닫기루프 v1.5.81/84 무회귀 재확인, 신규는 UI 라우팅·비앵커 매칭·replan 가드).**

## 대시보드/집계 엔진 최대강도 재감사 (v1.5.86)

대시보드(개인별현황·전사 운영 스냅샷)를 5렌즈(신호정합·스코프/인가·중복/누락·렌더/손상·성능/의미)로 재감사 — **확정 2(중1·저1)·반증 7**. 대시보드는 최대 팬인 집계면이라 각 타일이 출처 화면(도메인 스크린·notify)과 예측을 공유해야 한다. 코어 정합 재확인: 미이수(eligibleForCourse 대상스코프)·미서약(일반서약 단일원천)·지연SR(상태집합 v1.5.85 정합)·점검 미등록·경과(점검화면 결과미등록+기한경과 합집합과 정확히 일치 — 반증 3회) 무회귀. 조치:

| # | 상태 | 위치 | 내용 | 심각도 |
|---|:---:|------|------|:---:|
| DA-1 | ✅ | `dashboard` 전사 스냅샷 런타임 권한 정합 | '전사 운영 스냅샷' 카드가 정적 `canManage`(role 리터럴)로만 게이트 → 출처 화면(장애/지연SR/프로젝트/서약/점검)은 모두 `requireMenu`=effectiveRoles(menus∩menuOverrides)로 가드하는데, ADMIN 이 메뉴권한 화면에서 한 도메인을 제한해 담당자를 그 화면에서 리다이렉트시켜도 대시보드 집계 타일로 같은 수치가 새어 나감(런타임 접근통제 무력화). 5개 타일 각각을 출처 href 의 effectiveRoles 로 게이트, 카드는 가시 타일이 하나라도 있을 때만 렌더. e2e dashboard_ops_scope(스태시검증). | 중 |
| DA-2 | ✅ | `dashboard` '나의 SR' 상태 라벨 정합 | '나의 SR 진행현황' 카드가 로컬 중복 SR_CHIP 맵으로 원본 `r.status` 를 그대로 렌더 → 데이터·계정/권한 SR 의 '개발중'을 다른 SR 화면·검색(모두 공유 srStatusLabel 로 '처리중' 표기)과 다르게 노출(한 요청이 화면마다 다른 상태). 공유 `sr/chips`(SR_CHIP·srStatusLabel) import 로 통일, 로컬 맵 제거(향후 상태 추가 시 미정의 tone 드리프트도 차단). | 저 |

**교훈: 집계면 타일은 값 계산만 정합해선 부족하고 노출 게이트도 출처 화면과 같은 유효권한을 따라야 한다
(정적 role 게이트는 런타임 메뉴권한 제한을 우회 — 검색 v1.5.56 과 동형 결함이 대시보드에 잔존했음). 라벨·색상은
공유 헬퍼 단일 원천을 쓰고 화면별 중복 맵을 두지 않는다. 점검 미등록·경과 타일의 결과미등록 무월가드는 결함이 아니라
점검화면 두 타일(결과미등록+기한경과)의 의도된 합집합(반증 3회 일치). 대시보드 코어 확정 2(권한정합·라벨정합).**

## 인가/권한 엔진 최대강도 감사 (v1.5.87) — 교차도메인 게이트 클래스 완결

인가/권한 엔진을 5렌즈(서버액션 인가완전성·정적게이트우회·menuOverride 불변식·스코프·게이트정합)로 감사 — **확정 2(중)·부분 1(저)·반증 다수**. 서버액션 인가는 견고 재확인: 36개 액션 전수에서 무가드 없음, requireMenuRole 가 화면 유효권한(menus ∩ menuOverrides)을 쓰기에도 적용, 공용 액션(resubmitSettlement)은 계약 종류별 effectiveRoles 를 수동 교집합, 소유자/기안자 스코프 서버측 검증. menuOverrides 는 축소 전용(effectiveRoles 교집합)·ADMIN 잠금방지로 상승·락아웃 불가. **핵심 발견: v1.5.86 이 대시보드 ops 타일에 적용한 effectiveRoles 게이트의 형제 결함이 다른 교차참조 화면에 잔존** — v1.5.56(검색)·v1.5.86(대시보드 스냅샷)이 이 클래스를 열었으나 스윕이 안 끝나 있었다. 조치:

| # | 상태 | 위치 | 내용 | 심각도 |
|---|:---:|------|------|:---:|
| AZ-1 | ✅ | `infra/systems` 장애 교차도메인 게이트 | 시스템·서버 현황 화면이 자기 메뉴로만 게이트되나 '조치중 장애' 집계 타일·시스템별 '장애 이력' 열은 장애관리(/infra/incidents) 도메인 신호 → ADMIN 이 장애관리를 담당자에서 제한해 그 화면에서 리다이렉트시켜도 시스템 화면 집계·연계로 같은 수치가 새어 나감(v1.5.86 동형). 타일·열을 effectiveRoles('/infra/incidents') 로 가드. e2e infra_systems_incident_scope 스태시검증. | 중 |
| AZ-2 | ✅ | `dashboard` 공지 교차도메인 게이트 | v1.5.86 이 ops 타일은 canSee 로 가드했으나 같은 페이지 공지사항 카드는 미가드 → /board/notices 를 특정 역할에서 제한해도 대시보드 미리보기로 공지(보안 분류 포함)가 새어 나감. 공지 카드를 effectiveRoles('/board/notices') 로 가드(기본 roles:ALL 이라 평상시 무변화, 명시 제한만 반영). e2e dashboard_notices_scope 스태시검증. | 중 |
| AZ-3 | ✅ | `projects/status` 재무 계약 교차도메인 게이트 | 프로젝트 화면이 연동 계약의 거래처명·전체 계약목록(드롭다운)을 재무 유효권한 재검사 없이 노출 → 재무 제한자가 거래처·계약 카탈로그 열람(계약↔프로젝트 연동 요구는 유지). 거래처 표시·계약 드롭다운을 effectiveRoles('/finance/invest'|'/finance/expense') 로 가드, 미열람자는 '-'·연동안함 저하. | 저 |

**교훈: 교차도메인 게이트 클래스는 대시보드·검색뿐 아니라 모든 '연계(cross-reference)' 화면에 존재한다 — 한 화면이
다른 도메인의 신호를 재유도하면 출처 화면의 effectiveRoles 로 가드해야 런타임 메뉴권한 제한이 우회되지 않는다.
클래스 완결 스윕: 검색(v1.5.56)·대시보드 ops(v1.5.86)·대시보드 공지·시스템 장애·프로젝트 계약(v1.5.87). 서버액션
인가는 requireMenuRole 로 견고(무가드 0). AZ 코어 확정 2·부분 1.**

## 인프라 운영 엔진 최대강도 감사 (v1.5.88) — 회전 재상신 과다마감(AP3-3) 해소 포함

인프라 운영 엔진을 5렌즈(장애/변경 승인 상태기계·CRUD 참조무결성·렌더내성·연계정합·에스컬레이션)로 감사 — **확정 2(중)·부분 2(저)·반증 다수**. 승인 상태기계 견고 재확인: 장애보고 배치 반려→전건 미상신 복원+reportRef 초기화, 변경 2단계(계획/결과) 반려→각 단계 정확 복원, 두 단계 모두 소유자무관 재상신 닫기(closeChangeResignTodo), 전이마다 정확상태 find 가드. CRUD 삭제 참조가드 전수 존재(랙→HW/서버, HW→서버, 서버→시스템, 시스템→배치/인터페이스). 조치:

| # | 상태 | 위치 | 내용 | 심각도 |
|---|:---:|------|------|:---:|
| IO-1 | ✅ | `approvals` 회전 재상신 과다마감(AP3-3) 해소 | 회전 묶음 문서(장애보고·출력물폐기·서약현황)의 docType-only 닫기가 매 상신마다 발화 → 반려와 무관한 신규 묶음 상신이 대기 중 재상신 할일을 과다 마감(반려방치 알림 영구 소실). v1.5.84 가 부서서약만 안정키로 고쳤던 클래스의 잔여 3종. 재상신 할일에 반려 묶음 항목 id(batchItems) 를 남기고(반려 시점 캡처), 현재 상신 항목(opts.items)이 그 항목을 재포함할 때만 닫도록 정밀화(여러 묶음 동시 재상신도 정확, 레거시는 개수일치 폴백). e2e rotating_fresh_no_overclose 스태시검증(+rebundle_multi·rotating_resign_orphan 무회귀). | 중 |
| IO-2 | ✅ | `notify` 점검 경과 재직자 교집합 | '점검 경과' 에스컬레이션만 유일하게 재직 명단 교집합 없이 p.inspector 로 발송(형제 5경로는 모두 people 교집합) → 점검자 퇴사 시 유령 독촉·집계 부풀림. 타 person 경로와 동일하게 people.some 교집합 추가(점검 계획 자체는 화면에 남아 미등록·경과 가시성 유지, 정상 점검자는 모두 재직 명단에 존재 확인). | 중 |
| IO-3 | ⏳(문서) | `store` 시드 BJ-02 유령 시스템 참조 | 시드 배치 BJ-02 의 system '보안·출력물 시스템'이 s.systems 에 없어 랙 토폴로지 조인(이름=FK)에서 누락(무크래시 표시 언더카운트). 시드 데이터 정합 이슈로, 의도한 시스템 확정 후 별도 조정(운영 목록엔 정상 표기). | 저 |
| IO-4 | ⏳(문서) | `operations` 인터페이스 엔드포인트 FK | addInterface 의 from/to 가 시스템 FK 검증 없이 자유문자열 — 외부 연계 엔드포인트(비내부 시스템)가 정당할 수 있어 일괄 FK 강제는 보류, deleteSystem 가드는 보수적으로 유지. | 저 |

**교훈: 회전참조 묶음 문서의 재상신 닫기는 docType·개수만으로는 신규 상신과 재상신을 구분할 수 없다 — 반려 묶음의
항목 신원(batchItems)을 남겨 '항목 재포함' 기준으로 닫아야 과다마감이 사라진다(AP3-3 3종 완결: 부서서약 v1.5.84 안정키,
장애·출력물·서약현황 v1.5.88 항목매칭). person 지향 알림은 예외 없이 재직 명단 교집합(점검경과가 마지막 사각).**

## 프로젝트 엔진 최대강도 감사 (v1.5.89)

프로젝트 엔진을 5렌즈(진척/이슈 생명주기·참여서약 연계·집계정합·렌더내성·인가/스코프)로 감사 — **확정 2(중)·부분 다수·반증 다수**. 입력검증·FK·정확상태 가드 견고 재확인: addProject(headcount 유한·≥1·날짜순서·멤버 실존/중복/상한·계약 FK), updateProgress(0-100 클램프·완료 편집잠금), addIssue(프로젝트 FK·리스크 enum), resolveIssue(정확상태 오픈 가드), 평균진척 0분모 '-' 가드, 모든 액션 requireMenuRole. 완료 생명주기의 미정리 구멍 2건 발견. 조치:

| # | 상태 | 위치 | 내용 | 심각도 |
|---|:---:|------|------|:---:|
| PJ-1 | ✅ | `projects/status`·`pledge/my` 빈 명단 참여서약 정합 | 옵셔널 project.members 가 파일 영속화·재로딩에서 undefined→[] 로 정규화(시드 첫 행에 members 존재→arrFields 필수 취급)되면, 빈 배열은 truthy 라 signedCount 가 [].filter=0 을 반환(인메모리 names.size→재시작 후 0). signProject 멤버 가드도 []에 전원 서명 차단. 두 소비처를 length>0 판정으로 고쳐 빈 명단=미지정(전원 집계·전원 서명허용) 동일 취급. e2e project_emptymembers_signcount 스태시검증. | 중 |
| PJ-2 | ✅ | `projects/status` 완료 시 재서약 할일 정리 | 프로젝트 재서약 할일은 signProject(진행중 프로젝트 서명)로만 닫히는데, 멤버의 유일 참여 프로젝트가 서명 전 완료되면 서명할 진행중 프로젝트가 없어 할일이 영구 방치·notify 재서약 안내 매 배치 재발송(완료 생명주기 미정리). updateProgress 완료 확정 시, 서명 대상 진행중 프로젝트가 더 없는 멤버의 재서약 할일을 닫음(signProject remaining 로직 미러). e2e project_complete_resign_cleanup 스태시검증. | 중 |
| PJ-3 | ⏳(문서) | `projects/status` 진척 100 반올림·완료 불가역 | Math.round(99.5~99.99)→100 자동완료+잠금(reopen 액션 없음)·상한 없는 headcount — 데이터 입력오류 의존·의도된 완료잠금이라 보류(부분). | 저 |

**교훈: '완료'는 종결 상태 전이만으로 끝나지 않는다 — 완료 시 그 엔티티에 매달린 미결 의무(재서약 할일 등)를
정리해야 유령 독촉이 남지 않는다(폐쇄 루프는 생성뿐 아니라 종결에도 필요). 옵셔널 배열은 undefined 와 [](정규화
산물)를 소비처에서 동일 취급(length 판정)해 영속화 왕복에 불변이어야 한다. 프로젝트 코어 확정 2·부분 3.**

## 연동/어댑터 엔진 최대강도 감사 (v1.5.90) — syncHr 병렬명단 재조정

연동/어댑터 엔진을 5렌즈(동기화 생명주기·계약검증·고아정리·장애내성·멱등정합)로 감사 — **확정 3(중)·부분 다수·반증 다수**. 어댑터 코어 견고 재확인: syncHr(ADMIN 가드·withTimeout·try/catch·계약검증[Array+name/dept string]·위반 시 로스터 보존·원자적 단일할당·감사), 채널 게이팅, 장애주입(throw/hang/malformed/badfield) 내성. **핵심 발견: syncHr 이 s.people 을 통째 교체하나, s.people 과 별개인 병렬 명단(securityOfficers·remoteTargets)과 reSignTargets 의 일부 분기가 재직 명단과 재조정되지 않아, 퇴사자가 유령 신호로 잔존('프로젝트' 분기·notify 만 교집합 적용된 클래스의 미완결분).** 조치:

| # | 상태 | 위치 | 내용 | 심각도 |
|---|:---:|------|------|:---:|
| IN-1 | ✅ | `pledge/manage` reSignTargets 특별·재택 재직교집합 | reSignTargets('프로젝트')는 s.people 교집합하나 '특별'(securityOfficers)·'재택근무'(remoteTargets) 분기는 누락 → 서약양식 개정 시 퇴사자에게 유령 재서약 할일·안내메일 생성. 두 분기에 people.some 교집합 추가('프로젝트'와 정합). | 중 |
| IN-2 | ✅ | `awareness/remote`·`export` 퇴사 재택 대상자 유령 미제출 | 종료일 없는 재택 대상자가 퇴사(syncHr 로 s.people 제외)해도 remoteTargets 엔 남아 화면·통계·export 가 매월 '미제출' 유령으로 집계(제출·종료 경로 없어 해소불가). targetsRaw·export scopeRaw 에 people.some 교집합. e2e remote_departed_ghost 스태시검증. | 중 |
| IN-3 | ✅ | `pledge/manage` 보안담당자 카운트 재직교집합 | 보안담당자 Stat(securityOfficers.length)이 퇴사 officer 를 계속 카운트(해제 불가·s.people 렌더 테이블에도 미표기). 카운트를 people 교집합으로 산정. | 중 |
| IN-4 | ✅ | `prints`·`notify` secdata 이관 명시 구성 | importDaily/자동배치가 원시 어댑터 행을 `{id, ...row, status}` 로 스프레드 → row 가 포털 통제 id 를 덮어써 중복·오형 id 심을 수 있음(부분). 검증 필드만 명시 구성(id·status 포털 통제, personalInfo boolean 강제), v1.5.74 계약 완전성. | 저 |

**교훈: 로스터 통째 교체(syncHr)는 s.people 만 갱신할 뿐 s.people 과 이름으로만 연결된 병렬 명단(securityOfficers·
remoteTargets)·파생 대상(reSignTargets 분기)을 자동 정리하지 않는다 — 모든 person 파생 신호는 예외 없이 재직 명단과
교집합해야 유령이 없다(notify·'프로젝트'에 이어 '특별'·'재택'·officer 카운트·remote 화면/export 완결). 어댑터 payload 는
스프레드 말고 검증 필드만 명시 구성해 포털 통제 필드(id·status)를 보호한다. 연동 코어 확정 3·부분 2.**

## 첨부/문서생성 엔진 최대강도 감사 (v1.5.91)

첨부/문서생성 엔진을 5렌즈(dedup·고아정리·업로드검증·카운트정합·자동첨부/인가)로 감사 — **확정 1(저)·부분 다수·반증 다수**. 코어 견고 재확인: registerUpload(instanceof File·size 0/10MB·파일명 정제·sizeKb 하한), registerGenerated 날짜뺀 접두 dedup(v1.5.57)·공통 폴백, attachCount 순수 refId 필터, 삭제 시 첨부 정리(notices·qna 는 s.attachments 정리, 첨부 보유 엔티티 대부분 비삭제라 고아 미발생). 조치:

| # | 상태 | 위치 | 내용 | 심각도 |
|---|:---:|------|------|:---:|
| AT-1 | ✅ | `attachments` 필수 자동양식 파일명충돌 우회 | registerGenerated 중복판정이 refId+'양식이름_v버전_' 접두 startsWith 로만 비교하는데, registerUpload 와 첨부 레코드 형태가 동일(출처 구분자 없음)이라 사용자 업로드가 같은 접두 파일명을 가지면 자동양식으로 오인돼 필수 양식 생성이 막힘(변경결과 submitResult 는 업로드 후 자동생성 순서라 우회 가능 — 첨부파일자동생성 통제 무력화). Attachment.gen 플래그 추가(registerGenerated 만 설정), 중복판정을 gen 양식으로 한정. e2e autoform_upload_defeat 스태시검증. | 저 |

**교훈: 자동생성과 사용자업로드가 같은 저장소·동일 형태로 섞이면 '출처'를 파일명 같은 사용자 통제 문자열로 추론할
수 없다 — 명시적 출처 구분자(gen)로 판정해야 필수 자동첨부 통제를 파일명 조작으로 우회당하지 않는다. 첨부 코어는
견고(업로드검증·dedup·삭제정리 완비), 잔여 부분(업로드 무-dedup 누적·파일수 상한·타입검증)은 저심각 표시전용.**

## 게시판/QnA 엔진 최대강도 감사 (v1.5.92)

게시판/QnA 엔진을 5렌즈(답변/배정 생명주기·작성자스코프·인가·렌더내성·정합)로 감사 — **확정 0·부분 다수·반증 다수**. 코어 견고 재확인: QnA ask(도메인 검증·제목 상한), assign(BIZ_MGR/ADMIN 가드·미답변 가드), answer(답변1회 !answer 가드·본문 상한), deleteQna/deleteNotice(작성자 스코프 서버측·첨부 정리), addNotice(분류 검증). 확정 결함 없음 — 잔여 부분 저심각 정합/일관성 3건만 정리(감사 수렴):

| # | 상태 | 위치 | 내용 | 심각도 |
|---|:---:|------|------|:---:|
| BQ-1 | ✅ | `board/qna` 담당 지정 역할 정합 | assign 대상 검증·드롭다운이 role!=='USER'(DEPT_MGR 포함)인데 answer·canAnswer 는 BIZ_MGR/ADMIN → DEPT_MGR 지정 시 답변 폼 없어 막다른 길(게이트 불일치). 지정 대상을 답변 가능 역할(BIZ_MGR/ADMIN)로 한정. e2e qna_assign_role 스태시검증. | 저 |
| BQ-2 | ✅ | `dashboard` 공지 정렬 게시판 정합 | 대시보드 공지 미리보기 정렬이 board/notices 의 postedAt 보조키 누락 → 손상 파일 비불리언 pinned("true")로 Number()=NaN 비교자 불안정(게시판은 '\|\| localeCompare' 폴백으로 안전). 대시보드도 보조키 추가(정렬 정합+NaN 방어). | 저 |
| BQ-3 | ✅ | `dashboard` 공지 '교육' 분류 칩 | 대시보드 공지 칩이 '교육' 분류를 색상 없이(neutral) 표기 — 게시판('교육'→ok)과 불일치. 대시보드 칩에 '교육'→ok 추가. | 저 |

**교훈: 게시판/QnA 코어(작성자 스코프 서버가드·답변1회·삭제 첨부정리)는 견고 — 확정 0. 다만 한 워크플로의 두
게이트('지정 가능 역할'·'답변 가능 역할')는 같은 역할집합을 써야 막다른 지정이 없다. 대시보드는 원천 화면(board)과
정렬·칩 규칙을 공유해야 표시 정합. 엔진별 감사 사이클 사실상 포화(코어 견고, 잔여는 저심각 정합).**

## 교차 패턴 마이크로 스윕 — NaN 비교자·손상 날짜 (v1.5.93)

엔진별 감사가 포화된 뒤, BQ-2(비불리언 pinned NaN 정렬)가 드러낸 'NaN 비교자' 클래스를 전역 타깃 스윕. **정렬 사이트 전수(.sort 6곳): NaN 비교자 클래스는 이미 닫힘** — Number(booleanField) 정렬은 pinned 2곳뿐이고 둘 다 postedAt 보조키로 방어(대시보드 v1.5.92·게시판 기존), 숫자 정렬(diskUsedPct)은 numField→0 정규화, 문자열 정렬은 strField 정규화로 보호. **인접 클래스(날짜 산술 NaN)에서 잔여 1건 발견·수정:**

| # | 상태 | 위치 | 내용 | 심각도 |
|---|:---:|------|------|:---:|
| NP-1 | ✅ | `lib/dates`·`sr/delayed`·`sr/manage` 손상 날짜 일수계산 NaN | strField 정규화는 날짜를 문자열로만 보장(유효성 미검증)하므로, 손상 파일의 파싱 불가 dueDate('0000-00-00')가 지연목록에 들어가면 daysBetween 의 Date.parse=NaN → 'D+NaN'·'최대 지연일수 NaN'·완료소요 'NaN일' 렌더(손상파일 게이트는 크래시만 잡아 통과). 공용 daysBetween(lib/dates) 유한값 가드 신설(비유한→null), sr/delayed·sr/manage 3개 호출부를 '-'·0 폴백으로 통일. e2e delayed_corrupt_date 스태시검증. | 저 |

**교훈: 손상데이터 방어는 '크래시 없음'을 넘어 'NaN·[object Object] 등 잘못된 값 렌더 없음'까지다(게이트가 크래시만
잡으므로 NaN 렌더는 통과). 날짜 산술은 입력이 정규화로 문자열·숫자만 보장돼도 값 유효성(파싱 가능·유한)은 별도
가드해야 한다. rate 클래스(div-zero·false-100%)는 전수 확인 결과 이미 완비. NaN 비교자 클래스 완결.**

## 시드 참조무결성 — 미등록 시스템 정식화 (v1.5.94)

v1.5.88 IO-3(부분·문서 추적)로 남겨둔 시드 참조무결성 잔여를 정식 조치. **BJ-02(출력물 자료 이관 배치)·IF-04(출력물 자료 수신 인터페이스)·secdata 어댑터가 모두 원천 시스템 '보안·출력물 시스템'을 참조하나 s.systems 에 미등록**이라, 이름-기준 토폴로지 조인(랙→H/W→서버→시스템→배치)에서 BJ-02 가 매핑 실패로 누락돼 랙구성도 배치 수가 과소 집계됐다(무크래시). 실재하는 원천 시스템이므로:

| # | 상태 | 위치 | 내용 | 심각도 |
|---|:---:|------|------|:---:|
| SI-1 | ✅ | `store` 보안·출력물 시스템 정식 등록 | SYS-05 '보안·출력물 시스템'(SV-04 배치서버 매핑, 운영계, 담당 한지원) 시드 등록 → BJ-02·IF-04 가 참조하는 시스템 노드가 실재해 시스템 목록·랙구성도에 노출되고 배치 사슬 과소집계 해소. 삭제 가드(배치·인터페이스 참조 중)로 사용중 삭제 불가도 자동 성립. e2e secprint_system_registered 스태시검증. | 저 |

**교훈: 이름-문자열을 FK 로 쓰는 비정규화 구조(batch.system·interface.from/to = 시스템명)에서는 시드 자체가 참조무결성을
지켜야 한다 — 참조되는 원천은 반드시 등록 컬렉션에 실재해야 조인 누락(과소집계)이 없다. 어댑터 원천 시스템도 도메인
엔티티로 정식 등록(secdata 원천=보안·출력물 시스템). 감사에서 부분·문서 추적으로 남긴 잔여도 소진 시 정식 마감.**

## 요구복원 + 거버넌스 신호 정합 (v1.5.95–99)

엔진별 감사 포화 후, sample.xlsx(메뉴 82행·결재 14흐름·첨부 33점)와 제품안내서(6장)를 원문 대조해 **미구현 요구 2건을
복원**하고, rate/신호 클래스에서 **거버넌스 오신호 3건을 정합**했다. fankh reset/push 환경 제약상 전 항목을 무회귀
소단위 버스트(편집→build→commit→push 즉시 완료, 기본값 불변)로 분할해 origin 생존을 확보했다.

| # | 상태 | 위치 | 내용 | 심각도 |
|---|:---:|------|------|:---:|
| RQ-1 | ✅ | 재택 주기 config (v1.5.95) | 제품안내서 '재택 체크리스트 주기별(매일·분기·반기)' 요구 — RemoteCycle 타입·store·notify·remote·export·UI 6버스트로 복원. 기본 '월'=기존 동작 동일(무회귀). remotePeriodKey/Bounds/isRemoteTargetIn 헬퍼로 기간 집계 일원화. | — |
| RQ-2 | ✅ | SR 중지 status (v1.5.96) | 결재 시트 4·6·7 '진행상태 SR중지(BA030014) 시 반영 안함' — SrStatus+='중지'·suspendedFrom, suspendSr/resumeSr(결재 대기 없는 SUSPENDABLE 상태만, resume 시 suspendedFrom 복원·초기화), 지연목록·SR지연 알림·대시보드·replan 4개 배제 가드. e2e sc_sr_suspend. | — |
| GS-1 | ✅ | 예산 초과 신호 — 투자·비용 (v1.5.97/98) | 계획대비실적에서 집행>계획(집행률>100%)이 예산 접근(≥90%, warn)과 동일 warn 톤으로만 표기돼 예산 초과가 구분 안 됨. 행 rate 칩·상단 집행률 KPI 를 >100% 시 err(적색)로 분리(순수 표시, 무회귀). | 저 |
| GS-2 | ✅ | 이수현황 export false-100 (v1.5.99) | 보안교육 이수현황 CSV 에서 대상 완료과정 없는 인원(req=0, 전 셀 '해당없음')을 이수율 100% 로 표기 → '전원 이수'와 '의무 없음' 혼동(코드 주석 의도와도 상충). 이수율 셀도 '해당없음'으로 정합해 감사에서 구분 가능. | 저 |

**검증: 단일 실행 게이트 e2e 67/67 통과**(+build·smoke 569·a11y 696·emptystate 37/37·corruptfile 37/37·keycheck 3·health 47).
동시 실행(2게이트)이 포트(3700+idx)·데이터파일(.e2e-*.json) 공유로 낸 66/67·55/67 은 **동시 실행 아티팩트**로 확인(코드 회귀
아님, 단일 실행 재검증으로 확정).

**교훈: 감사 포화 후에도 원문(요구서·안내서) 재대조는 미구현 요구를 드러낸다 — sample.xlsx 결재 비고·제품안내서 본문의
조건절이 실제 기능 요구다. 거버넌스 포털의 rate 신호는 '접근'과 '초과/N-A'를 톤으로 구분해야 감사 가치가 산다(false-100·
budget-overrun 동일 클래스). 어댑터 4종·게시판·재무 트윈·SR 생명주기·deploy-readiness 는 전수 재감사 결과 확정 0 —
코드 게이트 표면은 완비. 잔여 로드맵(양식 xlsx export·SSO/근태/MS-SQL 어댑터·버튼단위 권한)은 외부 계약·오너 결정 의존.**

## 신규 도메인 + 감사 근거 산출물 (v1.5.102–116)

요구 재대조가 포화된 뒤 지시가 "gap 찾아 새 작업 생성"이라, 제품안내서 VI장의 미구현 요구를 신규 도메인으로
복원하고 감사 근거 산출물을 추가했다. 전 항목 무회귀 소버스트(fankh 공유트리)·게이트 그린.

| # | 상태 | 위치 | 내용 |
|---|:---:|------|------|
| SEC-1 | ✅ | 보안성 검토 신규 도메인 (v1.5.102–105) | 제품안내서 VI장 '시큐어코딩·취약점 점검·자료 제출' — SecurityReview 엔터티·시드, 화면(계획→진행→조치중→완료, 발견/조치·증적, 조치율 div-zero·false-100 방어, 완료는 발견 전건 조치 후만), 엑셀 export, e2e sc_security_review. 게이트 화면 37→38. |
| SEC-2 | ✅ | 보안성 검토 전 계층 통합 (v1.5.107·108·110) | 대시보드 '미조치 취약점' 운영 스냅샷(canSee 게이트·openFindings 단일원천), 일일 배치 '착수 경과' 안내메일(검토자 재직 교집합), 통합검색 인덱싱(canAccess 게이트). 원 도메인과 동일 통합 깊이. |
| SEC-3 | ✅ | 감사 로그 액션 톤 정합·강제 (v1.5.111·112) | ACTION_TONE 이 Record<string> 이라 신규 액션이 neutral 로 조용히 떨어짐(보안위반 등록 등 4종 미분류). 전 21종 색인 + Record<AuditAction> 로 타입 강제(신규 액션 톤 누락=빌드오류). |
| SEC-4 | ✅ | 신규 export 레지스트리 편입 결함 2건 (v1.5.113·114) | **실결함**: security-reviews export 가 EXPORT_MENU 누락 → 런타임 메뉴권한 게이트 우회(정적 isMgr 만). client_health ROUTES('전 화면') 누락 → 콘솔오류 커버리지 빠짐. 둘 다 등록. **교훈: 신규 화면은 NAV·emptystate·EXPORT_MENU·export핸들러·client_health·dashboard·notify·search 전 레지스트리 편입 필수 — 하나라도 빠지면 조용한 갭(전수 통과해도).** |
| SEC-5 | ✅ | 보안 컴플라이언스 종합 현황 export (v1.5.115·116) | 제품안내서 IV·VI장 'ISMS 외부 감사 대응 근거' — 서약률·이수율·점검·보안성검토 조치율·위반을 한 CSV 로 집계(각 지표 소속 화면 단일-원천 술어·div-zero/false-100 방어). /compliance/inspection(BIZ) EXPORT_MENU 게이트, smoke 로 BIZ_MGR 200·USER/DEPT_MGR 403 실증. |

**교훈: '요구 100% 구현' 뒤에도 제품안내서가 명시한 목적(ISMS 감사 근거·시큐어코딩 점검)을 서비스하는 산출물은
남아 있었다 — 82행 메뉴에 없어도 안내서 본문이 명시한 목적은 실 요구다. 단, 근거 없는 대칭 확장(IT운영 종합 등)은
안내서 특정 문구가 없으면 만들지 않는다(추측 스코프 금지). 검증 게이트 최종: build·smoke 572·a11y 697·
emptystate/corrupt 38·keycheck 3·health 48·e2e 68.**

## IT 운영 종합 현황 export (v1.5.118)

컴플라이언스 종합(SEC-5)의 대칭 — 사용자 지시로 IT 운영 관리 리포트 추가. SR(진행·지연)·장애(조치중)·
프로젝트(진척·이슈)·투자/비용 집행률을 한 CSV 로 집계(각 지표 소속 화면 단일-원천 술어·div-zero 방어).
개인별현황 전사 운영 스냅샷 카드에 다운로드(canSee('/sr/manage') 렌더), /sr/manage(BIZ) EXPORT_MENU 게이트로
담당·Admin 전용(smoke BIZ_MGR 200·USER/DEPT_MGR 403). export 유형 25종. 게이트 smoke 575·a11y 697·
emptystate/corrupt 38·e2e 68. **교훈: SEC-5 때 '근거 없는 대칭 확장 금지'로 보류했으나, 사용자가 명시 지시하면
그것이 특정 요구다 — 보류 기준(추측 스코프)과 실행 기준(명시 지시)의 경계를 정확히 지킴.**

## 종합 현황 export 대시보드 정합 실검증 (v1.5.118 후속)

컴플라이언스·IT운영 종합 export 가 대시보드와 같은 수치를 내는지 실행 서버(next start)로 BIZ_MGR 세션
실측 비교(코드상 단일-원천 술어 재사용에 더해 empirical 확인):
- **IT운영**: 조치중 장애 1=1 · 지연 SR 2=2 · 프로젝트 오픈 이슈 1=1 — 겹치는 3지표 전부 일치.
- **컴플라이언스**: 미조치 취약점 3=3(직접 일치) · 서약 signed 5 + 미서약 3 = 전체 8(교차 정합).
검증 스크립트는 스크래치패드 일회용(저장소 미포함)으로 실행 후 삭제. **교훈: 집계 export 는 소속 화면
단일-원천 술어를 재사용해도, 대시보드와의 실측 비교로 표시 계층 회귀(포맷·파싱·라벨 매칭)까지 확인하면
'수치 불일치' 리스크를 empirical 로 닫는다.**

## 보안성 검토 심각도 등급 (v1.5.127~130)

사용자 지시로 보안성 검토에 심각도 등급을 추가 — findings/fixed 단일 총계를 등급별(심각·높음·보통·낮음)
발견/조치로 세분하고, 고위험(심각+높음) 미조치를 우선 조치 신호로 전면 노출.

| # | 상태 | 위치 | 내용 |
|---|:---:|------|------|
| SV-1 | ✅ | 데이터 모델 (v1.5.127) | SecurityReview 에 등급별 발견/조치 평면 numField 8종(critFound/critFixed…). 중첩 객체는 스토어 정규화(corrupt-guard)가 드롭하므로 평면. findings/fixed 는 등급 합의 유지된 총계 → 기존 소비처(대시보드·notify·search·export) 무변경. |
| SV-2 | ✅ | 화면 + 단일원천 (v1.5.128) | recordFindings 등급별 입력(각 등급 조치 ≤ 그 등급 발견), 상단 '고위험 미조치' 스탯·행 '고위험 N' 마커. `highSevOpen`(store) 단일원천으로 화면·대시보드·export 정합. |
| SV-3 | ✅ | 통합 (v1.5.129) | 대시보드 운영 스냅샷 '고위험 미조치'(err)·기존 미조치 warn 강등. 컴플라이언스 종합 export '고위험 미조치 N건'. 전부 highSevOpen. |
| SV-4 | ✅ | e2e (v1.5.130) | sc_security_review 등급별 갱신 — 고위험 2 마커 → 완료 차단 → 등급별 전건 조치 → 고위험 소거 → 완료. 게이트 e2e 68/68·smoke 575·a11y 697·keycheck 3·health 48. |

**교훈(운영): 게이트 keycheck 2/3 실패가 '역전된 결과'(커스텀 키 거부·기본 키 통과)로 나오면 코드 회귀가
아니라 그 포트(3419)에 남은 유령 서버에 붙은 것이다 — 이번엔 삭제한 일회용 검증 스크립트(verify_*.mjs,
next start -p 3419)의 server.kill() 이 npx→next 자식을 못 거둬 서버가 남았다. 스크립트를 지워도 서버는
남으니, 검증 스크립트는 keycheck 와 같은 포트(3419)를 피하거나 종료 후 netstat 로 리스너 회수 확인할 것.**

## 컴플라이언스 추세 스냅샷 (v1.5.135~138)

사용자 지시로 컴플라이언스 포스처를 주기별 스냅샷으로 남겨 전기 대비 개선 추이(ISMS 감사 근거)를 본다.

| # | 상태 | 위치 | 내용 |
|---|:---:|------|------|
| CT-1 | ✅ | KPI 단일원천 (v1.5.135) | 종합 export 인라인 KPI 술어를 `lib/compliance` computeComplianceKpis 로 추출 — 종합·스냅샷이 공유해 수치 불일치 구조적 차단. export 는 헬퍼 사용(무회귀 smoke 575). |
| CT-2 | ✅ | 데이터 모델 (v1.5.136) | ComplianceSnapshot(period·서약/이수/조치율·점검·고위험·미조치·위반) store 컬렉션·시드 2건(2026-06·07 개선 추이). 평면 필드(손상 내성). |
| CT-3 | ✅ | 스냅샷·추세 카드·export (v1.5.137) | 보안점검(ISMS) 화면 '컴플라이언스 추세' 카드(기간별 KPI + 직전 대비 델타 칩, 개선 방향 색)·'현황 스냅샷 기록'(당월 upsert). compliance-trend export(BIZ 게이트). audit '컴플라이언스 스냅샷' 액션(Record<AuditAction> 강제 톤). |
| CT-4 | ✅ | 테스트 (v1.5.138) | e2e sc_compliance_trend(시드 2건·개선 델타 +12·기록→감사 확인) + smoke 추이 export BIZ_MGR 200·USER 403. 게이트 e2e 69/69·smoke 577·a11y 698·keycheck 3·health 48·emptystate/corrupt 38. |

**교훈: 집계 산출물이 둘 이상(종합 export·추세 스냅샷)이면 KPI 술어를 lib 헬퍼로 먼저 단일원천화한 뒤
소비처를 얹어야 시점 간 수치가 어긋나지 않는다(highSevOpen·computeComplianceKpis 동일 원칙). 스냅샷은
동일 기간 재기록을 upsert 로 처리해 중복 없이 최신 포스처를 유지한다.**

### CT-5 추세 자동 스냅샷 — 일배치 (v1.5.141~142)
추세 스냅샷을 수동에서 자동으로 확장. upsert 를 `upsertComplianceSnapshot(s, by)`(lib/compliance) 단일
경로로 추출(수동 액션 리팩터, 무회귀)한 뒤, runDailyNotify 일배치가 당월 포스처를 자동 스냅샷(by 스케줄러)한다.
재진입 가드가 중복 틱 직렬화, 당월 upsert 라 매일 호출돼도 1건 유지·최신 refresh. 배치 이력·감사 기록.
e2e sc_scheduler 에 자동 스냅샷 검증(배치 이력·추세 카드 by 스케줄러) 추가, 게이트 69/69. **교훈: 서버
액션의 store 뮤테이션을 lib 헬퍼로 추출하면 배치·스케줄러에서 동일 로직을 재사용할 수 있다(중복·표류 없음).**

## 컴플라이언스 포스처 점수 (v1.5.144~146)

사용자 지시로 컴플라이언스 KPI 를 경영 보고용 단일 점수(0~100)로 합성. 5축(서약·이수·조치·점검·리스크)
각 20점 균등, 리스크축은 고위험 미조치(-10/건)·위반 미완료(-5/건) 차감(하한 0). compliancePostureScore·
postureRating(양호/보통/주의/미흡) lib/compliance 단일원천.

| # | 상태 | 위치 | 내용 |
|---|:---:|------|------|
| PS-1 | ✅ | 점수 산출 (v1.5.144) | compliancePostureScore(5축 20점)·postureRating. ComplianceSnapshot 에 score field, 시드 2건(56·65), upsert score 계산. |
| PS-2 | ✅ | 화면·대시보드·export (v1.5.145) | 보안점검 화면 '현재 포스처 점수' 배너(점수·등급)+추세 카드 점수 열(델타). 대시보드 운영 스냅샷 '컴플라이언스 점수' 신호(등급 tone). 종합 export 최상단 점수 행, 추이 export 점수 열. |
| PS-3 | ✅ | 테스트 (v1.5.146) | e2e sc_compliance_trend 에 점수 배너·점수 델타(+9, 56→65)·대시보드 점수 신호 검증. 게이트 e2e 69/69·smoke 577·a11y 698·keycheck 3·health 48. |

**교훈: 다지표 거버넌스는 '한 숫자'(가중 점수)로 요약하되 산출을 lib 헬퍼로 단일원천화해 화면·대시보드·
export·스냅샷이 같은 값을 보이게 한다. 점수 축 가중치는 코드에 명시(문서화 가능)해 현업이 근거를 안다.**

## 포스처 축 분해 — 개선 우선순위 (v1.5.149~151)

사용자 지시로 포스처 점수(단일 숫자)를 구성 5축으로 분해해 행동으로 바꿈. postureAxes 가 서약·이수·조치·
점검·리스크 각 축의 원점수·기여분·개선 행동 안내를 산출하고, compliancePostureScore 를 그 축들의 평균으로
리팩터(값 동일)해 점수와 분해를 단일원천화. weakestPostureAxis 가 최약 축(개선 우선순위)을 지목.

| # | 상태 | 위치 | 내용 |
|---|:---:|------|------|
| AX-1 | ✅ | 축 분해 (v1.5.149) | postureAxes(5축 pct·points·advice)·weakestPostureAxis. compliancePostureScore=축 평균 리팩터(수치·시그니처 불변). |
| AX-2 | ✅ | 화면·export (v1.5.150) | 보안점검 화면 포스처 배너 아래 축 분해 패널(미니 막대·최약 축 빨강 틴트)+개선 우선순위 줄(행동 안내). 종합 export 개선 우선순위(최약 축) 행. |
| AX-3 | ✅ | 테스트 (v1.5.151) | e2e sc_compliance_trend 축 분해 패널·개선 우선순위 검증, smoke 종합 export 포스처·우선순위 행 needle. 게이트 e2e 69/69·smoke 578·a11y 698·keycheck 3·health 48. |

**교훈: 경영용 단일 점수는 '무엇을 먼저 고칠지'로 이어져야 실행된다. 점수를 구성 축으로 분해하되 점수를
축의 평균으로 정의해 분해와 총점이 한 원천에서 나오게 하고, 최약 축을 개선 행동 문장으로 지목한다.**

## 인프라 운영 헬스 — 단일 원천 (v1.5.154~157)

사용자 지시로 비-컴플라이언스 도메인 갭 발굴 결과, 인프라 운영의 3대 헬스 신호(배치 실패·인터페이스 오류·
디스크 경고)가 각 화면 인라인 집계로만 존재해 대시보드·경영 보고 종합 현황에서 안 보이던 사각을 해소.
lib/infra computeInfraHealth 단일원천으로 모아 화면·대시보드·export 정합.

| # | 상태 | 위치 | 내용 |
|---|:---:|------|------|
| IF-1 | ✅ | 단일 원천 (v1.5.154) | lib/infra computeInfraHealth(배치·인터페이스·디스크)·DISK_WARN export. 데이터 전부 in-store(연동 무관). |
| IF-2 | ✅ | 화면 소비 (v1.5.155) | operations·systems 화면이 헬퍼 소비, 인라인 중복 집계·DISK_WARN 재정의 제거. |
| IF-3 | ✅ | 대시보드·export (v1.5.156) | 전사 운영 스냅샷 배치·인터페이스·디스크 타일(출처 화면 유효권한 게이트), IT운영 종합 export 인프라 운영 행. |
| IF-4 | ✅ | 테스트 (v1.5.157) | e2e sc_infra_health 대시보드 타일=출처 화면 값 대조(단일원천 증명)·종합 export 행, smoke needle. 게이트 e2e 70/70·smoke 579·a11y 698·keycheck 3·health 48. |

**교훈: 운영 헬스 신호가 소속 화면에만 갇히면 '지금 뭔가 났나'를 판단하려 화면을 여럿 열어야 한다. 도메인
공통 헬퍼(lib/infra)로 모아 대시보드·종합 현황이 같은 값을 공유하게 하고, 크로스도메인 타일은 출처 화면
유효권한으로 게이트해 런타임 메뉴 제한과 정합을 지킨다.**

## 재무 집행 KPI — 단일 원천 (v1.5.160~164)

비-컴플라이언스 도메인 갭 발굴 #2. 투자·비용 계획 대비 집행률이 화면 2곳(invest·expense)+IT운영 종합
export 1곳에 3중 복제돼, 확정계획 스코프 산식 변경 시 화면·산출물 divergence 위험이 있었다. lib/finance
computeFinanceKpis 로 통합.

| # | 상태 | 위치 | 내용 |
|---|:---:|------|------|
| FN-1 | ✅ | 단일 원천 (v1.5.160) | lib/finance computeFinanceKpis(plans·contracts·settlements) — 확정계획 스코프 정합·집행률·div-zero 방어. 컬렉션 인자형(화면 표와 KPI 가 같은 배열). |
| FN-2 | ✅ | 화면 소비 (v1.5.161) | invest·expense 화면이 헬퍼 소비, byte-for-byte 중복 집행률 블록 제거(-25행). |
| FN-3 | ✅ | export·대시보드 (v1.5.162) | IT운영 종합 export execRate 세번째 복제 제거(local pct 삭제), 대시보드 투자·비용 집행률 타일. |
| FN-4 | ✅ | 접근 불변 수정·테스트 (v1.5.163~164) | e2e sc_finance_exec 화면=대시보드=export 정합(3중 drift 폐쇄 증명)·smoke needle. 게이트가 잡은 회귀 수정: 재무 타일 canSee('/finance/*')(역할스코프 접근 열림)가 운영 스냅샷을 USER·DEPT_MGR 에 노출 → 관리자(/sr/manage) 게이트로 제한. 게이트 e2e 71/71·smoke 580. |

**교훈: 같은 지표가 화면·export·대시보드 3곳에 복제되면 산식 변경이 divergence 를 낳는다. 컬렉션 인자형
헬퍼로 통합하면 표와 KPI 가 같은 배열을 공유한다. 대시보드 전사 집계 타일은 개별 화면 접근권(역할 스코프)
이 아니라 관리 시야(담당·Admin) 게이트로 제한해야 운영 스냅샷 노출 불변이 깨지지 않는다(게이트가 적발).**

## 프로젝트 PMO — 이슈·리스크·산출물 대장·신호 (v1.5.167~170)

비-컴플라이언스 도메인 갭 발굴 #3. 일정·산출물·이슈 화면의 이슈·리스크·산출물 데이터가 화면 밖으로
나갈 수 없었고(대장 export 없음), 대시보드는 오픈 이슈 건수만 보였다. lib/projects computeProjectPmo 로
신호를 단일원천화하고 두 대장 export·높은 리스크 신호를 추가.

| # | 상태 | 위치 | 내용 |
|---|:---:|------|------|
| PJ-1 | ✅ | 단일 원천 (v1.5.167) | lib/projects computeProjectPmo — 산출물 완료·기한경과, 오픈 이슈, 높은 리스크(오픈). |
| PJ-2 | ✅ | export·화면 (v1.5.168) | 이슈·리스크 대장(project-issues)·산출물 대장(deliverables, 기한경과 상태) export(담당·Admin 게이트), schedule 화면 헬퍼 소비·다운로드 버튼. |
| PJ-3 | ✅ | 대시보드 (v1.5.169) | 전사 운영 스냅샷 프로젝트 높은 리스크 타일, 오픈 이슈 타일도 computeProjectPmo 단일원천 소비. |
| PJ-4 | ✅ | 테스트 (v1.5.170) | e2e sc_project_pmo 대시보드=schedule 화면 정합·두 대장 다운로드, smoke 대장 200·USER/DEPT_MGR 403. 게이트 e2e 72/72·smoke 584·a11y 698·keycheck 3·health 48. |

**교훈: 거버넌스 화면의 대장(이슈·리스크·산출물)은 보고 산출물로 나갈 수 있어야 한다(PMO·운영위). 화면
집계 신호는 lib 헬퍼로 단일원천화해 대시보드와 정합을 지키고, 신규 대장 export 는 화면과 동일 게이트로
접근을 제한한다(smoke 200·403 동시 검증).**

## 월별 장애 통계 export (v1.5.173~175)

비-컴플라이언스 도메인 갭 발굴 #4(정찰 shortlist 마지막). 장애관리 화면의 발생월×등급 통계표가 화면에서
렌더링만 되고 export 는 원시 대장(건별)뿐이라, 관리·감사가 보고하는 주기별 통계표(제품안내서 III장)를
산출물로 받을 수 없었다. lib/infra monthlyIncidentStats 로 통합.

| # | 상태 | 위치 | 내용 |
|---|:---:|------|------|
| IS-1 | ✅ | 단일 원천 (v1.5.173) | lib/infra monthlyIncidentStats — 발생월×등급 집계(등급=FAULT_GRADE 코드∪등장 등급, 계·조치완료). |
| IS-2 | ✅ | export·화면 (v1.5.174) | 월별 장애 통계 export(incident-stats, /infra/incidents 게이트), 화면 통계표가 헬퍼 소비(인라인 중복 제거)·다운로드 버튼. |
| IS-3 | ✅ | 테스트 (v1.5.175) | e2e sc_incident_stats 화면 첫 월 '계'=export 같은 월 '계' 대조(단일원천), smoke 통계 200·USER 403. 게이트 e2e 73/73·smoke 586·a11y 698·keycheck 3·health 48. |

**교훈: 화면의 집계 통계표(발생월×등급 등)는 렌더링만이 아니라 보고 산출물(엑셀)로 나갈 수 있어야 한다.
집계 술어를 lib 헬퍼로 단일원천화하면 화면 표와 export 가 같은 값을 보장하고, e2e 로 셀 단위 정합을 검증한다.**

## SR 중지-변경 고착 결함 수정 + 지연 판정 단일 원천 (v1.5.178~182)

정찰(untouched 도메인)에서 적발한 실결함. 시스템개발 SR 이 적용요청 상태로 변경관리에 편입된 뒤 중지되면,
변경결과 승인 전파(sr.status==='적용요청' 요구)가 '중지' 때문에 누락돼 SR 이 적용요청에 영구 고착되고
변경만 최종완료로 남았다(변경 approval 은 cw.id 로 키잉돼 suspendSr 의 srNo 대기 가드에 안 걸림).

| # | 상태 | 위치 | 내용 |
|---|:---:|------|------|
| SR-STRAND-1 | ✅ | 결함 수정 (v1.5.179) | suspendSr 에 비-최종완료 변경 편입 SR 중지 금지 가드 + resumeSr 에 최종완료 변경 존재 시 완료 전파(고아 해소). |
| SR-STRAND-2 | ✅ | 단일 원천 (v1.5.178·180) | lib/sr(isSrDelayed·delayedSrs·isSrReplannable·SR_INACTIVE_STATUSES). 지연·재계획 판정 5중 리터럴 복제(화면·대시보드·export·알림 배치)를 통합. |
| SR-STRAND-3 | ✅ | 테스트 (v1.5.181~182) | e2e sc_sr_suspend_strand — 적용요청 SR(SR-2026-0132) 변경 편입 후 중지 차단 검증(기존 sc_sr_suspend 정상 중지·재개와 양방향). 게이트 e2e 74/74·smoke 586·a11y 698·keycheck 3·health 48. |

**교훈: 상태 전파가 특정 상태(적용요청)를 요구하고 그 상태를 벗어나게 하는 다른 전이(중지)가 열려 있으면,
전파를 놓쳐 종결 불가 고아가 생긴다. 전파 선행 상태를 벗어나는 전이에 '진행 중 상위 워크플로(변경) 편입'
가드를 걸고, 복원 지점에 놓친 전파 보정(belt-and-suspenders)을 둔다. 결함 수정은 재현 e2e 를 동반한다.**

## QnA 폐쇄 루프 (v1.5.184~186)

정찰(untouched 도메인) 발굴 — QnA 가 포털에서 유일하게 폐쇄 루프(할일+일배치 독촉)가 없던 상호작용
워크플로였다. assign 은 담당만 설정하고 할일을 안 만들었고, runDailyNotify 에 미답변 QnA 유형이 없었다.

| # | 상태 | 위치 | 내용 |
|---|:---:|------|------|
| QNA-LOOP-1 | ✅ | 할일 폐쇄 루프 (v1.5.184) | TodoKind 에 'QnA' 추가(+KIND_HREF '답변하기' 컴파일 강제). assign 시 답변 할일 생성(재지정 이전 할일 마감), answer 시 id 선두 고정 마감(다른 관리자 답변도 닫힘). |
| QNA-LOOP-2 | ✅ | 일배치 (v1.5.185) | runDailyNotify 에 미답변 QnA 안내 — 담당 지정 미답변 문의 담당자(재직 교집합, 유령 독촉 방지). |
| QNA-LOOP-3 | ✅ | 테스트 (v1.5.186) | e2e sc_qna_loop — 문의→담당 지정→나의 할일 생성→답변→마감 실브라우저 검증. 게이트 e2e 75/75·smoke 586·a11y 698·keycheck 3·health 48. |

**교훈: 상호작용 워크플로(담당 지정→처리)는 담당의 나의 할일과 일배치 독촉으로 폐쇄 루프를 이뤄야 잊히지
않는다. 할일 마감은 id 선두 고정 앵커드-클로즈로 소유자 무관하게 닫아 고아·무한 독촉을 막고, 독촉 대상은
재직 명단과 교집합한다(전 배치 유형 공통 규약).**

## 프로필 데이터 격리 — AD-7 종결 (v1.5.188~189)

정찰 shortlist 마지막 항목. 재사용 프레임워크(한 이미지 × PORTAL_PROFILE 전환)에서 PORTAL_DATA_FILE 에
프로필 식별자가 없어, 한 파일을 프로필 전환에 재사용하면 프로필 스코프 런타임 설정(채널 가동상태·메뉴
오버레이)이 새던 무결성 갭.

| # | 상태 | 위치 | 내용 |
|---|:---:|------|------|
| AD-7a | ✅ | 스탬프·리셋 (v1.5.188) | saveNow 가 __profileStamp(현 PORTAL_PROFILE) 각인, loadFromFile 이 활성 프로필과 불일치 시 channelStates·menuOverrides 를 활성 프로필 시드로 리셋(도메인 데이터 보존). 스탬프 없는 파일은 무리셋(무회귀). |
| AD-7b | ✅ | 테스트 (v1.5.189) | e2e sc_profile_data_isolation — 'manufacturer' 스탬프+security-db 강제가동 파일을 default 로 기동 → 채널상태 시드 리셋(활성 4/5). 게이트 e2e 76/76·smoke 586·a11y 698·keycheck 3·health 48. |

**교훈: 재사용 프레임워크의 영속 파일은 어느 프로필로 만들어졌는지 각인해, 프로필 전환 시 프로필 스코프
런타임 설정이 새지 않게 격리한다(도메인 데이터는 보존). 스탬프 없는 파일은 리셋 대상에서 제외해 구버전·
손상 내성 동작과 무회귀를 지킨다. 이로써 정찰 shortlist(SR·QnA·프로필) 3건이 모두 종결됐다.**

## QnA 삭제 고아 할일 방지 (v1.5.191~193)

3차 정찰이 적발한 확정 결함 — QnA 폐쇄 루프(v1.5.184~) 도입 시 assign 이 담당의 답변 할일을 만들었으나
deleteQna 는 문의·첨부만 정리하고 그 할일을 닫지 않았다. answer·재지정만이 할일을 닫는데 삭제로 문의가
사라지면 두 경로가 막혀 담당 나의 할일에 영구 고아로 남았다(v1.5.92 QnA 생명주기 감사가 첨부정리·작성자
가드만 봐 assign→delete 상호작용을 놓침).

| # | 상태 | 위치 | 내용 |
|---|:---:|------|------|
| QNA-DEL-1 | ✅ | 결함 수정 (v1.5.191) | deleteQna 가 문의 id 선두 고정으로 담당 QnA 답변 할일 마감(앵커드-클로즈), revalidate '/' 레이아웃으로 확대(나의 할일 즉시 반영). |
| QNA-DEL-2 | ✅ | 테스트 (v1.5.192~193) | e2e sc_qna_delete_orphan — 문의→담당 지정→작성자 삭제→담당 할일 마감 검증. 삭제 후 RSC 재렌더 대기(재조회) 보정. 게이트 e2e 77·smoke 586·a11y 698·keycheck 3·health 48. |

**교훈: 워크플로에 할일 생성(assign)을 더하면, 그 엔터티의 모든 종료 경로(답변·재지정·삭제)에서 할일 마감을
빠짐없이 걸어야 한다. 삭제는 다른 마감 경로(답변)의 전제(엔터티 존재)를 없애므로 특히 놓치기 쉽다 — 앵커드-
클로즈로 소유자·경로 무관하게 닫고, 재현 e2e 로 잠근다.**

## 교육 이수 할일 앵커드-클로즈 (v1.5.198~199)

3차 정찰이 지목한 결함 — registerAttendees 가 '보안교육' 할일을 과정명 부분일치(includes)로 닫아, 등록
과정명이 다른 과정 할일 제목의 부분문자열이면('정보보호 교육' ⊂ '상반기 정보보호 교육 이수') 무관한 할일을
오마감했다. QnA·SR 등은 모두 id/제목 선두 고정 앵커드-클로즈인데 교육만 부분일치였다(아웃라이어).

| # | 상태 | 위치 | 내용 |
|---|:---:|------|------|
| EDU-CLOSE-1 | ✅ | 결함 수정 (v1.5.198) | registerAttendees 의 할일 마감 매칭을 t.title.includes(course.title) → t.title === `${course.title} 이수`(정확 일치)로 변경. 정상 마감 무변경, 부분일치 오마감만 차단. |
| EDU-CLOSE-2 | ✅ | 테스트 (v1.5.199) | e2e sc_education_todo_exact — 콜리전 과정 생성→명단 등록→무관 상반기 교육 할일 유지 검증. 수정 전 코드에선 실패함을 대조 확인(테스트 유효성). 게이트 e2e 78·smoke 586·a11y 698·keycheck 3·health 48. |

**교훈: 폐쇄 루프의 할일 마감은 부분일치(includes)가 아니라 id/제목 정확 일치(앵커드-클로즈)여야 한다.
부분일치는 이름/제목이 다른 항목의 부분문자열이면 무관한 항목을 오마감해 '완료로 보이지만 실제 미이수'인
숨은 괴리를 만든다. 수정에는 '수정 전엔 실패'를 대조 확인한 재현 e2e 를 동반해 테스트 유효성을 보증한다.**

## 월별 장애 통계 무효월 방어 (v1.5.201~202)

4차 정찰이 지목한 잠재 취약점 — v1.5.10 월별 장애 통계(monthlyIncidentStats)가 손상/누락 occurredAt('')를
만나면 slice→''→startsWith('') 전건 매칭으로 '' 월 행이 전체 장애를 중복 집계했다. 크래시는 아니나(corruptfile
게이트가 무크래시만 확인) 무날짜 데이터가 통계를 왜곡하는, 정찰이 짚은 corrupt-data 내성 사각.

| # | 상태 | 위치 | 내용 |
|---|:---:|------|------|
| INC-EMPTY-1 | ✅ | 결함 수정 (v1.5.201) | monthlyIncidentStats 월 버킷을 유효 YYYY-MM(/^\d{4}-\d{2}$/)만으로 한정 — ''·부분값 무효월 제외. 유효 날짜 집계 불변, 손상 날짜 장애만 제외. |
| INC-EMPTY-2 | ✅ | 테스트 (v1.5.202) | e2e sc_incident_stats_empty_month — occurredAt '' 장애 주입 데이터 파일로 빈-발생월 행 없음·2026-08 계=2 검증. 수정 전 코드에선 실패함을 대조 확인(테스트 유효성). 게이트 e2e 79·smoke 586·a11y 698·keycheck 3·health 48. |

**교훈: 문자열 접두 매칭(startsWith)으로 버킷을 만들 때 빈 문자열('')은 전건 매칭이라 '전체를 담는 유령
버킷'을 만든다. 버킷 키는 형식(정규식)으로 검증해 무효값을 제외한다. corruptfile 게이트가 무크래시만 보므로,
손상 데이터가 크래시 없이 집계를 왜곡하는 사각은 크래프트 데이터 파일 e2e 로 별도 검증한다.**

## 세 번째 예시 고객사 프로필 — 금융권 (v1.5.204~205)

확정 재현 버그 표면 포화(4차 정찰 0건) 이후의 프레임워크 실증 작업 — 결함 수정이 아니라 브리프 핵심('한 본체
× 프로필 전환으로 산업 확장')의 커버리지 확대다. 제조업·공공기관 두 예시만으론 '우연한 비결합'을 의심할 여지가
남아, 서로 다른 세 번째 산업(금융권) 토폴로지가 본체 무변경으로 뜨는지 검증했다.

| # | 상태 | 위치 | 내용 |
|---|:---:|------|------|
| PROFILE3-1 | ✅ | 프로필·등록 (v1.5.204) | profiles/sample-finance.config.ts 신규(나래금융 예시·NARAE FIN PORTAL — SMS 를 OTP·이상거래·경과 알림 실채널로, 보안·출력물 관제 기본가동, 전 채널 fin-* adapterId). profiles/registry.ts 에 finance 등록, lib/integrations/registry.ts 에 fin-mail·fin-sms·fin-hr·fin-asset·fin-secdata → 목업 매핑. 포털 본체·화면·로직 무변경. |
| PROFILE3-2 | ✅ | 테스트 (v1.5.205) | e2e sc_profile_finance(PORTAL_PROFILE=finance) — 로그인 브랜딩·상태바 고객사·채널 구성(OTP·secdata)·fin-* 어댑터 바인딩·자가진단 전 프로필 적합·fin-asset 어댑터 실동작(자산 조회) 실브라우저 검증. 4번째 프로필 추가 후 기존 sc_profile(제조)·sc_profile_public(공공) 통과 재확인(무회귀). 게이트 e2e 80·smoke 586·a11y 698·keycheck 3·health 48. |

**교훈: 재사용 프레임워크의 '본체 비결합'은 예시 프로필 2개로는 우연일 수 있다 — 서로 다른 산업 3개(제조·공공·
금융)가 본체 무변경으로 뜨고 어댑터 자가진단이 전 프로필 적합이면 커스터마이징 표면이 프로필·어댑터 계층으로
격리됐음이 실증된다. 새 프로필의 fin-* 어댑터는 registry 등록이 필수다 — 미등록 시 자가진단이 red 가 되어
기존 프로필 e2e(전 프로필 적합 단언)까지 깨지므로, 프로필 추가와 어댑터 등록은 한 커밋에 함께 간다.**

## 정산>계약>계획 기준액 우선순위 — 투자 반영 (v1.5.206~207)

요구사항(sample.xlsx)이 "검증가치 높음"으로 명시한 금액표시 우선순위 규칙의 구현 깊이 갭. 깊이 감사(spec 대비
행동 대조)가 지목했다 — 비용 속보 화면에는 `basisOf` 로 정산>계약>계획 우선순위가 있었으나 투자 화면에는
전혀 없어(계획대비실적이 계획액·계약액·집행액을 별도 열로만 표시), 요구사항이 말하는 '우선순위가 적용된 단일
기준액'으로 수렴하지 않았다. `lib/finance.ts`(재무 산식 단일 원천)에도 기준액 헬퍼가 없어 우선순위 로직이
화면·export 에 복제될 소지가 있었다.

| # | 상태 | 위치 | 내용 |
|---|:---:|------|------|
| BASIS-1 | ✅ | 헬퍼 (v1.5.206) | lib/finance.ts 에 planBasisAmount(plan, contracts, settlements) 신규 — 지급완료 정산 있으면 정산액, 없고 계약 있으면 계약액, 둘 다 없으면 계획액을 {basis, amount}로 반환. computeFinanceKpis 와 같은 스코프 규약(종류별 필터 컬렉션 주입)으로 화면·export 가 같은 우선순위를 공유하는 단일 원천. |
| BASIS-2 | ✅ | 화면·테스트 (v1.5.207) | 투자 계획대비실적(invest/page.tsx)에 '기준액' 열 추가 — planBasisAmount 산출 기준 칩+금액 표시, 기존 계획/계약/집행 열은 보존. e2e sc_invest_basis: IP-2026-01(계약+지급완료정산 병존)의 기준이 정산으로 표시됨을 확인해 정산>계약 우선순위 검증, IP-2026-02(계약만)의 기준이 계약임을 확인. 수정 전(열 제거) 코드에선 두 단언 모두 실패함을 대조 확인. 게이트 e2e 81. |

**교훈: 요구사항이 명시한 표시규칙(정산>계약>계획 우선순위)이 한 화면(비용)에만 있고 대칭 화면(투자)에
없으면 '부분 구현'이다 — 메뉴·화면이 다 있어도 행동 깊이에 갭이 남는다. 우선순위·기준선택 같은 산식은
lib/finance 단일 원천 헬퍼로 두어 화면·export 가 공유해야 복제 drift 를 막는다. 새 표시열도 '수정 전엔
실패'를 대조 확인한 재현 e2e 를 동반한다. (단, 비용 속보의 계약-계획 tier 월별 매핑은 모호성이 있어 이번
범위에서 제외 — 투기적 변경 금지 원칙.)**

## 보안점검 경과 알림 팀장 수신 (v1.5.208~209)

깊이 감사(spec 대비 행동 대조)가 지목한 두 번째 갭. 요구사항(sample.xlsx)의 보안점검은 "담당자, 팀장에 경과
항목 알림 - 메일"을 명시하나, 알림 배치의 '점검 경과'는 경과 점검 항목의 담당자(inspector)에게만 발송하고
팀장은 통지에서 빠져 있었다. 데이터 모델(InspectionPlan)에 팀장 참조가 없어 팀장 통지가 구조적으로 불가능했다.

| # | 상태 | 위치 | 내용 |
|---|:---:|------|------|
| INSPTL-1 | ✅ | 데이터·알림 (v1.5.208) | InspectionPlan 에 teamLead?(플랫 문자열) 추가, 시드 계획에 팀장 지정. notify.ts '점검 경과' 가 경과 점검의 담당자+팀장을 함께 수신자로(둘 다 재직 교집합, 팀장 미지정은 담당자만, Set 중복정리). 부서→팀장 불완전 유도 대신 계획별 명시적 지정. |
| INSPTL-2 | ✅ | 화면·테스트 (v1.5.209) | inspection 페이지: 점검계획 수립 폼에 팀장 선택(비-USER, 선택·검증) + 진행내역 표에 팀장 열. e2e sc_inspection_teamlead: 격리 데이터 파일(경과 점검 1건, 담당자 박정호·팀장 시스템관리자)로 배치 실행→감사 로그 '점검 경과 2명' 확인, 수정 전엔 '1명'으로 실패 대조. 게이트 e2e 82. |

**교훈: 요구가 복수 수신자(담당자+팀장)를 명시했는데 한쪽만 통지하면 부분 구현이다. 유도가 불완전한 관계
(Person 에 부서만 있어 팀장 유도 불가)는 명시적 필드로 모델링해 투기적 매핑을 피한다 — 플랫 문자열 필드는
스토어 손상 가드에 안전하고 선택 필드로 무회귀. 알림 수신자 수 갭은 감사 로그의 유형별 대상수를 격리
데이터 파일 e2e 로 단언해 '수정 전 1명 → 후 2명'을 대조 확인한다.**

## 인프라 변경 원복계획 별개 필드 (v1.5.210~211)

2차 깊이 감사가 지목한 유일한 명확·실행가능 갭. 요구사항(sample.xlsx·제품안내서 III장)은 "작업계획, 원복계획
필요"로 둘을 구분해 요구하나, ChangeWork 모델에 계획 필드가 plan 하나뿐이라 등록 폼이 단일 입력에
"작업계획 · 원복계획"으로 뭉쳐 받고, 목록·엑셀 어디에도 원복계획이 작업계획과 구분되지 않았다(변경 실패 시
되돌림 절차가 독립 산출물로 관리되지 않는 깊이 갭).

| # | 상태 | 위치 | 내용 |
|---|:---:|------|------|
| ROLLBACK-1 | ✅ | 데이터·등록 (v1.5.210) | ChangeWork 에 rollbackPlan?(플랫) 추가, 시드 변경을 작업계획·원복계획 별개 필드로 분리. addInfraChange 가 원복계획을 별개 입력으로 받아 저장(필수), 등록 폼을 두 입력으로 분리. |
| ROLLBACK-2 | ✅ | 표시·export·테스트 (v1.5.211) | 변경 목록에 '작업·원복계획' 열(원복 ↩ 구분 표기), changes export 에 원복계획 열 추가. e2e sc_infra_rollback_plan: 격리 데이터의 기존 변경(원복 보유) 별개 표시 + 폼 신규등록 별개 저장·표시 검증, 수정 전 원복 미표시로 실패 대조. 게이트 e2e 83. |

**교훈: 요구가 두 산출물(작업계획·원복계획)을 별개로 명시했는데 한 필드에 뭉쳐 담으면 placeholder 만 둘을
적어도 부분 구현이다 — 데이터 모델이 하나면 검색·집계·감사에서 원복 절차를 독립 추적할 수 없다. 별개 필드로
분리하고 등록·목록·export 전 경로에 반영하며, '수정 전엔 미표시'를 대조 확인한 격리 데이터 e2e 를 동반한다.**

## 특별서약(보안담당자) 담당업무 커버리지·문서 정합 (v1.5.213)

깊이 감사 이후 잔여 스펙 정찰 중 확인. 특별서약(보안담당자) 담당업무 추가입력은 완전 구현 상태(signSpecial
담당업무 필수 캡처·제출 폼·표시)였으나, e2e 는 특별서약 중 프로젝트 참여만 다루고 보안담당자 담당업무 입력
경로는 미검증인 커버리지 공백이 있었다. 또 관리 화면 안내가 "특별서약 화면은 이후 버전"으로 남아 이미 구현된
기능을 미구현으로 잘못 안내(코드-문서 불일치)했다.

| # | 상태 | 위치 | 내용 |
|---|:---:|------|------|
| SPDUTY-1 | ✅ | 테스트·문서 (v1.5.213) | e2e sc_special_pledge_duty — 격리 데이터(보안담당자 박정호, 일반 완료·특별 미제출)로 담당업무 입력·동의·제출→제출완료·담당업무 표시 검증. 담당업무 저장 제거 시 실패 대조(테스트가 캡처 경로를 지킴). pledge/manage 안내 문구를 구현 상태(담당업무 추가입력 제출)로 현행화. 게이트 e2e 84. |

**교훈: 기능이 구현돼도 필수 입력 필드가 있는 제출 경로가 e2e 미검증이면 회귀 위험이 남는다 — 커버리지
추가 시에도 '캡처 제거 시 실패'를 대조 확인해 테스트가 vacuous 하지 않음을 보증한다. '이후 버전' 류 낡은
안내 문구가 구현된 기능을 미구현으로 오안내하면 코드-문서 불일치이므로 함께 현행화한다.**

## 협력업체서약서 결재 폐쇄루프 커버리지 (v1.5.215)

잔여 커버리지 정찰 중 확인. 협력업체서약서 징구→결재상신→승인 폐쇄루프(결재 시트 12번)는 완전 구현
(addCompanyPledge·submitCompanyPledges·승인 시 완료 전파)이나 e2e 가 전혀 없었다. 대칭 흐름인 부서서약 현황
상신은 sc_pledge 로 검증되는데 협력업체 경로만 무검증인 비대칭 — 전체 결재 워크플로우가 회귀 보호 부재.

| # | 상태 | 위치 | 내용 |
|---|:---:|------|------|
| CPLG-1 | ✅ | 테스트 (v1.5.215) | e2e sc_company_pledge — 격리 데이터(협력업체 서약 없음)에서 시스템관리자 징구 등록(등록)→선택 결재상신(결재중)→박정호 승인→완료 전파 전 구간 검증. 승인→완료 전파(actions.ts 폐쇄루프 1-8) 제거 시 실패 대조(테스트가 워크플로우를 지킴). 등록자≠결재자라 자기결재 아닌 정상 흐름. 게이트 e2e 85. |

**교훈: 대칭 흐름(부서서약 vs 협력업체서약)에서 한쪽만 e2e 로 검증되면 나머지 결재 워크플로우 전체가 회귀
보호 사각이다 — 등록→상신→승인→완료전파 폐쇄루프는 전 구간을 실브라우저로 훑고, 핵심 전파 단계 제거 시
실패를 대조 확인해 커버리지가 진짜 워크플로우를 지키는지 보증한다.**

## 보안위반 결재제외자 별도관리 (v1.5.216~217)

3차 깊이 감사가 지목한 유일한 실행가능 갭. 요구사항은 결재제외자(녹스계정 없어 온라인 결재 불가)의 위반을
"확인서 징구 후 스캔해서 증빙으로 업로드"하는 별도 관리를 명시하나, 기존 구현은 위반자 본인 온라인 결재
경로(submitStatement→draftApproval)만 있어 결재제외 위반이 영구 징구중이었다.

| # | 상태 | 위치 | 내용 |
|---|:---:|------|------|
| VLEX-1 | ✅ | 데이터·액션 (v1.5.216) | Violation.exempt?(플랫) 추가. closeExempt 액션(BIZ_MGR/ADMIN) — 징구중 위반에 스캔 증빙 필수 업로드→완료+exempt, draftApproval 미경유(별도관리), 감사 기록. 위반 표에 업무담당자 스캔 업로드 폼('결재제외 완료(스캔)')·완료 시 결재제외 칩. export 처리구분 열. |
| VLEX-2 | ✅ | 테스트 (v1.5.217) | e2e sc_violation_exempt — 격리 데이터에서 등록(징구중)→스캔 업로드→결재제외 완료 검증, 결재함 확인서 결재 미생성 확인. 상태 셀만 검사(버튼 라벨 '결재제외 완료' 오탐 배제), 완료처리 제거 시 실패 대조. 게이트 e2e 86. |

**설계 결정(리뷰 요망): 스펙이 모델링을 못박지 않아 결재제외를 위반 단위(사람 속성 아님)로, 스캔 업로드가
결재를 대신(징구중→완료 직행)하도록 택했다. 코드베이스가 레코드 단위 상태관리이고, '별도 관리'가 결재 미경유를
함의한다는 해석. 릴리스노트에 명시해 리뷰 가능하게 남겼다.**

**교훈: '별도 관리' 류 대안 경로 요구는 기본 경로(결재)만 구현하면 부분 구현이다 — 대안 경로(스캔→완료)를
명시적으로 열되, 스펙이 모델링을 안 정하면 최소·규약정합 해석을 택하고 문서에 남겨 리뷰 가능하게 한다.
행 전체 텍스트 단언은 버튼 라벨('결재제외 완료')을 오탐하므로 상태 칩 셀만 검사하고, 완료처리 제거 시
실패를 대조 확인해 vacuous 테스트를 배제한다(이번에 실제로 오탐 테스트를 잡아 교정함).**

## SR 상태 라벨 단일원천 정합 (v1.5.218~219)

계정/권한 SR 커버리지 정찰 중 발견한 확정 표시 결함. srStatusLabel(chips.ts)은 "데이터·계정권한 SR 은 개발
단계가 없다 — '개발중'→'처리중' 표기"의 단일 원천이나, sr/ci '배정완료' 카드·sr/delayed 지연내역 두 화면이
raw {r.status} 를 렌더해 배정된 데이터·계정권한 SR 이 이 화면들에서만 '개발중'으로 오표기(대시보드·검색·
관리·신청내역은 헬퍼 사용 — 화면 간 불일치). 단일원천 헬퍼가 있는데 소비처 2곳이 누락된 전형적 drift.

| # | 상태 | 위치 | 내용 |
|---|:---:|------|------|
| SRLABEL-1 | ✅ | 수정 (v1.5.218) | sr/ci/page.tsx·sr/delayed/page.tsx 의 상태 칩을 {r.status}→{srStatusLabel(r)} 로 교체(import 추가). 전 SR 표시 화면이 상태 라벨을 단일 원천 공유. 시스템개발 SR '개발중' 불변(kind 분기), 저장·전이·칩색 무변경. |
| SRLABEL-2 | ✅ | 테스트 (v1.5.219) | e2e sc_sr_status_label — 격리 데이터의 계정/권한 SR(개발중·CI배정·기한경과)이 sr/ci 배정완료·sr/delayed 에서 '처리중'(개발중 아님) 표기 검증. 두 화면 모두 수정 전 raw 상태로 실패 대조. 무커버리지였던 계정/권한 SR 유형+srStatusLabel 표시 로직을 처음 검증. 게이트 e2e 87. |

**교훈: 단일원천 헬퍼(srStatusLabel)를 도입해도 소비처를 전수 교체 안 하면 일부 화면만 raw 값을 노출해
화면 간 불일치가 남는다 — 새 표시 헬퍼 도입 시 grep 으로 raw 필드 렌더({r.status}) 전수 확인. 무커버리지
유형(계정/권한 SR) 정찰이 공유 헬퍼의 미적용 소비처라는 실결함을 드러냈다 — '공유 코드라 재검증 불필요'가
틀렸음(공유 헬퍼도 미사용 소비처가 있으면 그 화면은 미검증·오표기).**

## SR 상태 라벨 export 정합 + lib 이관 (v1.5.220~221)

v1.5.219(화면 정합) 직후 같은 drift 클래스를 export 계층에 적용해 발견한 잔여 결함. SR 신청내역 export
(route.ts sr-requests)가 srStatusLabel 미적용 raw r.status 를 내보내, 데이터·계정권한 SR 이 화면에선 처리중인데
CSV(ISMS 산출물)에선 개발중으로 어긋났다(화면 6곳은 헬퍼 사용, export 만 누락).

| # | 상태 | 위치 | 내용 |
|---|:---:|------|------|
| SRLABEL-3 | ✅ | 이관 (v1.5.220) | srStatusLabel 을 app/(app)/sr/chips.ts → lib/sr.ts(SR 도메인 단일원천, delayedSrs 옆)로 이관. chips.ts 는 기존 소비처(../chips import 6곳) 위해 재수출 — 무중단. 화면·export 가 같은 단일원천 소비. |
| SRLABEL-4 | ✅ | export·테스트 (v1.5.221) | export route sr-requests 상태 열 r.status→srStatusLabel(r). sc_sr_status_label 확장(export CSV 도 처리중 표기 검증, raw 되돌리면 실패 대조). 게이트 e2e 87(시나리오 수 불변 — 기존 시나리오 확장). |

**교훈: 표시 단일원천 drift 점검은 화면에서 멈추지 말고 export 계층까지 훑는다 — 같은 raw 필드 렌더 결함이
CSV/감사 산출물에도 있으면 화면-산출물 불일치가 된다. 도메인 표시 규칙(srStatusLabel)은 page-dir(chips)이
아니라 lib 도메인 단일원천(lib/sr)에 두고 재수출로 무중단 이관 — 화면·API 가 같은 곳에서 규칙을 가져오게.**

## 인프라변경 이중 등록 방어 (v1.5.222~223)

등록 액션 이중제출 가드 정합성 점검 중 발견한 확정 결함. 서버액션 폼은 제출 후 자동 비활성화되지 않아 더블클릭
시 중복 생성되는 클래스를 codebase 는 createSr("중복 제출 방어")·receiveCiSr("이중 접수 방지")·부서서약 상신에서
가드하나, addInfraChange 만 누락 — 더블클릭 시 같은 제목 변경 2건이 각자 계획결재→결과결재 워크플로를 밟는
데이터 중복.

| # | 상태 | 위치 | 내용 |
|---|:---:|------|------|
| CHGDUP-1 | ✅ | 가드 (v1.5.222) | addInfraChange 에 dedup 가드 추가 — 같은 제목의 미상신(작업등록) 인프라 변경이 이미 있으면 무시(createSr·receiveCiSr 동일 패턴, 단일스레드 POST#1 삽입 후 POST#2 차단). addDevChange 는 이미 srNo 중복 가드 보유. |
| CHGDUP-2 | ✅ | 테스트 (v1.5.223) | sc_infra_rollback_plan 확장 — 같은 제목 재등록해도 목록 1건 유지 검증. 가드 제거 시 2건 생겨 실패 대조. 게이트 e2e 87(시나리오 수 불변). |

**교훈: '이중 제출 방어'는 등록/상신 액션 전반의 횡단 관심사다 — 새 등록 액션(addInfraChange)이 이 가드를
빠뜨리면 옆 액션(createSr)이 가드하는 클래스의 결함이 그 화면에만 남는다. 등록 액션 추가 시 sibling 의 dedup
가드 유무를 대조 확인한다. 순차 재등록 e2e(같은 제목 2회 → 1건)로 가드를 검증(진짜 동시성 없이도 재현).**

## 협력업체서약서 이중 징구 방어 (v1.5.224~225)

인프라변경 이중 등록 방어(CHGDUP) 후 워크플로 진입형 등록 액션 dedup 정합화 후속. addCompanyPledge 가 더블클릭
시 같은 업체·대상자 등록 건을 두 벌 생성 → 둘 다 결재상신 후보로 노출 → 같은 인원 결재 이중 생성. (addViolation·
addIncident·addReview 는 같은 주체 복수 건이 정당할 수 있어 제외 — 이중징구가 결코 정당하지 않은 협력업체 서약만.)

| # | 상태 | 위치 | 내용 |
|---|:---:|------|------|
| CPDUP-1 | ✅ | 가드 (v1.5.224) | addCompanyPledge 에 dedup 가드 — 같은 업체·대상자의 미상신(등록) 건이 이미 있으면 무시(addInfraChange·createSr 동일 패턴, 등록 상태 중복만 차단·상신 후 재등록 허용). |
| CPDUP-2 | ✅ | 테스트 (v1.5.225) | sc_company_pledge 확장 — 같은 업체·대상자 재등록해도 목록 1건 유지 검증. 가드 제거 시 2건 생겨 실패 대조. 게이트 e2e 87(시나리오 수 불변). |

**교훈: dedup 가드는 '중복이 결코 정당하지 않은' 워크플로 진입형 등록(협력업체서약·인프라변경·SR)에만 적용하고,
같은 주체의 복수 건이 정당한 등록(위반·장애·검토)에는 적용하지 않는다 — 무분별한 dedup 은 정당한 복수 등록을
차단한다. 가드 스코프는 미상신(초기) 상태로 좁혀 상신 후 재등록을 막지 않는다.**

## 점검계획 export 팀장 열 (v1.5.226~227)

세션 11개 신규 기능 적대 검토(서브에이전트, 확정 고·중 결함 0)가 지목한 저심각 갭. v1.5.209 에서 점검 진행내역
화면에 팀장 열을 더했으나 inspection-plans export 는 갱신 안 해 팀장이 ISMS 산출물(CSV)에서 누락 — SRLABEL
export(v1.5.221)와 같은 화면-export drift 클래스.

| # | 상태 | 위치 | 내용 |
|---|:---:|------|------|
| INSPTL-3 | ✅ | export (v1.5.226) | inspection-plans export 헤더에 '팀장'(점검자 다음), 행에 p.teamLead ?? '-' 추가. 화면과 동일 컬럼 구성 — 경과 알림 책임자 엑셀 감사 가능. |
| INSPTL-4 | ✅ | 테스트 (v1.5.227) | sc_inspection_teamlead 확장 — 점검계획 export CSV 도 팀장 열·값 검증. export 를 팀장 열 없이 되돌리면 실패 대조. 게이트 e2e 87(시나리오 수 불변). |

**교훈(재확인): 화면에 새 컬럼(팀장·기준액·원복계획 등)을 더할 때 대응 export 를 반드시 함께 갱신한다 —
화면-export drift 는 감사 산출물이 화면과 어긋나게 만든다. 적대 검토가 이 저심각 drift 1건을 잡았고 고·중
결함은 0 — 세션 신규 기능 11건 건전성 확인.**

## 보안성 검토 export 고위험 미조치 열 (v1.5.228~229)

화면-export drift 계열(SRLABEL·점검 팀장)을 export 전반으로 훑던 중 발견. 보안성 검토 화면은 검토별 고위험
(심각+높음) 미조치를 `고위험 {highSevOpen}` 칩으로 우선 신호화(v1.5.127)하나, security-reviews export 는 발견·
조치 총계만 담아 고위험 미조치 누락 — 발견 총계에서 등급 분해 복원 불가라 감사자가 CSV 만으로 심각/높음
미조치 잔여 검토를 식별 불가.

| # | 상태 | 위치 | 내용 |
|---|:---:|------|------|
| SVEXP-1 | ✅ | export (v1.5.228) | security-reviews export 에 '고위험 미조치' 열(조치 다음)·값 highSevOpen(r)(화면·대시보드·종합 export 와 같은 store 단일 원천). import 추가. |
| SVEXP-2 | ✅ | 테스트 (v1.5.229) | sc_security_review 확장 — 시드 SEC-2026-02(고위험 미조치 2)가 export CSV 에서 그 열·값 2로 나옴을 헤더 인덱스 파싱 검증. 열 없이 되돌리면 실패 대조. 게이트 e2e 87(시나리오 수 불변). |

**교훈: export drift 스윕은 '화면이 강조하는 신호'(우선 조치·고위험 미조치)가 감사 산출물에 있는지까지 본다 —
집계 총계만 있고 신호 컬럼이 빠지면, 총계에서 신호를 복원할 수 없을 때(발견→등급 분해 불가) 감사자가 CSV 로
우선순위를 못 본다. 화면 신호는 export 도 단일 원천 헬퍼(highSevOpen)로 동반 노출.**

## 협력업체 서약 징구 감사 기록 (v1.5.230~231)

감사 커버리지 정합성 점검 중 발견. v1.5.44 에서 addViolation(타인 위반 등록)이 감사 무기록·등록자 필드 부재로
추적 불가였던 것을 감사로 마감했는데, addCompanyPledge 는 같은 클래스(타 주체 파트너사 기록 등록, CompanyPledge
에 징구자 필드 없음)임에도 감사가 없었다. 협력업체 서약은 공유 워크스페이스에서 징구·상신되어 징구자≠상신자
가능한데 상신만 '결재 상신'으로 감사돼 최초 징구자 누락.

| # | 상태 | 위치 | 내용 |
|---|:---:|------|------|
| CPAUD-1 | ✅ | 감사 (v1.5.230) | AuditAction 에 '협력업체 서약 징구' 추가, ACTION_TONE(strict Record) 매핑. addCompanyPledge 에 audit(징구자, '협력업체 서약 징구', 번호·업체·대상자) — addViolation 과 동일 §VI 정책. |
| CPAUD-2 | ✅ | 테스트 (v1.5.231) | sc_company_pledge 확장 — 징구 등록 후 설정>감사에 '협력업체 서약 징구'+업체명 기록 검증. 감사 호출 제거 시 실패 대조. 게이트 e2e 87(시나리오 수 불변). |

**교훈: 관리 행위가 '타 주체 기록을 등록'하고 그 레코드에 행위자 필드가 없으면 감사 로그가 유일 추적점이다
(§VI 이력추적성) — 등록/징구 액션에 audit 유무를 sibling(addViolation) 대조. AuditAction 추가 시 strict
Record<AuditAction> 톤맵도 컴파일러가 강제하니 함께 갱신.**

## 보안담당자 지정 감사 기록 (v1.5.232~233)

협력업체 서약 징구 감사(CPAUD) 후 관리 액션 감사 커버리지 체계 점검에서 발견. 설정/관리 액션 대부분 감사되나
(결재선·메뉴권한·재택 대상자·공통코드 변경), toggleOfficer(보안담당자 지정/해제)만 감사 부재. 보안담당자는
특별서약 대상·보안 관리 책임 거버넌스 역할인데 securityOfficers(이름 배열)에 지정자 필드 없어 역할 배정 이력
전무. (settings/menus uploadSpec은 스펙 문서 첨부라 비대상 확인.)

| # | 상태 | 위치 | 내용 |
|---|:---:|------|------|
| OFAUD-1 | ✅ | 감사 (v1.5.232) | AuditAction '보안담당자 지정' 추가·ACTION_TONE 매핑. toggleOfficer 에 행위자 포착 + audit(행위자, '보안담당자 지정', 대상·지정/해제). 재택 대상자 변경과 동일 §VI 정책. |
| OFAUD-2 | ✅ | 테스트 (v1.5.233) | e2e sc_officer_audit(신규) — 김현우 보안담당자 지정→감사 기록 검증(후 해제 원복). 감사 호출 제거 시 실패 대조. 게이트 e2e 88. |

**교훈: 관리 액션 감사 커버리지는 '거버넌스 역할 배정'(보안담당자·재택 대상자 등)까지 본다 — 역할을 이름
배열로만 관리하면 행위자 이력이 감사에만 남는다. 관리 액션 sweep 은 등록뿐 아니라 역할 토글도 포함. 두
연속 발견(협력업체 징구·보안담당자)으로 관리 감사 계열 정합 완료.**

## 정산품의 결재대기 이중 상신 방지 (v1.5.234~235)

상태전이 가드 스윕(장애 조치완료 가드 v1.0.30 계열, 전역 `'use server'` 액션 33파일 대조)에서 발견. 상태전이·
컬렉션 삽입·카운터 증가가 모두 촘촘히 가드됐으나, requestSettlement(정산품의 상신)만 결재대기 dedup 부재.
비-임시저장 상신은 정산(결재중)+결재선(draftApproval)을 만드는데, 더블클릭·재전송으로 같은 (계약·항목·금액)
결재중 정산이 2건 서면 서면 결재자가 둘 다 승인 시 지급완료가 이중 반영돼 집행액이 겹계상(집행률·계획대비실적·
속보 기준금액 왜곡). 이미 도입한 addCompanyPledge(CPAUD 계열)·addInfraChange(CHGDUP 계열)의 '대기 상태 중복
방지'와 같은 클래스이며, 결재를 생성해 재무 집계에 전파되는 가장 고위험 create.

| # | 상태 | 위치 | 내용 |
|---|:---:|------|------|
| SETDUP-1 | ✅ | 가드 (v1.5.234) | invest·expense requestSettlement 에 비-임시저장 상신 시 같은 (contractId,item,amount) 정산이 이미 결재중이면 무시하는 가드. 종결(지급완료·반려) 후 신규 정산은 정상 진행 — 순차·반복 정당 다건 무영향. |
| SETDUP-2 | ✅ | 테스트 (v1.5.235) | e2e sc_settle_dedup(신규, 격리 settlements 비움) — 동일 정산 재상신 시 ST-0002 미생성 검증. 가드 제거·재빌드 시 ST-0002 생성으로 실패 대조(fails-without-fix). 게이트 e2e 89. |

**교훈: 상태전이 가드 스윕은 '결재를 생성하는 create'(정산품의·서약 징구·인프라 변경)의 대기중 중복까지 본다 —
add/create 라도 그 결과가 결재→집계 전파되면 더블클릭 이중 승인이 집계를 겹계상한다. dedup 필요 판정은
sibling create(addPlan 은 정당 다건이라 dedup 없음) 대비가 아니라 '결과가 대기 상태로 남아 재무·상태 집계에
전파되는가'로 한다. 임시저장(전파 없음)은 제외, 결재대기만 차단.**

## 출력물 미등록 알림 부서장 통지 (v1.5.236~237)

스펙-대-구현 깊이 감사(요구사항 sample.xlsx 행 정밀 대조)에서 발견. 64행(점검계획) "담당자, 팀장에 경과 항목
알림"은 v1.5.209(INSPTL)에서 담당자+팀장 이중 통지로 구현됐으나, 같은 이중-수신자 클래스인 56행(출력물폐기
현황) "주기적으로 안내메일(부서장, 출력자)"은 출력자 단일 통지에 머물러 있었다. 부서장은 부서원 폐기자료를
취합·결재신청하는 책임자라, 부서에 미등록 출력물이 있으면 통지받아야 안내 루프가 닫힌다. Person 에 역할이
없어 부서장은 ACCOUNTS(DEPT_MGR·부서 일치)로 해석한다.

| # | 상태 | 위치 | 내용 |
|---|:---:|------|------|
| PRDEPT-1 | ✅ | 알림 (v1.5.236) | notify '출력물 미등록' 수신자를 출력자 ∪ 미등록 부서의 부서장(ACCOUNTS DEPT_MGR)으로 확장, 재직 교집합·Set 중복제거. ACCOUNTS import 추가. 점검 경과 팀장 통지와 동일 이중-수신자 정책. |
| PRDEPT-2 | ✅ | 테스트 (v1.5.237) | e2e sc_printout_deptlead(신규, 격리) — 경영지원팀 정민서 미등록 출력물 1건에서 배치 '출력물 미등록 2명'(출력자+부서장 이수진) 검증. 부서장 통지 제거 시 '1명'으로 실패 대조. 게이트 e2e 90. |

**교훈: 스펙-대-구현 깊이 감사는 요구사항 시트의 '수신자 명시'(부서장·팀장 등 복수 역할)를 구현 수신자 목록과
행 단위로 대조한다 — 한 이중-수신자 요구(점검 담당자+팀장)를 구현했어도 같은 클래스의 다른 행(출력물
부서장+출력자)이 단일 수신에 머물 수 있다. 역할이 Person 에 없으면 ACCOUNTS 로 해석(부서 일치), 재직 교집합·
부재 폴백(해당 역할 없으면 단일 통지)은 기존 알림 정책과 동일하게 유지.**
