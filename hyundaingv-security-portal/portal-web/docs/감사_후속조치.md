# 적대 감사 후속조치 (미조치 잔여 항목)

v1.5.x 에서 9개 핵심 서브시스템(결재·영속화·SR/변경·컴플라이언스·재무·보안의식·인프라·
게시판/설정, 그리고 직전 델타)을 적대적으로 감사해 확정 결함을 전수 수정했다. 아래는 감사가
찾은 저심각·잠재 항목의 처리 현황이다. 전부 상태 불일치·데이터 손상이 아니라 알림/가시성/
정합성 성격이며, portal-web 은 본 트랙 단독 소유다(병렬 v1.24x~ 트랙은 별개 프로젝트
`seekerslab-itam/itam-web` 소속으로 portal-web 파일을 건드리지 않는다).

**#1~#5 전부 조치 완료(v1.5.31·v1.5.33·v1.5.32·v1.5.35·v1.5.36).** #2 타임스탬프 승격은
v1.5.24 프로젝트 과다계수 회귀 테스트까지 가능케 해 실증했고(v1.5.34), #4 는 비-일반 양식
개정 재서약 통지를 일반과 동일하게 확장했다. #5 는 개인 폐기 상신이 부서 취합('취합') 할일을
닫지 않도록 방어(휴면이나 실데이터 대비). **적대 감사 9종 + 후속조치 5종 전부 종결.**

| # | 상태 | 위치 | 내용 | 심각도 | 비고 / 권장 조치 |
|---|:---:|------|------|:---:|-----------|
| 1 | ✅ v1.5.31 | `work/approvals/actions.ts` | SR 신청 승인이 `'SR 처리'` 할일을 안 만들어 `assignCi` 마감 루프가 반쪽이었음. | 저 | 승인 시 업무담당(BIZ_MGR)에게 할일 생성(시드 TD-104 형태), assignCi 가 마감. e2e sr 회귀테스트 추가. |
| 2 | ✅ v1.5.33 | 서약 상태 판정 전 화면 | 서약 유효 판정 `signedAt >= revisedAt` 가 날짜 단위라 같은 날 개정 전 서명이 유효로 남던 문제. | 저~중 | `nowStampSec()`(초 KST)로 서약·개정 시각 타임스탬프 승격, 개정=현재시각 확정. 표시는 date. e2e revision 재현(당일 서명→개정→무효화 8명) + 프로젝트 과다계수 회귀(v1.5.34) 실증. |
| 3 | ✅ v1.5.32 | `dashboard`·`work/approvals`·`notify` | 일반 서약 집계 3곳에 `year` 필터 누락(다른 뷰는 있음) — 레거시 데이터 파일에서만 갈리는 잠재 불일치. | 저(latent) | 세 곳에 `p.year === '2026'` 추가해 전 뷰 정합. |
| 4 | ✅ v1.5.35 | `pledge/manage`·`pledge/my`·`notify` | 비-일반 양식(관리책임자·재택·특별·프로젝트) 개정이 서명 무효화만 하고 재서약 할일·안내·리마인더가 없던 문제. | 저 | 사용자 결정(기본값)으로 일반과 동일하게 확장: reSignTargets 로 유형별 의무자(관리책임자=비-USER 계정·재택=당월 대상자·특별=보안담당자·프로젝트=진행중 참여인력) 산정, 개정 시 유형 명시 재서약 할일+안내메일, notify 일배치 리마인더, 서명 시 **유형별** 마감(교차마감 방지). e2e pledge_multikind 로 교차마감 방지 실증. |
| 5 | ✅ v1.5.36 | `awareness/prints` (`submitDiscards`) | 개인 폐기 제출이 부서 취합 할일(시드 TD-103)까지 닫던 문제. | 매우 저(휴면) | 개인 상신 마감에서 부서 취합('취합' 포함 제목) 할일을 제외해 조기 마감 방지. printouts 시드가 비어 게이트 미실행(early return)이라 무회귀·코드 검토 검증. 개인 폐기 전용 todo 모델은 데이터 경로 구축 시 별도 확장. |

> 참고: 확정 결함(수정 완료)과 감사 범위는 `docs/구축_요약.md`·커밋 이력(v1.5.14~30) 참조.
> 재사용 프레임워크 온보딩은 `docs/어댑터_연동_가이드.md`·`docs/배포_운영_가이드.md`.

## 결재 엔진 최대정밀 재감사 (v1.5.46~47)

5개 적대 렌즈(다단 큐·전파·재상신/재번들·회수·인가/멱등) 발견 → 렌즈별 반증 검증(13 에이전트)으로
확정 8건. 상태 손상·인가 우회·스톨은 없음(전 전파 블록 상태가드 정확, 회수는 forward 의 정확한
역전이, 자기결재 대체·큐 전진 건전). 확정분은 이력/할일 매칭 정밀성과 표현/추적성.

**수정 완료:**

| # | 상태 | 위치 | 내용 | 심각도 |
|---|:---:|------|------|:---:|
| A1 | ✅ v1.5.47 | `work/approvals/page.tsx` | 비회전 '이전 회차' 이력이 `ref` 만으로 묶여, 같은 SR 번호를 ref 로 공유하는 SR 신청↔적용요청·변경계획↔변경결과가 교차 노출(적용요청 결재자=시스템관리자가 SR 신청 반려사유 열람 = 비당사자 유출). `docType` 조건 추가. | 중(접근통제) |
| A2 | ✅ v1.5.47 | `lib/approvals.ts` | 재상신 할일 닫기가 `title.includes(ref)` 부분문자열이라 (a) 반려 사유에 인용된 타 건 ref 로 무관 할일 오마감, (b) 같은 ref 공유 문서 교차마감. 유형+ref 선두 고정(`startsWith`) 매칭. e2e `reject_reason_collision` 회귀(stash 실측 1 vs 0). | 중 |
| A3 | ✅ v1.5.47 | `work/approvals/actions.ts` | ref 없는 결재(시드 AP-2026-0709) 반려 시 ap.id 로 키된 재상신 할일이 닫기키(엔티티 ref)와 달라 영구 미마감·방치알림. ref 있을 때만 재상신 할일 생성. | 저 |

**미조치(저심각·데이터모델/표현 성격, 실데이터·시연 무해):**

| # | 위치 | 내용 | 심각도 | 권장 조치 |
|---|------|------|:---:|-----------|
| B1 | `work/approvals/page.tsx`·`actions.ts` | 다단 중간 결재자가 승인 후 자기 결재 UI 흔적을 전부 잃음(inbox·상세 접근이 현재 approver 스칼라 기준, 할일 마감). 감사 로그에는 남음. 점검결과 상신(유일 다단)만 해당. | 저(추적성) | Approval 에 결재자 이력 배열 추가 후 inbox/상세를 '과거 결재자 포함'으로 확장(데이터모델 변경). |
| B2 | `actions.ts`(반려 전파)·`page.tsx`(refSummary) | 묶음(장애·출력물·서약) 반려가 역링크(reportRef/approvalRef)를 지워, 반려된 과거 결재 상세가 'N건'→0건으로 빈다(제목엔 N건). | 저(표현/감사완결) | 상신 시 묶음 건수/스냅샷을 결재에 저장해 상세가 역링크 없이도 재구성. |
| B3 | `page.tsx`(회전 이력) | 회전 이력이 같은 유형·기안자의 모든 과거 배치를 포집해, 무관한 월별 배치가 '이전 회차'로 표기(동일 당사자, 유출 없음). | 저(표현) | 헤더 문구를 '이전 배치'로 명확화하거나 회차 계보 링크 도입. |

> 재감사 결론: 결재 엔진의 상태·인가·전파 코어는 건전하며, 확정분은 문자열 매칭 정밀성(A1~A3, 수정)과
> 다단/묶음의 표현·추적성(B1~B3, 저심각 보고)에 국한된다. B1~B3 는 데이터모델·표현 변경이라 실고객
> 결재 UX 피드백 시 함께 반영 권장.

## 영속화/스토어 엔진 최대정밀 재감사 (v1.5.49)

5개 적대 렌즈(로드 머지·저장 내구성·라이프사이클·싱글턴/캡·참조 무결성) 발견 13건 → 반증 검증(14
에이전트)으로 확정 3건, 반증 6건. 코어(디바운스 저장·SIGTERM 플러시·백업 순서·로그 3종 캡)는 건전
재확인. 확정분은 손상 파일 처리(손실·요소/중첩 검증).

**수정 완료(전부 v1.5.49):**

| # | 상태 | 위치 | 내용 | 심각도 |
|---|:---:|------|------|:---:|
| D1 | ✅ | `lib/store.ts loadFromFile catch` | 파싱 불가 DATA_FILE → seed 폴백 → 읽기 트리거 저장(scheduleSave)이 300ms 뒤 손상 원본을 빈 시드로 덮어써 복구 불가 손실(스케줄러 off 배포는 .bak 도 없어 전손). catch 에서 원본을 `.corrupt.<시각>` 격리 보존 후 seed 기동. e2e emptystate quarantine 프로브. | 높음 |
| D2 | ✅ | `lib/store.ts loadFromFile` | v1.5.14 타입 안전 머지가 상위 타입만 봐, 요소가 객체여도 시드에서 배열/숫자인 중첩 필드(codeGroups[].values·systems[].serverIds·investPlan[].amount)가 어긋나면 소비처 `.length/.filter/.toLocaleString` 가 전 화면 500. 요소 필터(원시값 제거) + 시드 형판 기준 배열→[]·숫자→0 정규화. corruptfile 프로브에 values 누락 그룹. | 높음 |
| D3 | ✅ | `lib/store.ts nextNo` | id 없는 요소가 남으면 existing 에 undefined 섞여 `undefined.startsWith` 가 상신 서버액션 500. `typeof n === 'string'` 가드. | 중 |

**반증(비결함, 참고):** null 요소 크래시 — 요소 필터가 제거(반증); 배치 ref 교차링크 — incidents.reportRef 와
approvals 는 원자 공동저장이라 부분 파일에서 갈리지 않음(반증); dangling ref — 의도된 per-key graceful
degradation, 소비처가 .find/.filter 로 미스 허용(반증); 외부 파일 삭제 — 스케줄러 틱의 audit 뮤테이션+백업
saveNow 로 자가치유(반증, existsSync 가드는 값싼 하드닝 여지); SIGTERM drain 선점 — 뮤테이션이 await 전
선적용돼 플러시가 보전(반증, store 의 SIGTERM 핸들러는 Next 가 이미 처리해 중복); 업무 컬렉션 무한증가 —
로그(cap)와 달리 승인/할일/첨부는 system-of-record, 캡이 되레 감사추적 손실(반증). **워크플로 반증 패스가
진행중이던 내 요소필터 수정을 검증하고 부족분(중첩 필드)을 정확히 지목 — 선제수정+반증검증의 이상적 협업.**

## SR/변경 전파 엔진 최대정밀 재감사 (v1.5.50)

5개 적대 렌즈(SR 상태전이·변경 2단·교차 전파·동시성/멱등·직접접수/할일) 발견 9건 → 반증 검증(14
에이전트)으로 확정 2건, 반증 7건. 코어(상태전이·kind 분기·SR 완료 전파·2단 변경·큐 전진)는 건전 재확인.

**수정 완료(전부 v1.5.50):**

| # | 상태 | 위치 | 내용 | 심각도 |
|---|:---:|------|------|:---:|
| E1 | ✅ | `infra/changes submitPlan/submitResult` | 변경은 공유 워크스페이스(담당·Admin)라 재상신자가 원 기안자와 다를 수 있는데, draftApproval 의 기안자-매칭 닫기에만 의존해 타 담당 재상신 시 원 기안자 재상신 할일이 고아 + 방치 알림 무한 반복. `closeChangeResignTodo`(유형+변경번호, 소유자 무관)로 닫음 — SR 재상신(requester 바인딩)과 동등. e2e change_resign_orphan. | 중 |
| E2 | ✅ | `sr/new createSr` | 멱등 가드 부재 — 상신 폼 이중 클릭이 SR·결재·할일을 두 벌 생성(타 전이는 상태 게이트). 같은 신청자·시스템·제목의 결재중 SR 존재 시 중복 무시. | 중 |
| E0 | ✅ | `sr/ci assignCi/baReject` | (선제) 'SR 처리' 할일 누수 2경로: assignCi 가 owner==me.name 로 닫아 ADMIN 배정 시 방치 + baReject 미닫음. 유형+SR번호로 양 경로 닫기. **워크플로 반증 패스가 이 수정을 확인(고쳐진 코드 읽고 finding 반증).** | 저 |

**반증(비결함, 참고):** SR 상태전이 스톡 — 각 상태에 유효 액션 존재(반증); SR 완료 전파 조건(sr.status==='적용요청')
— 변경 사슬 동안 SR 동결로 정확(반증); 2단 변경 중복 상신 — 상태 가드(작업등록/작업등록승인)로 재상신 차단(반증);
직접접수 CI(receiveCiSr) — srRequests 와 격리(반증); withdraw 역전이 — 정확한 forward 역(반증); advance 이중
호출 — nextOf(테스트)=undefined 안전(반증); addDevChange 중복 링크 — srNo dedup(반증). **E0(내 선제수정)를
워크플로가 읽고 반증 — 3연속 재감사(알림·영속화·SR/변경)에서 선제수정+반증검증 협업 패턴 확립.**

## 알림/스케줄러 엔진 최대정밀 재감사 (v1.5.48)

5개 적대 렌즈(타깃팅·스케줄러 동시성·멱등·실패 관측성·기록/백업) 발견 17건 → 반증 검증(14 에이전트)으로
확정 3건, 반증 14건. 코어(어댑터 예외/타임아웃 내성 v1.5.16~17, batch0 dedup, SR지연 CI 스코프, 채널
중지 실패기록)는 건전 재확인. 확정분은 연도 리터럴·백업 관측성/커버리지.

**수정 완료(전부 v1.5.48):**

| # | 상태 | 위치 | 내용 | 심각도 |
|---|:---:|------|------|:---:|
| C1 | ✅ | `lib/notify.ts` 외 6곳 | 미서약 안내가 `year === '2026'` 리터럴로 굳어(다른 날짜는 today() 파생), 연도 전환 후 연차 서약 리마인더가 조용히 멈춤(2027 이후 미서약자 미통지). `currentYear()`(today 파생) 헬퍼로 notify·dashboard·pledge/{dept,manage,my}·export 통일. 2026 불변. | 높음 |
| C2 | ✅ | `lib/store.ts backupDataFile` | 디스크 오류에도 null 반환(스킵과 동일)이라 틱이 성공만 기록 → 지속 백업 실패가 배치 이력·로그에 안 보임(광고된 '복구 지점' 부재를 운영자가 모름). catch 에서 '실패' 배치 기록. | 중 |
| C3 | ✅ | `platform/integrations`·`store.ts` | backupDataFile 이 스케줄러 틱에서만 호출 → 외부 스케줄러 사용(NOTIFY_INTERVAL 미설정)+영속화 켬 배포에선 스냅샷 전무. 수동 '알림 배치 실행'도 백업 남기게 배선. e2e `manual_backup` 회귀. | 중 |
| C0 | ✅ | `instrumentation.ts` | (선제) async tick 이 인터벌보다 길 때 setInterval 이 다음 tick 을 발화해 겹침 → 중복 발송·동시 뮤테이션. `__ngvNotifyTicking` 재진입 가드. 워크플로 반증 패스가 이 가드 존재를 확인. | 저(방어) |

**반증(비결함, 참고):** batch0 성공 마스킹 — batch0 은 자체 실패를 배치 이력에 기록(반증); SR지연이 '적용요청결재중'
누락 — 제외 축이 CI 배정 유무라 정상, 제외하면 되레 지연 알림 소실(반증); 수동+스케줄 동시 이중발송 — 목 어댑터가
마이크로태스크로 직렬화돼 재현 불가(실 async 어댑터 배포 시 저심각 잠재만, 수동 경로는 __ngvNotifyTicking 미공유가
잠재 갭). 매 틱 재발송은 의도된 일일 리마인더(반증). year 리터럴은 앱 전역 데모 상수였으나 notify 의 무관측 특성이 높음.

## 컴플라이언스/ISMS 엔진 최대정밀 재감사 (v1.5.51)

5개 적대 렌즈(교육 이수율·점검 생애·ISMS 기준 CRUD·인가/검증·컴플라이언스 정합) 발견 12건 → 반증
검증(14 에이전트)으로 확정 2건(교육 target 무시는 5 finder 중복), 반증 다수. 점검(2단 결재·상태전이·
referential 무결성·CSV 검증)과 이수율 재직자 스코프(v1.5.24)는 건전 재확인.

**수정 완료(전부 v1.5.51):**

| # | 상태 | 위치 | 내용 | 심각도 |
|---|:---:|------|------|:---:|
| F1 | ✅ | `compliance/education`·`store`·`export` | 이수율이 과정 대상(전임직원/개발자/보안담당자)을 무시하고 완료 과정 전부를 전 재직자에게 요구 → 개발자 전용 과정을 개발자가 100% 이수해도 비개발자 6명이 '미이수', 전사 이수율 88%→56% 급락(감사 지표 오표기 + 개인 허위 비준수). `eligibleForCourse`(개발자=부서 '개발', 보안담당자=securityOfficers, 그 외=전 재직자) 단일 원천으로 이수율·개인상태·미이수 명단·N/M·USER 화면·엑셀 전부 대상 스코프. e2e education_target. | 높음 |
| F2 | ✅ | `compliance/education registerAttendees` | '보안교육' 할일을 유형만으로 닫아, 다른 과정 이수 시 무관한 과정의 교육 의무 할일이 닫혀 실제 미이수가 대시보드에서 숨음(할일-이수 괴리). 할일 제목의 과정명으로 좁혀 그 과정 할일만 닫음. | 중 |
| F0 | ✅ | `compliance/education` (과정별 이수 인원 N/M) | (선제) recordsOf(c.id).length 가 퇴사자 기록까지 세어 '이수 인원 > 대상'(6/5) 표기 → 재직자+대상 스코프로 통합. **워크플로 반증 패스가 이 수정을 확인(고쳐진 코드 읽고 orphan finding 반증).** | 저 |

**반증(비결함, 참고):** 과정 완료=첫 기록 시 확정(의도된 '세션 개최' 의미); inspection 상태전이·2단 점검결과 전파
(완료는 최종 승인만)·referential(deleteItem 이 plan 참조 시 삭제 차단)·CSV 무효 주기 건너뜀·addPlan inspector 검증;
addCourse/registerAttendees 무감사(개인 submission 아닌 관리행위지만 도메인 레코드가 추적 — 감사정책 §5 경계).
**F0(내 선제수정)를 워크플로가 읽고 반증 — 5연속 재감사에서 선제수정+반증검증 협업 패턴 일관 확립.**

## 재무/자금 엔진 최대강도 재감사 (v1.5.52)

5개 적대 렌즈(집행률·계약/투자·속보 수정·인가/스코프·감사 정합) → 반증 검증. 자금 코어(집행률 확정
스코프 v1.5.25·크로스-kind export v1.5.25·연도 필터)는 건전 재확인, 표시·감사 정합 2건만 확정. optimizePlan
가설은 상태 가드로 자기 반증.

**수정 완료(전부 v1.5.52):**

| # | 상태 | 위치 | 내용 | 심각도 |
|---|:---:|------|------|:---:|
| M1 | ✅ | `finance/invest` | 계약 체결 stat 의 건수 note 가 종류(자본/비용) 무관하게 전체 `investContracts` 를 세어, 자본 화면에 비용 계약까지 합산된 교차집계 누수 → 해당 종류 `kindContracts` 건수만 표기(비용 화면은 이미 정상). | 저 |
| M2 | ✅ | `finance/expense`·`lib/audit` | 비용 속보 예상액 수정 감사가 '공통코드 변경' 액션으로 오기록 → 신설 controlled-vocabulary '재무 속보 수정' 으로 정정. | 저 |

**반증(비결함):** 집행률 확정 스코프·크로스-kind export·연도 필터·optimizePlan(상태 가드) 모두 건전.

## 임직원 의식제고 엔진 최대강도 재감사 (v1.5.53)

5개 적대 렌즈(재택 명단 생애·출력물 폐기 폐쇄루프·서약/교육 연계·인가/스코프·집계 정합) → 반증 검증.
재택 명단 경계월 이중 집계(중)와 부서 취합 할일 고아(저) 확정, 내 선제 '역순 등록 거부' 수정은 워크플로가
읽고 반증(확인). importDaily 중복·서약 시각정밀은 반증.

**수정 완료(전부 v1.5.53):**

| # | 상태 | 위치 | 내용 | 심각도 |
|---|:---:|------|------|:---:|
| A1 | ✅ | `awareness/remote`·`export` | 한 사람이 인접한 두 재택 기간(경계월 전환)으로 당월 명단·통계·엑셀에 둘로 잡히던 이중 집계 → (a) 역순/열린 기간 중복 등록 거부, (b) 구 기간을 신규 시작 **전일**로 마감(off-by-one: 당일 마감 시 그 달에 구·신 함께 걸림), (c) 화면·export 이름 기준 중복 제거. e2e remote_overlap. | 중 |
| A2 | ✅ | `awareness/prints` | 부서 출력물 폐기 '취합' 할일(TD-103)이 개인 폐기 상신으로는 닫히지 않아(v1.5.36 조기마감 차단) 닫을 경로 없이 영구 방치(기한 경과) → 부서담당 '부서 취합 완료' 버튼으로 명시적 마감 경로 추가. | 저 |

**반증(비결함):** 역순 두-열린-기간(선제수정이 처리 → 반증으로 확인); importDaily 일배치 중복 방지; 서약 당일
개정 시각정밀(v1.5.33). **6·7연속 재감사에서 선제수정+반증검증 협업 패턴 유지.**

## 권한/접근통제 엔진 최대강도 재감사 (v1.5.54)

5개 적대 렌즈(메뉴권한 write·서버액션 게이트 우회·nav-gate 발산·effectiveRoles 엣지·신원 게이트) → 반증
검증(7 에이전트). 코어 전량 건전 재확인: effectiveRoles 페일클로즈(미등록 href→[]→리다이렉트)·권한 상승/
ADMIN 잠금 원천 차단·requireMenuRole 교집합·nav-gate 락스텝(hiddenHrefs=effectiveRoles 파생)·결재 승인/
회수 신원 게이트(approver/drafter===me). 확정 0, PARTIAL 1(수정), 반증 1.

**수정 완료(v1.5.54):**

| # | 상태 | 위치 | 내용 | 심각도 |
|---|:---:|------|------|:---:|
| G1 | ✅ | `work/approvals/page.tsx` | 결재 상세 '이전 회차 이력'이 회전 문서(장애보고·출력물폐기·서약 현황·부서서약 현황)에서 유형+기안자만으로 매칭 → `selected` 는 신원 게이트(결재자/기안자 본인)되나 이력 쿼리는 재적용 안 함. 결재선 변경·다단 회부로 결재자가 바뀐 뒤엔 내가 관여 안 한 같은 기안자의 별개 묶음 반려사유·처리이력 노출. 이력 행에도 `approver===me||drafter===me` 재적용(기안자 조회는 본인 전 회차 유지). e2e approval_history_identity. **PARTIAL 판정**(결재자는 (기안자,유형)당 결정적이라 안정 구성에선 미교차, 결재선 재구성·큐 진행 시 도달 — 반증 패스가 정확히 지목, 권고 수정과 일치). | 저 |

**반증(비결함):** nav 가시성 이중 파생(AppShell.roles+hiddenHrefs vs 게이트 effectiveRoles) — 현재 수학적
등가(effectiveRoles⊆item.roles)이고 nav 는 서버 가드 아래 비하중(cosmetic)이라 접근 경계 불변. 유지보수 노트.
**교훈: 확정 0건이어도 PARTIAL(조건부 도달) 방어심화는 반증 패스의 권고와 일치하면 수정 가치 — 신원 게이트는
selected 뿐 아니라 파생 조회(history·첨부·요약)에도 재적용해야 일관.**

## 대시보드/집계 엔진 최대강도 재감사 (v1.5.55)

5개 적대 렌즈(집계 단일원천·스코프 누출·수치 정합·엣지/빈값·신선도) → 반증 검증(10 에이전트). 개인/전사
스냅샷 스코핑(canManage 게이트)·배지(todos·approvals) 정합·지연 SR 경계는 건전 재확인. 확정 2(수정)·PARTIAL 1·반증 3.

**수정 완료(전부 v1.5.55):**

| # | 상태 | 위치 | 내용 | 심각도 |
|---|:---:|------|------|:---:|
| D1 | ✅ | `dashboard/page.tsx` (myEduMissing) | '보안교육 미이수' 포틀릿이 과정 대상(eligibleForCourse)을 무시하고 완료 과정 전부를 세어 → 개발자 전용 과정이 비개발자 대시보드에 '미이수'로 오표기(v1.5.51 F1 클래스가 대시보드에 잔존). 교육 화면과 동일한 대상 스코프 단일 원천 적용. e2e dashboard_edu_scope. (선제 발견·수정, 반증 패스가 확인) | 중 |
| D2 | ✅ | `dashboard/page.tsx` (pledgeTodo) | '일반 서약' 타일이 kind '보안서약서' 할일 유무로 판정 — 이 kind 는 관리책임자·재택·특별·프로젝트 재서약이 공유(제목만 상이)라, 무관한 유형 개정 시 일반 서약 유효자가 '미제출'로 오표기되고 일반 서약 제출로는 안 닫혀(sign 은 '일반' 제목 할일만·유효 시 early return) 방치. 일반 서약 유효성(validSign) 단일 원천으로 판정(전사 미서약 집계와 정합). e2e dashboard_pledge_general. **워크플로 반증 패스가 확정(내 독립 패스는 놓친 finding — 워크플로가 보강).** | 중 |

**PARTIAL(무해):** 공지 정렬 `Number(pinned)` 이 손상 파일의 문자열 pinned("true")에 NaN → 정렬 미이동(크래시·
오수치 없음, 순서만). 타입안전 머지는 크래시 유발 필드(배열/숫자)만 정규화하는 의도적 범위라 불리언은 밖. 미조치.
**반증(비결함):** '점검 미등록·경과' 합성 지표는 라벨이 '미등록'을 포함해 전월 경과+당월 결과미등록을 함께
세는 의도된 집계(전용 화면 '기한 경과' 순수 overdue 와 다른 지표); 지연 SR strict-< 경계는 전용 화면과 동일;
미래월 결과미등록은 도달성 낮음. **교훈: 대시보드 집계는 도메인 화면의 단일 원천 헬퍼(eligibleForCourse·
validSign)를 반드시 재사용 — 재유도하면 원천이 갱신돼도 대시보드만 옛 규칙으로 남아 지표가 갈린다.**

## 통합 검색 엔진 최대강도 재감사 (v1.5.56)

5개 적대 렌즈(스코프 정합·필드 누출·쿼리 처리·결과 정합·엣지) → 반증 검증(16 에이전트). 확정 6·부분 5·반증 0.

**수정 완료(전부 v1.5.56):**

| # | 상태 | 위치 | 내용 | 심각도 |
|---|:---:|------|------|:---:|
| S1 | ✅ | `search/page.tsx` (도메인 게이트) | 검색이 도메인 노출을 me.role 리터럴(canManage·canViewFinance)로 판정 → 화면 가드는 requireMenu→effectiveRoles 라 런타임 메뉴권한(menuOverrides) 제한을 반영하는데 검색은 무시. ADMIN 이 특정 화면을 역할에서 제한해도 그 역할이 검색으로 SR·계약·장애·변경·프로젝트·게시판 데이터 열람(요구 72행 통제 우회). 각 도메인을 소속 화면 href 의 effectiveRoles 로 게이트(재무는 투자/비용 종류별 화면 분리 + canViewFinance 유지). e2e search_menu_override. (반증 패스가 5도메인 전수 확정) | 높음 |
| S2 | ✅ | `search/page.tsx` (has 매처) | `f?.includes(query)` 가 손상 파일의 비문자열 필드(숫자)에 TypeError 500 → `String(f ?? '').includes` 로 방어. | 중 |

**부분(무해/UX):** 과대 부분매칭(ID·연도 substring)·대소문자 구분 zero-result·React key 접두 관례 의존·무한 렌더
(페이지네이션 없음) — 저심각 UX/성능 노트. **교훈: 검색·리포트 등 집계 화면은 반드시 effectiveRoles(런타임 권한)로
게이트 — 정적 menus.ts 기본 역할 비교(내 초기 독립 패스가 이걸로 오판)로는 menuOverrides 런타임 제한을 못 잡는다.**

## 첨부/파일 업로드 엔진 최대강도 재감사 (v1.5.57)

5개 적대 렌즈(refId 인가·가시성 스코프·파일 검증·집계/dedup·엣지) → 반증 검증(23 에이전트). 보안 코어 건전
재확인: refId 는 호출부가 자기 신규 id 또는 검증된 기존 레코드(+역할 게이트)로만 전달(교차문서 오염 없음);
파일은 크기 상한·경로 정제·메타데이터 전용(디스크 미기록→트래버설·실행 없음)·CSV 미유출·React 이스케이프.
확정 3(집계/dedup)·부분 다수·반증 다수.

**수정 완료(v1.5.57):**

| # | 상태 | 위치 | 내용 | 심각도 |
|---|:---:|------|------|:---:|
| T1 | ✅ | `lib/attachments.ts` registerGenerated | 자동첨부 dedup 키가 파일명에 today() 를 포함해 당일만 방어 → 변경 계획/결과(안정 refId=cw.id)가 반려·회수 후 다른 날 재상신되면 같은 양식 자동첨부가 날짜만 달라 중복 누적, attachCount 부풀림 → 날짜 뺀 접두(양식명+버전) 기준 dedup(버전 상승 시 정상 재생성). e2e attach_generated_dedup. | 중 |

**잔여 저심각(문서화·미조치):** submitResult 업로드가 registerUpload 무-dedup 이라 반려·회수 재상신마다 결과파일
중복 누적(저, 선택 첨부·관리 워크스페이스); 변경 계획/결과 결재가 ref=cw.id 공유라 배지·상세 첨부가 단계 교차
합산(저, 상세는 신원 게이트·동일 변경 작업). 둘 다 count/표시 이슈로 보안 경계 무관 — 첨부에 docType 태깅 시 정리 가능.
**교훈: 자동생성 산출물 dedup 키에 날짜를 넣으면 '당일만' 방어가 된다 — 안정 식별자(refId+양식버전)로 dedup.**

## 알림/스케줄러 엔진 최대강도 재감사 2차 (v1.5.58)

5개 적대 렌즈(발송 폴트·스케줄러 타이밍·대상 정합·배치 기록·엣지) → 반증 검증(18 에이전트). 1차(v1.5.48)
이후 확정 3·부분 1·반증 9. 코어(sendVia withTimeout+catch 폴트격리·스케줄러 인터벌/finally/HMR·대상 정합) 건전.

**수정 완료(전부 v1.5.58):**

| # | 상태 | 위치 | 내용 | 심각도 |
|---|:---:|------|------|:---:|
| N1 | ✅ | `lib/notify.ts` (재서약 필터) | title 누락(undefined) 손상 할일에 `x.title.includes` 가 TypeError → 배치 전체 중단·후속 유형 전부 누락·실패기록 없음(수동 실행은 500) → `String(x.title ?? '')` 강제. e2e notify_corrupt_todo. | 높음 |
| N2 | ✅ | `pledge/{my,manage,dept}` | `const YEAR = currentYear()` 를 모듈 스코프에서 1회 평가·고정 → 프로세스가 연도 경계를 넘으면 서약 유효 판정·신규 레코드 year 가 전년에 묶여 재서약 사이클 미시작 + 호출시점 평가하는 notify/dashboard/export 와 영구 불일치 → 렌더/액션 호출 시점 평가로 이동. | 중 |
| N3 | ✅ | `lib/notify.ts`·`instrumentation.ts`·`platform/integrations` | 재진입 가드(__ngvNotifyTicking)가 스케줄러 틱 전용이라 수동 실행이 우회 → 겹침 시 중복 발송 → runDailyNotifyOnce 공유 래퍼로 두 경로 직렬화(가드를 래퍼 안으로 이동, 틱은 중복 가드 제거). | 저 |

**반증(비결함):** 미정의 수신자(ci/requester/inspector 부재)는 실무상 항상 정의; 빈배치 성공기록은 정상.
**교훈: 재진입 가드는 한 진입점(스케줄러)만 감싸면 다른 진입점(수동)이 우회한다 — 공유 래퍼로 전 경로 직렬화.**

## 세션/인증 엔진 최대강도 재감사 (v1.5.59)

5개 적대 렌즈(위조·로그인 무결성·쿠키/CSRF·생애주기·파싱) → 반증 검증(12 에이전트). 확정 0·부분 다수·반증 다수.
코드 레벨 코어 건전 재확인: 서명(HMAC-SHA256+timingSafeEqual 길이 가드)·만료(exp)·로그인(서버 ACCOUNTS
매핑, 클라이언트 역할 주입 불가)·쿠키(httpOnly·sameSite lax·secure(prod))·파싱(try/catch·null 가드)·로그아웃(delete).

**하드닝 완료(v1.5.59):**

| # | 상태 | 위치 | 내용 | 심각도 |
|---|:---:|------|------|:---:|
| SS1 | ✅ | `next.config.mjs` | HSTS(Strict-Transport-Security) 헤더 부재 — 어떤 env 로도 안 켜지던 잔여 → max-age 2년·includeSubDomains 추가(HTTPS 배포에서 쿠키 평문 유출 방어, http 는 브라우저가 무시해 무영향). 스모크 검증. | 저 |
| SS2 | ✅ | `lib/readiness.ts` | 'HTTPS 쿠키' 준비상태가 프로덕션에서도 info 라 세션 키 경고(warn)보다 낮게 노출 → 프로덕션 && Secure 미설정 시 warn 승격(동등 수준). | 저 |

**부분·미조치(배포 구성, 문서화):** SESSION_SECRET 미설정 시 공개 개발키로 세션 위조 가능 — 기동 경고·준비상태
카드·docker-compose(`:?` 강제) 3중 노출된 구성 리스크. getSession fail-closed(프로덕션+개발키 → 세션 전면 거부)는
정당한 방어심화이나, 게이트·http 데모가 개발키를 프로덕션 모드에서 쓰므로(스모크/e2e/health 전부 개발키로 쿠키
서명) 도입 시 게이트 전면 붕괴 + 신규 env 플래그로 데모 기동 의미 변경 → 별도 트랙(게이트가 SESSION_SECRET 을
쓰도록 선행 개편 필요). **교훈: 0-확정 감사에서도 잔여 하드닝(HSTS·경고 승격)은 가치 — 단 게이트 인프라가
특정 구성(개발키)에 의존하면 fail-closed 도입 전 인프라부터 정리.**

## SR/변경 전파 엔진 최대강도 재감사 2차 (v1.5.60)

5개 적대 렌즈(상태기계·전파·멱등성·인가·고아루프) → 반증 검증(13 에이전트). 1차(v1.5.50) 이후 확정 2·부분 2.

| # | 상태 | 위치 | 내용 | 심각도 |
|---|:---:|------|------|:---:|
| SR1 | ✅ | `sr/manage` submitApply | 적용요청 상신이 반려 재상신 할일을 소유자 무관하게 닫지 않아, 공유 워크스페이스에서 재상신자≠원 기안자면 재상신 할일 고아+'반려 방치' 알림 무한 반복(v1.5.50 변경엔 closeChangeResignTodo 로 고쳤으나 적용요청 누락) → 유형+SR번호 선두로 소유자 무관 마감. e2e apply_resign_orphan. | 중 |
| SR2 | ✅ | `sr/ci` receiveCiSr | 결재없는 CI 직접접수에 중복 가드 부재 → 더블클릭 이중 접수 → 같은 제목+요청자+접수 중복 무시. | 저 |

**부분(저·미조치):** addInfraChange 중복(인프라 변경은 자연키 없어 보류)·변경결과 반려 시 cw.result 잔류(재상신 시 덮임).
**교훈: 폐쇄루프 수정은 유형별로 흩어진 전 상신 경로에 적용 — 변경만 고치고 적용요청을 놓친 v1.5.50 형제 결함.**

## 컴플라이언스/ISMS 엔진 최대강도 재감사 2차 (v1.5.61)

5개 적대 렌즈(점검 상태기계·이수율·referential·CSV·인가/고아) → 반증 검증(17 에이전트). 확정 2·부분/반증 다수.

| # | 상태 | 위치 | 내용 | 심각도 |
|---|:---:|------|------|:---:|
| C1 | ✅ | `compliance/education` registerAttendees | 명단 이름을 과정 대상(eligibleForCourse)으로 재검증하지 않아, 임의 POST 로 비대상자를 이수 기록에 넣으면 그 기록이 과정을 '완료'로 확정하면서도 이수율 집계엔 안 잡혀 전사 이수율 왜곡(화면은 대상자만 노출, 서버 미검증) → 이름 대상 스코프 재검증 + '완료' 확정도 대상 이수자 기준(단일 원천). | 중 |
| C2 | ✅ | `lib/csv.ts` esc() | 인용 판정이 내부 CR(\r)을 빠뜨려 업로드 필드 중간 단독 CR 이 export 시 비인용→일부 파서 행 깨짐 → 인용 대상에 \r 추가. | 저 |

**반증:** 이수율 단일 원천(>100% 불가·0나눗셈 가드)·점검 상태전이·referential 삭제 가드·인가 건전. C1 은 서버측
가드(UI 미도달 크래프트 POST)라 코드 검토 확인, education_target 정상 경로 회귀 유지. **교훈: 화면이 대상자만
노출해도 서버액션은 직접 POST 가능 — 목록 렌더 필터와 동일 스코프를 쓰기 액션에도 재적용(export 재적용 원칙과 동일).**

## 재무/자금 엔진 최대강도 재감사 2차 (v1.5.62)

5개 적대 렌즈(집행률·정산전파·속보기준·종류격리·인가) → 반증 검증(18 에이전트). 확정 3(HIGH×2·MED)·부분 다수.

| # | 상태 | 위치 | 내용 | 심각도 |
|---|:---:|------|------|:---:|
| M1 | ✅ | `finance/invest`·`finance/expense` addContract | requireMenuRole 에 USER 포함 + /finance/* 메뉴 roles:ALL → USER 가 게이트 통과. 계약 카드는 canViewFinance(비-USER) 섹션이라 화면엔 안 보이나 서버액션 직접 POST 로 USER 가 전사 계약 신규 생성(write-where-read-denied, BAC) → 섹션 가드와 동일 DEPT_MGR+ 로 정정. | 높음 |
| M2 | ✅ | `finance/expense` editFlash | 동일 BAC — USER 가 전사 속보 기준금액 변조(감사기록까지 남아 정당해 보임) → DEPT_MGR+. | 높음 |
| M3 | ✅ | `finance/expense` addFlash | 동일 BAC — USER 가 전사 속보 신규 생성(관리자 전용 뷰 오염) → DEPT_MGR+. | 중 |

**반증:** 집행률(확정계획 스코프 ratio-of-sums, 내 독립 패스도 정합 확인)·종류 격리(kind 필터)·정산 전파(status
guard)·>100%(실 초과집행+계약 초과 플래그) 건전. 경영계획 액션은 본인 스코프라 USER 유지(정상). **핵심 교훈:
v1.5.42/43 이 재무 섹션(UI)·정산 상신은 잠갔으나 계약·속보 쓰기 4개 액션을 놓쳤다 — 섹션 가드(canViewFinance)를
바꿀 때 그 섹션의 전 쓰기 액션 가드도 동일 역할로 맞출 것. 내 독립 패스가 놓친 걸 워크플로가 적발(이번 런
검색·알림에 이어 3번째) — 집계 수학만 보고 액션 인가를 교차검증 안 한 사각.**

## 영속화/스토어 엔진 최대강도 재감사 2차 (v1.5.63)

5개 적대 렌즈(저장/플러시·로드/머지·백업 로테이션·nextNo/기록·정합) → 반증 검증(18 에이전트). 확정 다수가
동일 클래스로 수렴. 저장(원자 tmp→rename·비굶음 디바운스·SIGTERM 플러시)·격리(파싱+읽기 오류)·백업·nextNo
(NaN 안전·연도 키)·recordBatch 캡 건전 재확인.

| # | 상태 | 위치 | 내용 | 심각도 |
|---|:---:|------|------|:---:|
| ST1 | ✅ | `lib/store.ts` 요소 정규화 | 타입 안전 머지가 배열(→[])·숫자(→0)만 복구하고 문자열 필드는 방치 → 손상 파일이 문자열 필드를 숫자/객체로 남기면 소비처 .slice/.includes/.localeCompare 가 평범한 GET 에서 500(예: incidents.occurredAt 숫자→/infra/incidents 500). strFields 정규화 추가(존재하는 비문자열만 String 강제, 누락 undefined 는 옵셔널 보존). 알림 title.includes(v1.5.58)와 동일 클래스 체계적 마감. emptystate 요소 오염 프로브. | 높음 |

**미조치(문서):** 빈 객체 요소(누락 필수필드)는 옵셔널 보존과 상충해 머지 일괄 강제 불가 — 핫스팟은 호출부 String 방어.
**교훈: 요소 정규화는 array/number(→.map/.length 크래시)뿐 아니라 string(→.slice/.includes 크래시)도 대칭 커버.**

## 인프라 CRUD 엔진 최대강도 재감사 2차 (v1.5.64)

5개 적대 렌즈(참조삭제·참조생성·입력검증·인가·렌더견고성) → 반증 검증(13 에이전트). 확정 1·부분 2·반증 다수.
참조 무결성 삭제 가드(랙←HW/서버·서버←시스템·시스템←배치/IF·HW←서버) 전량·생성측 부모 검증·인가·IP
옥텟(v1.5.27) 건전 재확인 — v1.5.27 CLEAN 유지.

| # | 상태 | 위치 | 내용 | 심각도 |
|---|:---:|------|------|:---:|
| IN1 | ✅ | `infra/systems` incidentsOf | systemName.replace 무방비 호출 → 손상 파일이 system.name 누락(undefined)·비문자열이면 전 페이지 500. 머지 strFields(v1.5.63)가 비문자열은 강제하나 누락은 옵셔널 보존 위해 두므로 호출부 String(name ?? '')로 방어. emptystate systems.name 누락 프로브. | 중 |

**부분(저·미조치):** deleteSystem 이 incident.system(자유텍스트, 이미 create 측 허용) 미가드 → 소프트 dangling
(무크래시·표시만); 인터페이스 from/to 이름 기반 가드 우연 일치 과차단(rename 액션 없어 역방향 orphan 불가).
**교훈: 삭제 참조 가드는 완비됐으나 렌더 견고성(손상 파일 문자열 필드)은 별도 축 — 문자열 메서드 호출부 방어.**

## 게시판/설정 엔진 최대강도 재감사 2차 (v1.5.65)

5개 적대 렌즈(공통코드 전파·결재선 편집·board CRUD·엑셀양식·설정 견고성) → 반증 검증(15 에이전트). 확정 1·부분 다수.
공통코드 6그룹 전파(activeCodes·enabledGrades·activeItems 모두 isCodeActive)·결재선 편집 검증(비-USER·2차 구분)·
설정 ADMIN 게이트·board 인가 건전 재확인(v1.5.28/29 유지).

| # | 상태 | 위치 | 내용 | 심각도 |
|---|:---:|------|------|:---:|
| BS1 | ✅ | `settings/audit` | 설정변경 집계가 l.action.includes 무방비 호출 → 손상 파일이 감사 로그 action 을 누락(undefined)·비문자열로 남기면 전 감사화면 500(머지 strFields 는 비문자열만 강제, 누락은 옵셔널 보존). 호출부 String(l.action ?? '') 방어. emptystate 프로브에 action 누락 로그 추가. (선제 수정, 워크플로 반증이 확인) | 중 |

**부분(저·미조치):** resubmitSr 가 SR_KIND 활성 재검증 생략(은퇴 유형 초안이 결재 진입 — 반증이 grandfathering
으로 판정); addCodeValue 그룹당 상한 없음(ADMIN 1건/요청, 벌크 경로 없음); sr/requests KIND_TABS 하드코딩(읽기
필터라 신규 코드가 탭엔 없으나 전체 조회 정상). 엑셀양식 중복 docType(unshift+find 로 최신 우선)은 반증.
**교훈: 손상 파일 문자열 필드 방어는 머지 strFields(비문자열)+핫스팟 호출부 String(누락 undefined) 이중이 패턴 —
audit/incidents/systems/notify 전 화면 동일 처리. 이로써 렌더 견고성 축 체계적 마감.**

## 의식제고/서약 엔진 최대강도 재감사 2차 (v1.5.67)

5개 적대 렌즈(재서약 대상·서명 시각정밀·스코핑·출력물 폐쇄루프·위반/견고성) → 반증 검증(16 에이전트). 확정 3·부분 다수.
pledge/dept·export 스코핑(DEPT_MGR 부서한정+effectiveRoles)·서명 시각정밀(signedAt>=revisedAt)·프로젝트/특별 대상·
위반 self-only 건전 재확인.

| # | 상태 | 위치 | 내용 | 심각도 |
|---|:---:|------|------|:---:|
| AP1 | ✅ | `pledge/manage` reSignTargets '재택근무' | remoteTargets 를 이름 dedup 없이 매핑 → 인접 두 기간(경계월, v1.5.53) 보유자에게 재서약 안내메일 중복+sendLog to.length 부풀림(할일은 line63 별도 dedup) → Set dedup. (선제 수정, 반증이 확인) | 중 |
| AP2 | ✅ | `awareness/prints` (myCollation·submitDiscards·completeCollation) | '취합' 할일 조회가 title.includes 무방비 → 손상 파일의 title 누락 '출력물 폐기확인' 할일이 있으면 평문 GET·액션 500 → String(t.title ?? '') 방어(3곳). emptystate 프로브. | 중 |
| AP3 | ✅ | `awareness/remote` applyTarget | CSV/폼 날짜가 DATE 정규식만 통과(2026-13-45·2026-02-30 등 달력 무효)해도 dayBefore 의 new Date(NaN).toISOString() 가 RangeError 로 업로드 액션 전체 500 → realDate(UTC 파싱 왕복) 가드로 무효 행 거부(v1.5.53 dayBefore 가 도입한 크래시 벡터 마감). e2e remote CSV 무효행. | 중 |

**부분(저·미조치):** 특별 재서약이 validSign 일반 선행 미충족자까지 통지(순서 강제로 해소 가능 — 반증이 dead-todo
주장 반증); signProject 비멤버·signKind 재택 비대상·관리책임자 ACCOUNTS 기준·pledgeForm 누락 시 전원 유효 등 —
반증이 방어가능/저영향 판정. **교훈: 이름 dedup·문자열 필드 방어·달력 유효성은 각 엔진에 반복 적용해야 하는
횡단 클래스 — 특히 dayBefore 처럼 날짜 산술을 도입할 땐 정규식만으론 부족(실 달력 검증 필수).**

## 프로젝트 엔진 최대강도 재감사 (v1.5.68)

5개 적대 렌즈(생애주기·참조/첨부·입력검증·인가·렌더견고성) → 반증 검증(14 에이전트). 확정 1·부분 다수.
진척 범위검증(0-100·NaN)·projectOf 옵셔널체이닝·날짜 렉시컬 비교(크래시 클래스 없음)·멤버 실인원 검증·인가 건전.

| # | 상태 | 위치 | 내용 | 심각도 |
|---|:---:|------|------|:---:|
| PR1 | ✅ | `projects/status` updateProgress | 현재 상태 미확인 → 화면은 완료 프로젝트에 폼 숨기나 서버액션 직접 POST 로 완료(100%)→진행중(진척 하향) 되돌리기(불변식 위반) → `pj.status==='완료'` 서버 가드. | 중 |
| PR2 | ✅ | `pledge/manage` reSignTargets 프로젝트 | pj.members 를 s.people 교집합 없이 대상화 → 실 HR 어댑터가 퇴사자 제거해도 members 잔존(재조정 없음), 프로젝트 서약 개정 시 떠난 인원에 재서약 할일·메일(유령) → 현재 재직자만 대상화. | 저 |

**부분(저):** toggleDeliverable 대칭 토글(설계). **교훈: members 처럼 외부 동기화로 변하는 참조는 사용 시점에 현
디렉터리와 교집합(생성 시점 검증만으론 stale).**

## 연동/어댑터 엔진 최대강도 재감사 2차 (v1.5.69)

5개 적대 렌즈(채널게이팅·폴트내성·자가진단 이빨·프로필 단일원천·계약/타입) → 반증 검증(16 에이전트). 확정 2·부분 다수.
withTimeout(타이머 클린업)·sendVia 폴트격리·프로필 단일원천·전 어댑터 호출부 try/catch+withTimeout 건전 재확인.

| # | 상태 | 위치 | 내용 | 심각도 |
|---|:---:|------|------|:---:|
| IG1 | ✅ | `platform/integrations` syncHr | adapter.fetchPeople() 결과를 형태 검증 없이 s.people 대입 → 실 고객사 HR 어댑터가 계약(Person[]) 어겨 비배열({data,total})·필드 누락을 resolve 로 반환하면(withTimeout·try/catch 는 throw·hang 만 잡음) 스토어 오염→전 화면 s.people.filter/.map·p.dept.includes 500(앱 전역 브릭) → isPerson 형태 검증을 수집 경로에 적용, 위반 시 기존 명단 보존. e2e adapter_malformed. | **높음** |
| IG2 | ✅ | `lib/integrations/conformance.ts` asset | 자가진단 asset 계약 검사가 searchAssets 행 형태 미검증(hr·secdata 는 검증) → 등록자산 없는 그린필드서 malformed 어댑터가 '적합' 통과(false-clean) → ExternalAsset{serial,model,category,holder} 행 형태 검증. | 중 |

**부분(저):** hrAdapter isEnabled 리졸버 미게이트(호출부 syncHr 재검사, 잠재). **핵심 교훈: 프레임워크의 어댑터
경계 방어는 throw·hang(v1.5.16/17)뿐 아니라 resolve 된 오형(계약 위반)도 수집 경로에서 검증해야 완결 — 자가진단
검증기(isPerson)와 동일 기준을 실 수집 write 에도 적용. 목은 항상 정상이라 게이트가 못 잡음(실 어댑터 전용 결함).**

## 결재/다단(2차)·영속화(3차) 최대강도 재감사 (v1.5.70)

두 워크플로 동시 완료 — 같은 클래스(손상 파일 요소 정규화 사각)의 렌더 500 을 각각 적발, 머지에서 함께 마감.

**결재 다단 (5렌즈, 확정 2·반증 4):** 셀프결재 방지(resolve 가 기안자 미산출)·다단 큐 전진(중간 승인 early-return
으로 propagation 미발화)·앵커 재상신 닫기·신원 게이트(v1.5.47/54) 건전. 확정 2(둘 다 손상 파일 React child):
결재의 옵셔널 문자열 decidedAt·rejectReason 이 객체로 오염되면 {a.x ?? '-'} 가 React child 로 렌더돼 /work/approvals
평문 GET 500. rejectReason 은 시드 어느 행에도 없어(반려 시드 없음) strFields(v1.5.63)로 못 잡던 사각.

**영속화 3차 (5렌즈, 확정 1·부분 다수):** 저장·격리·nextNo·백업 로테이션 건전 재확인(내 독립 패스와 일치).
확정 1[높음]: codeGroups[].values 안의 null·원시 원소를 상위 요소 필터가 못 걸러 isCodeActive(null) 이 .enabled
접근으로 전 업무화면(장애·SR·공통코드) 500(중첩 배열 원소 미검증). 부분(저): .corrupt 파일 무제한 누적(포렌식
아티팩트라 방어적 설계)·격리 실패 시 다음 saveNow 가 원본 덮어씀(Windows 파일락 엣지).

| # | 상태 | 위치 | 내용 | 심각도 |
|---|:---:|------|------|:---:|
| AP-E | ✅ | `store.ts` 머지 요소 정규화 | 결재 옵셔널 문자열(rejectReason·decidedAt) 객체 오염 → React child 500. strFields 를 전 시드 요소 합집합으로 확장 + 요소 필드의 순수 객체값 일괄 제거(시드에 없는 필드까지 보편 방어). | 중 |
| ST3-1 | ✅ | `store.ts` 머지 중첩 배열 | codeGroups[].values 의 null·원시 원소 미검증 → isCodeActive(null) 전 화면 500 → 시드 형판 원소 타입 기준 중첩 원소 필터(v1.5.49 상위 필터의 중첩 확장). | 높음 |

**교훈: 시드 첫 요소만 형판으로 쓰면 later-row-only 옵셔널 필드가 샌다(합집합). 요소 필드에 순수 객체는
불변식 위반이라 일괄 제거가 시드 무관 보편 방어. 손상 파일 방어 3층 완성: 상위요소(v1.5.49)·필드값 타입
(v1.5.63/66)·중첩원소·객체값 제거(v1.5.70).**

## 알림/스케줄러(3차)·통합 검색(2차) 최대강도 재감사 (v1.5.71)

**알림 3차 (5렌즈, 확정 0·부분 다수):** 코어(재진입 가드 finally·스케줄러·대상 정합·sendVia 폴트격리) 건전.
부분 2건을 기존 횡단 클래스와 정합화(저심각):
- type-0 일배치 이관이 fetchPrintouts 행을 형태 검증 없이 push → 실 어댑터 계약 위반(name 누락) 시 undefined
  수신자·집계 오염 → 행 형태 검증(syncHr v1.5.69 와 동일 수집 경로 계약 검증).
- 비일반 재서약 notify(1-b)가 재서약 할일 소유자를 재직 명단 교집합 없이 통지 → 퇴사자 유령 통지 → 재직자
  교집합(reSignTargets v1.5.67·미서약·재택 미제출 타 유형과 동일). 미조치(무해): 배치 '명' 은 유형별 합(라벨 의미).

**검색 2차 (5렌즈, 확정 0):** v1.5.56(effectiveRoles 게이트)·행 스코핑(SR 부서·재무 USER 제외+canAccess) 보존,
쿼리 반향 React 이스케이프·숫자/키 렌더 안전(v1.5.70) 전량 건전 재확인 — CLEAN. 부분(저·미조치): 손상 파일
중복 id 시 React key 경고(무크래시)·무제한 렌더(데모 데이터셋 한정 perf). **교훈: 어댑터 수집 계약 검증·재직자
교집합은 전 수집/집계 경로에 반복 적용할 횡단 클래스 — 한 경로(syncHr·reSignTargets) 고쳐도 형제 경로(fetchPrintouts·
notify 1-b)에 남는다. 검색은 v1.5.56 이후 견고(2차 확정 0).**

## 컴플라이언스/ISMS(3차)·재무/자금(3차) 최대강도 재감사 (v1.5.72)

두 워크플로 동시 완료 — 둘 다 같은 클래스(v1.5.70 손상 파일 정규화의 **'시드에 없는/누락된 필드'** 사각)의
렌더 500 을 각각 적발, 스토어 머지 + 렌더 가드로 마감. 코어 로직(정산 생명주기·기준금액 우선순위·집행률
스코프·kind 격리·교육 이수율·점검 월 비교)은 전량 반증 — 3차 재감사가 남긴 건 손상 파일 엣지 2건뿐.

**재무 3차 (5렌즈, 확정 1·부분 다수·반증 다수):** 정산 이중지급(status==='결재중' 가드로 1회 전이)·재상신
중복결재(대기 결재 가드)·기준금액 basisOf 우선순위(정산>계약>계획, month×vendor 집계)·집행률 확정계획 스코프·
kind 격리(CT 채번 전역 유일)·paidOf 전역참조(현 데이터 무누수) 전부 반증. 확정 1[중]: **ExpenseFlash.history**
(옵셔널 배열, 시드 3행 모두 부재)가 비배열 원시값('x')으로 오염되면 머지 arrFields(시드 형판 기반)가 못 잡아
`f.history.length>0`(문자열 length) 통과 후 `.map` TypeError → /finance/expense 전 화면 500. 원소 오염
(null·비숫자 expected)도 fmt(undefined).toLocaleString 로 동일 크래시.

**컴플라이언스 3차 (5렌즈, 확정 1·부분 1·반증 3):** 교육 이수율 라이브 분모·이수 완료 후 신규 대상·점검
월 비교(어휘 비교, Date 무연산) 전부 반증/부분. 확정 1[중]: **person.dept 필드 누락**(값 오염이 아니라 키
부재) — 머지 strFields 는 옵셔널 보존 위해 undefined 를 안 건드려 살아남고, eligibleForCourse('개발자')의
`p.dept.includes('개발')` 가 undefined 에서 500(/compliance/education 평문 GET, 시드 ED-2026-03 개발자 과정).
부분 1[저]: addPlan 월 정규식이 2026-13 등 달력 무효월 통과(어휘 비교라 무크래시, 데이터 정합만).

| # | 상태 | 위치 | 내용 | 심각도 |
|---|:---:|------|------|:---:|
| FE3-1 | ✅ | `finance/expense/page.tsx` history 렌더 | 시드 무존재 옵셔널 배열 history 비배열/원소 오염 → .length/.map/toLocaleString 500. 스토어가 시드 형판으로 못 잡으므로 렌더에서 `Array.isArray` + 원소(`h?.at`·비숫자 expected→0) 방어. | 중 |
| CE3-1 | ✅ | `store.ts` 머지 reqStrFields | 전 시드 행 공통 필수 문자열 필드가 손상 파일에서 통째 누락(키 부재) → undefined 에서 .includes/.slice 500. 옵셔널(일부 행) 보존과 별개로, 필수(전 행) 필드는 누락도 ''로 채움(정상 데이터 무영향). | 중 |

**교훈: v1.5.70 정규화는 '값 오염'(wrong-type)을 잡지만 두 사각이 남았다 — (a) 시드 어느 행에도 없는 옵셔널
필드(history)는 형판 부재라 스토어가 원천적으로 못 잡아 소비 렌더가 방어해야 하고, (b) 전 행 공통 '필수'
필드의 키 부재(dept)는 undefined-보존 규칙에 걸려 살아남으니 필수/옵셔널을 구분해 필수는 누락도 기본값으로
채워야 한다. 손상 파일 방어 4층: 상위요소(v1.5.49)·필드값 타입(v1.5.63/66)·중첩원소·객체값(v1.5.70)·필수필드
누락채움+시드무존재 필드 렌더가드(v1.5.72). 코어 재무·컴플라이언스는 3차에서 확정 0(손상 파일 엣지만).**

## 시드무존재 필드(FE3-1) 전수 스윕 + 동시성/TOCTOU 감사 (v1.5.73)

**FE3-1 클래스 폐쇄(코드변경 없음):** 시드 어느 행에도 없는 옵셔널 배열/객체 필드는 정확히 2개
(ExpenseFlash.history·Approval.queue)뿐임을 전수 확인 — history 는 v1.5.72 렌더가드, queue 는 store.ts:405
F2 가드로 둘 다 방어. 확정 0, 클래스 구조적 폐쇄. queue 소비처(approvals/page.tsx:146 truthiness 가드)는
store 상류 정합으로 무해 → 과수정 지양 원칙 적용(미변경).

**동시성/TOCTOU 감사(5렌즈, 확정 0·부분 다수):** 공유 globalThis 인메모리 스토어의 await 갭 경합·재진입·
ID 충돌·영속화 인터리브를 전수 감사. 핵심 모델: JS 단일스레드라 동기 read-modify-write 본문은 원자적 —
경합은 read-check 와 mutation 사이 await 가 있을 때만. 전부 반증/무해 부분: (a) asset-reg 취득·출력물 이관·
nextNo 삽입은 await 후 dedup+삽입이 동기라 원자적(둘째 요청은 dedup 에서 스킵); (b) saveNow 는 완전 동기
(stringify→write→rename)라 프로세스 내 무경합, 크로스-프로세스 torn-write 는 인메모리 싱글턴이 미지원
토폴로지; (c) 디바운스 저장 드롭은 다음 틱 recordBatch 재무장으로 복구. 확정 1[저·조치]:

| # | 상태 | 위치 | 내용 | 심각도 |
|---|:---:|------|------|:---:|
| CC-1 | ✅ | `notify.ts` 명단 스냅샷 | runDailyNotify 가 s.people 을 3구간(미서약·재서약·재택)에서 sendVia await 사이로 읽는데 syncHr 가 배치 도중 `s.people = new`(참조 교체)하면 앞뒤 구간이 옛/새 명단으로 갈려 배치 내부 불일치 → 배치 시작 시 `[...s.people]` 얕은복사로 1회 포착, 전 구간 반복읽기 일관성(참조교체·in-place 양쪽 불변). | 저 |

**교훈: 인메모리 스토어 동시성은 단일스레드 원자성이 대부분 방어 — 실 경합면은 (1)비멱등 외부 어댑터를
await 하는 사이드이펙트(자산번호 취득, 현 목 어댑터는 멱등이라 무해)·(2)참조 교체(s.people=new)로 갱신되는
배열을 배치가 여러 구간 걸쳐 읽을 때의 read-skew뿐. 후자는 배치 시작 스냅샷으로 마감. __ngvNotifyTicking(notify-
vs-notify) + people 스냅샷(notify-vs-syncHr) 로 배치 일관성 이중화. 동시성 감사 확정 0(스냅샷 1건 선제 조치).**

## 연동/어댑터 계약 완전성 감사 (v1.5.74)

제품 핵심 차별점(재사용 프레임워크·클린 어댑터 계층)을 대상으로 폴트 내성(throw/hang/malformed, v1.5.16/17/69)이
아닌 **계약 완전성**을 5렌즈(연동지점 전수·수집 검증 패리티·컨포먼스 커버리지·폴트 랩 전수·프로필 격리)로
감사. 확정 다수 — **수집 검증 패리티 사각**이 핵심 테마: 자동 경로(syncHr v1.5.69·notify type-0 v1.5.71)는
어댑터 resolve 오형을 형태검증하나 형제 경로(자산·수동 출력물·메일·컨포먼스)가 누락돼 있었다.

| # | 상태 | 위치 | 내용 | 심각도 |
|---|:---:|------|------|:---:|
| AD-1 | ✅ | `asset-reg/page.tsx` searchAssets | `.catch(()=>[])` 는 reject 만 흡수 — 실 어댑터가 비배열({data,total} 봉투) resolve 시 assets.filter/.map 전 화면 500 → Array.isArray+행 형태검증(conformance 와 동일 엄격도). e2e adapter_asset_malformed(스태시검증). | 높음 |
| AD-2 | ✅ | `asset-reg/page.tsx` acquireAssetNo | 빈/비문자열 assetNo 무검증 저장 → 빈 취득번호 이력 + serial dedup 이 재시도 영구차단(복구불가) → 비어있지 않은 문자열일 때만 기록. | 중 |
| AD-3 | ✅ | `prints/page.tsx` importDaily | 수동 '전일자 이관'이 fetchPrintouts 행을 무검증 push(자동 notify.ts:40 은 검증) → name·document·printedAt 형태검증 패리티. | 중 |
| AD-4 | ✅ | `registry.ts` sendVia | 실 메일 어댑터가 undefined resolve 시 result.ok 접근이 try 밖에서 500 → resolve 값 SendResult 형태검증·정규화. | 중 |
| AD-5 | ✅ | `conformance.ts` secdata | 자가진단이 dept·personalInfo(필수, 부서 스코핑·개인정보 표시에 소비) 미검증 → 이를 뺀 고객사 어댑터가 녹색 통과 후 실배포 오작동 → 전 필수 필드 검증. | 중 |
| AD-6 | ✅ | `registry.ts` hrAdapter | 유일하게 isEnabled 게이트 없이 어댑터 반환(asset·secdata 리졸버는 게이트) → 채널 토글 계약 위반 잠재 트랩 → isEnabled('hr-sync') 자체 게이트로 대칭화. | 저 |
| AD-7 | ⏳ | `store.ts` 프로필 스탬프 | PORTAL_DATA_FILE 에 프로필 식별자 미기록 → 같은 파일로 PORTAL_PROFILE 전환 시 프로필A channelStates 잔존. 단일 테넌트 배포(고객사별 별도 DB)라 데모 한정 풋건 — 별도 트랙 검토(형제 렌즈 다수는 per-deployment 설계로 반증). | (보류) |

**교훈: 수집 검증(어댑터 resolve 오형 형태검증)은 전 어댑터 write 경로에 적용할 횡단 계약 — 자동 경로만
굳히면 자산·수동·메일·컨포먼스 형제가 샌다. 목 어댑터는 항상 정상이라 게이트가 실 어댑터 결함을 못 잡으므로
PORTAL_FAULT_{HR,ASSET,SECDATA,MAIL}=malformed 주입 훅으로 각 경로를 결정적 검증. 어댑터 계약 4폴트(throw·
hang·malformed-resolve·조회봉투) × 전 write 경로 = 재사용 프레임워크 무결성 축 완성. e2e 45→46.**

## 엑셀/CSV 익스포트 보안·정합 감사 (v1.5.75)

익스포트 계층(app/api/export·lib/csv)을 5렌즈(CSV 수식인젝션·구조 이스케이프·인가·스코프 누수·손상필드)로
감사 — **확정 0·부분 1(저)**. 핵심 보안(수식인젝션·구조)은 이미 견고: lib/csv.ts esc() 가 사용자 문자열 셀의
수식 트리거(=+-@ 탭 CR)를 작은따옴표로 무력화(CWE-1236)하고 RFC-4180(따옴표·콤마·CR·LF 인용)로 이스케이프,
단일 choke point(csvResponse)라 전 익스포트가 상속. 인가(USER 403·kind·dept 스코프)·스코프 누수 전량 반증.

| # | 상태 | 위치 | 내용 | 심각도 |
|---|:---:|------|------|:---:|
| EX-1 | ✅ | `store.ts` 빈시드 컬렉션 null 원소 | printouts 는 유일한 '빈 시드 + 익스포트' 컬렉션이라 형판이 없어 요소 정규화가 스킵된다(sample===undefined→raw 수용). 손상 파일 {"printouts":[null]} → /awareness/prints·엑셀 익스포트가 p.name/p.id 접근에서 전 역할 500. null/undefined 는 어느 컬렉션(객체·문자열 배열)에도 무효라 빈시드 분기에서 제거(형태 판별 불가해도 null 제거는 안전). emptystate 게이트에 null printout 픽스처(스태시검증). | 저 |

**교훈: 익스포트 CSV 인젝션·이스케이프는 이미 성숙(단일 choke point). 잔여는 손상파일 방어의 마지막 사각 —
'빈 시드 컬렉션'은 형판이 없어 요소 형태검증을 못 하나, null 원소만은 전 컬렉션 공통 무효라 제거 가능(형태는
못 잡아도 null 은 잡는다). 손상 파일 방어: 상위요소(v1.5.49)·필드값타입(v1.5.63/66)·중첩원소+객체(v1.5.70)·
필수필드+시드무존재렌더가드(v1.5.72)·빈시드 null원소(v1.5.75). 6축 수렴(+익스포트 보안 확정 0).**

## SR/변경 전파 엔진 최대강도 재감사 (3차, v1.5.76)

SR/변경 전파를 5렌즈(생명주기·변경링크·지연알림·인가·렌더견고성)로 3차 재감사 — **확정 2(저)·부분 다수·반증
다수**. 코어(전파 멱등성·이중생성 방지·인가) 전량 견고 재확인: 모든 폐쇄루프가 대상 현재상태를 가드(sr.status
==='결재중' 등)해 멱등, addDevChange 중복가드, assignCi 의 ci∈BIZ_MGR/ADMIN 검증(v1.5.60), decide ap.status
==='대기' 가드. 반증: SR지연 ghost(ci 는 ACCOUNTS 명이라 로스터 무관·dueDate 있으면 ci 항상 존재→requester
분기 도달불가), 적용요청결재중 제외(dueDate 축으로 반증), 인가 전파(설계상 의도).

확정 2 — v1.5.70 객체드롭의 **배열 사각**(같은 클래스 일반화):

| # | 상태 | 위치 | 내용 | 심각도 |
|---|:---:|------|------|:---:|
| SR3-1 | ✅ | `store.ts` 객체드롭→배열 확장 | v1.5.70 객체드롭은 순수객체만 제거하고 '객체 담은 배열'은 통과 → 시드 어느 행에도 없어 arrFields 로 못 잡는 스칼라 필드(ChangeWork.srNo·SrRequest.completedAt)가 손상 파일에서 `[{}]` 이면 {c.srNo} 직접 렌더가 React child 500(/infra/changes·/sr/manage). arrFields(정상 배열, 원소 정규화 완료)만 예외로 두고 그 외 '객체 담은 배열' 제거. 유일한 시드무존재 배열-객체 필드 history 는 시드에 []추가로 arrFields 편입해 보존(queue 는 문자열 배열이라 무해). emptystate srNo:[{}] 픽스처(스태시검증). | 저 |

**교훈: 손상파일 방어의 마지막 사각 — 객체 제거는 '순수 객체'뿐 아니라 '객체를 담은 배열'까지 봐야 완결
(둘 다 {field} 직접 렌더 시 React child 500). 시드무존재 배열-객체 필드는 history 하나뿐이라 시드 편입으로
예외 처리, 나머지 스칼라 필드의 배열-객체 오염은 제거. 손상파일 방어 6층(+빈시드 null v1.5.75·배열-객체드롭
v1.5.76). SR/변경 코어 3차 확정 0(렌더 견고성 사각만).**

## 재무/자금 엔진 최대강도 재감사 (4차, v1.5.77)

재무를 5렌즈(계획 생명주기·금액연산·참조무결성·인가·렌더)로 4차 재감사 — **코어 확정 0·부분 다수·렌더 확정 1**.
코어 전량 반증/부분: 계획 생명주기(작성중→취합→효율화→확정) 스킵불가(confirmPlan 은 효율화 필수)·submitPlanDraft
소유자 스코프·optimize/confirm BIZ_MGR — invest/expense 완전 대칭. 금액연산: 전 입력 Number.isFinite+≤0+1e9,
집행률 planTotal===0 가드. 참조무결성: planId·contract kind 검증, confirmedIds 스코프. 부분(저·미조치): 집행률
>100% 주석(과집행 의도 기능이라 표시는 정직·주석만 낡음), invest 정산항목 하드코딩 vs expense activeCodes(투자=
마일스톤 착수/중도/잔금, 월정산은 비용 전용이라 의도적 비대칭일 개연 — 미변경).

확정 1 — 빈 시드 컬렉션의 객체값 필드(v1.5.75 null필터의 사각):

| # | 상태 | 위치 | 내용 | 심각도 |
|---|:---:|------|------|:---:|
| FN4-1 | ✅ | `store.ts` 빈시드 객체필드 제거 | assetAcquisitions 는 빈 시드([]) 라 형판 기반 필드 정규화 루프(v1.5.70/76 객체드롭 포함)를 스킵한다(expected.length===0 continue). v1.5.75 는 null 원소만 필터 → 객체값 필드(at={$date} 등, Mongo/ISO export)가 살아남아 {a.at} 직접 렌더 React child 500(/finance/asset-reg). 빈시드 분기에 순수객체·객체담은배열 필드 제거 추가(형판 없이도 적용 — 이 값들은 어느 타입이든 렌더 크래시). emptystate at={} 픽스처(스태시검증). | 중 |

**교훈: v1.5.75 빈시드 방어는 null '원소'만 잡고 객체값 '필드'는 놓쳤다 — 빈시드 컬렉션은 형판 기반 정규화
전체(타입 강제+객체드롭)를 스킵하므로, null 필터에 더해 객체값·객체배열 필드 제거까지 빈시드 분기에 넣어야
완결(assetAcquisitions·printouts 등 전 빈시드 컬렉션 공통). 손상파일 방어 7층(+빈시드 객체필드 v1.5.77). 재무
코어 4차 확정 0 — 남은 건 빈시드 렌더 사각 하나뿐이었고 마감.**

## 알림/스케줄러 엔진 최대강도 재감사 (4차, v1.5.78)

알림/스케줄러를 5렌즈(발화기전·배치필터·로스터/dedup·기록성장·손상/동시성)로 4차 재감사 — **확정 2(높음 1·저 1)·
부분 다수·반증 다수**. batchRuns 200 캡·재진입 가드(__ngvNotifyTicking) finally·per-day 무가드는 인터벌 시맨틱상
의도·점검경과 inspector(ACCOUNTS 안정) 반증. 확정 2:

| # | 상태 | 위치 | 내용 | 심각도 |
|---|:---:|------|------|:---:|
| NS4-1 | ✅ | `instrumentation.ts` 기동 따라잡기 | setInterval 이 부팅에 앵커돼 인터벌보다 잦은 재시작(야간 배포·오토스케일·유휴 축출)이 매번 카운트다운 리셋 → 24h 주기면 틱이 영영 안 와 미서약·SR지연·재택·확인서 컴플라이언스 메일 조용히 영구 중단. 오늘자 일일 배치 기록 없으면 기동 즉시 1회 발화(재진입 가드가 중복 방지, recordBatch 가 오늘 실행 표시 → 하루 1회 수렴). e2e scheduler_bootcatchup(1h 인터벌, 스태시검증). | 높음 |
| NS4-2 | ✅ | `notify.ts` 반려방치·확인서 재직교집합 | 반려 방치(재상신 todo.owner)·확인서 미제출(violation.name)이 재직 명단 교집합 없이 발송 → 퇴사자 유령 독촉·집계 부풀림. 비일반 재서약(v1.5.71)의 구조적 쌍둥이 사각(형제 경로) → people 스냅샷 교집합 적용(떠난 인원 제외, 위반/할일 기록 자체는 화면에 유지). | 저 |

**교훈: (1) 인터벌 스케줄러는 부팅 앵커라 '인터벌보다 잦은 재시작'에 취약 — 기동 시 '오늘 안 돌았으면 따라잡기'
1회 발화로 무발송 마감(cron 없이도 하루 1회 수렴). (2) 재직 명단 교집합은 전 person 발송 경로 횡단 클래스 —
v1.5.71 이 재서약 고쳐도 형제(반려방치·확인서)에 남았다(v1.5.73 교훈 재확인). 알림 코어(재진입·필터·기록 캡)는
4차에도 견고, 신규 확정은 발화기전(운영)·형제 경로(로스터)뿐.**

## 컴플라이언스/ISMS 엔진 최대강도 재감사 (4차, v1.5.79)

컴플라이언스를 5렌즈(이수율연산·점검생명주기·대상적격·인가·렌더)로 4차 재감사 — **확정 1(중)·부분 다수**.
코어(이수율 분자⊆분모라 ≤100%·div가드·완료 flip eligibleNames.size>0·점검 생명주기 상태가드·기준 사용중 삭제
차단·결재 멱등) 전량 견고 재확인. 확정 1 + 조치한 부분 2:

| # | 상태 | 위치 | 내용 | 심각도 |
|---|:---:|------|------|:---:|
| CI4-1 | ✅ | `inspection` registerResult | 점검결과 재상신을 원 상신자와 다른 관리자(BIZ_MGR·ADMIN 공유 화면)가 하면 draftApproval 의 기안자-매칭 닫기(owner 게이트)가 놓쳐 원 상신자 '재상신' 할일 고아 → '반려 방치' 무한 독촉. SR submitApply·변경 closeChangeResignTodo(v1.5.50) 와 동일 결함 → 유형+계획번호 선두 고정 소유자무관 닫기. e2e inspection_resign_orphan(스태시검증). | 중 |
| CI4-2 | ✅ | `education`·export 이수율 | 전사 이수율이 Math.round 라 99.5% 를 100 으로 올려 미이수자 남은 컴플라이언스를 거짓 완주로 표시(governance 오신호) → 전원 이수(분자≥분모)일 때만 100, 아니면 99 캡(화면·엑셀 동일). | 저 |
| CI4-3 | ✅ | `inspection`·`education` 월 검증 | /^\d{4}-\d{2}$/ 가 2026-13·2026-00 통과 → 어휘 비교(경과·notify)에서 영구경과(2026-00)·경과누락(2026-13) 오분류(임의 POST) → 달력유효월 /^\d{4}-(0[1-9]\|1[0-2])$/ 로 강화(addPlan·addCourse). | 저 |

**미조치(문서화): namesake 이름-키(전 앱 공통, add-person 경로 없음)·registerResult SoD(등록≠승인은 강제됨,
동일신뢰 role)·반려 결과텍스트 export 잔존(상태열이 결과미등록 표기). 교훈: 교차-상신자 고아 할일은 전 공유
워크스페이스(SR·변경·이제 점검) 횡단 클래스 — draftApproval owner 게이트를 페이지가 소유자무관 닫기로 보완해야
완결(v1.5.50 이 SR/변경 고쳐도 점검 registerResult 에 남음). 컴플라이언스 코어 4차 확정 0(생명주기·이수율 견고).**

## 영속화/스토어 엔진 최대강도 재감사 (4차, v1.5.80)

영속화를 5렌즈(저장내구성·격리복구·백업로테이션·구버전머지·객체필드)로 4차 재감사 — **확정 3(높음1·중2)+선제1**.
이전 3패스는 손상 파일 '렌더' 방어(7층) 위주였고, 저장 내구성·복구 내부는 이번에 처음 심층 감사. 코어(saveNow
try/catch·tmp→rename 원자성·exit 플러시·백업 로테이션 slice(len-keep)·seed-driven 머지 구버전호환) 견고 재확인.
확정 3 + 선제 1:

| # | 상태 | 위치 | 내용 | 심각도 |
|---|:---:|------|------|:---:|
| PS4-1 | ✅ | `store.ts` 격리 실패 no-save | 손상 파일 격리 renameSync 가 throw(Windows AV·백업 파일락 EPERM)하면 catch 가 삼키고 seed 폴백 → scheduleSave→saveNow 가 원본을 빈 시드로 덮어써 격리 사본조차 없는 유일 손상 원본까지 파괴(복구지점 상실). 격리 실패 시 __ngvPortalNoSave 로 저장 비활성화(읽기전용 인메모리)해 원본 보존. | 높음 |
| PS4-2 | ✅ | `store.ts` 손상 후 백업 보류 | 손상→격리→seed 기동 후 catch-up 틱의 backupDataFile 이 seed 를 그날의 양호한 .bak(같은 날짜 스탬프) 위에 덮어써 유일 복구지점 파괴. __ngvPortalQuarantined 면 백업 보류(실패 기록)해 기존 복구지점 보존. | 중 |
| PS4-3 | ✅ | `store.ts` channelStates 검증 | menuOverrides·queue 와 달리 channelStates 값 무검증 → 비불리언 값({})이 isEnabled 의 `st[id] ?? default`(?? 는 null 만 폴백)를 통과, {} truthy 로 중지 채널(security-db) 오활성·발송. menuOverrides 와 동일 불리언 값 필터. e2e channelstate_corrupt(4/5 유지, 스태시검증). | 중 |
| PS4-4 | ✅ | `store.ts` __proto__ 가드 | 머지 키 스킵 `k in base` 가 프로토타입 체인을 봐 '__proto__'·'constructor' 통과 → mergedRec['__proto__']=v 가 스토어 프로토타입 조작(CWE-1321) → hasOwnProperty 로 시드 자기 키만 수용(선제 하드닝). | 저 |

**미조치(문서): saveNow 무신호 실패(디스크 지속실패 무경보, 300ms마다라 스로틀 필요)·.corrupt 무한누적(포렌식
설계)·백업 copyFileSync 비원자(외부 리더 엣지)·SIGKILL 300ms 창(불가피). 교훈: 손상→seed 회귀는 '저장·백업이
멀쩡한 복구지점을 덮어쓰는' 2차 손실을 부른다 — 격리 실패는 저장 금지(NoSave), 격리 성공이라도 seed 상태
백업은 보류(Quarantined)해야 복구지점을 지킨다. 렌더 방어(7층)와 별개로 저장 내구성·복구는 별도 축. 영속화
코어 4차: 저장/원자성/머지 견고, 신규 확정은 손상-후-회귀 2차손실·객체필드 검증뿐.**

## SR/변경 전파 엔진 최대강도 재감사 (4차, v1.5.81)

SR/변경을 5렌즈(다단큐×전파·CI직접접수·교차상태·첨부ref·렌더/인가)로 4차 재감사 — **확정 1(중)·부분 다수**.
코어 전량 견고 재확인: 다단 큐 중간승인은 전파 전 return(최종에만 전파)·CI직접접수 상태가드·교차플로 단조+락
(적용요청 승인 후 회수불가→체인지-링크 SR 잠금)·createSr 이중제출 dedup. 부분(저·미조치): CI '진행중' 리스트가
완료 SR 미제외(표시), replan 상태가드 없음(권한자·표시전용), 결재 todo includes(id) 5자리 충돌(>9999 도달불가),
attachCount 라벨 오표기(표시). 확정 1:

| # | 상태 | 위치 | 내용 | 심각도 |
|---|:---:|------|------|:---:|
| SR4-1 | ✅ | `approvals.ts` 회전 닫기 소유자무관 | 회전 문서(장애보고·서약현황·부서서약) 재상신을 원 기안자와 다른 관리자(공유 워크스페이스)가 하면 draftApproval 회전 닫기가 owner 게이트에 막혀 원 기안자 '재상신' 할일 고아 → '반려 방치' 무한 알림. SR submitApply·변경 closeChangeResignTodo(v1.5.50)·점검 registerResult(v1.5.79) 의 회전판 사각. 회전 닫기는 회전 ref 라 참조일치 불가하고 개수 일치(재상신 1회=반려 1건) 불변식이라 owner 무관이 옳다 → 회전 브랜치 owner 게이트 제거(비회전 ref-exact 는 owner 유지, 페이지레벨 닫기 담당). 중앙 1곳 수정으로 3 회전 제출부 동시 마감. e2e rotating_resign_orphan(스태시검증). | 중 |

**교훈: 교차-상신자 고아 할일 클래스 완결 — 비회전(SR·변경·점검)은 페이지레벨 소유자무관 닫기, 회전(장애·서약)은
draftApproval 회전 브랜치 자체를 소유자무관화(개수 불변식이라 안전). owner 게이트는 비회전 ref-exact 정확도용이지
회전엔 부적합했다. 전 orphan e2e(비회전 3 + 회전 1 + rebundle/batchref) 통과로 무회귀 확인. SR/변경 코어 4차 확정 0.**

## 재무/자금 엔진 최대강도 재감사 (5차, v1.5.82)

재무를 5렌즈(정산×결재전파·자산취득·익스포트완전성·연산·렌더/인가)로 5차 재감사 — **확정 1(중)·부분 다수·반증**.
코어 전량 견고 재확인: resubmitSettlement own-scope(정산품의 비회전이라 v1.5.81 무영향, ref-exact owner 일치로
닫힘)·결재중 이중지급 가드·exec-rate div가드·plan 생명주기·kind 격리 — settlement/rate 렌즈 확정 0. 확정 1 + 조치 부분 2:

| # | 상태 | 위치 | 내용 | 심각도 |
|---|:---:|------|------|:---:|
| FN5-1 | ✅ | `asset-reg`·conformance assetNo 검증 | searchAssets 수집 필터(v1.5.74)가 serial/model/category/holder 만 검증하고 렌더 필드 assetNo 는 빠뜨렸다 → 계약 위반 어댑터가 assetNo={} 반환 시 {a.assetNo} 직접 렌더가 React child 500(전 화면). conformance 도 동일 누락이라 녹색 통과 후 실배포 크래시(false-clean). 필터·자가진단에 assetNo 옵셔널-문자열 검증 추가. e2e adapter_asset_badfield(스태시검증). | 중 |
| FN5-2 | ✅ | export route 비용 헤더·flash planId | (a) invest/expense-actual 공유 헤더가 비용을 과제(투자 노운)로 오표기 → kind별 과제/항목 노운. (b) 비용 속보 export 가 planId 를 kind 무스코프로 find → 손상 상태서 투자 과제명 유출 → `&& p.kind==='비용'`(export 자체 원칙 route:45 정합). | 저 |

**미조치(문서): invest 정산항목 하드코딩(월정산 비용전용이라 straight symmetry 불가·교집합 필요, 거버넌스 반쪽)·
재취득 dedup 이 재발번 이력 갱신 안 함(비권위 세션 로그, decommission/re-onboard 엣지). 교훈: v1.5.74 수집
형태검증이 '렌더되는' 옵셔널 필드(assetNo)를 빠뜨렸다 — 검증은 화면이 직접 렌더하는 전 필드를 덮어야 완결
(conformance 도 동조). 재무 코어 5차 확정 0(어댑터 렌더 필드 사각·export 라벨/스코프뿐).**

## 의식제고/서약 엔진 최대강도 재감사 (v1.5.83)

의식제고/서약을 5렌즈(서약생명주기·현황상신·재택출력물·위반확인서·인가렌더)로 재감사 — **확정 2(중·저)·보류 1(높음·경계)**.
코어 견고 재확인: reSignTargets 전 kind ∩ 재직자, 개정 revisedAt 초정밀, submitStatement 위반자 본인 own-scope,
addViolation 감사·검증, dept scope(remindUnsigned·submitDeptStatus), 상태전파 멱등. 조치:

| # | 상태 | 위치 | 내용 | 심각도 |
|---|:---:|------|------|:---:|
| AP3-1 | ✅ | `prints`·`notify` dept/pages 검증 | importDaily·notify type-0 이 fetchPrintouts 행의 name/document/printedAt 만 검증하고 렌더 필드 dept/pages 를 무검증 spread → 계약위반 어댑터가 객체값 반환 시 {p.dept}/{p.pages} 직접 렌더 React child 500. 인메모리 주입이라 loadFromFile 정규화(v1.5.77) 우회. 양 경로에 dept(문자열)·pages(숫자) 검증 추가. e2e adapter_secdata_badfield(스태시검증). | 중 |
| AP3-2 | ✅ | `pledge/manage`·`pledge/dept` 서약률 | 전사·부서 서약률이 Math.round 라 99.5%→100 으로 미서약자 남은 서약 컴플라이언스 거짓 완주 표시 → 전원 서약(미서약 0)일 때만 100, 아니면 99 캡(교육 이수율 v1.5.79 동일). | 저 |
| AP3-3 | ⏳ | `approvals.ts` 회전 닫기(신선 제출 과닫기) | 회전 닫기(count 기반)가 '재상신' 아닌 신선 제출에도 발화 — submitDeptStatus/submitCompanyPledges 는 재상신 전용 액션이 없어 새 부서/새 배치 제출이 무관한 오래된 반려 재상신 할일을 닫는다 → 그 항목 반려방치 알림 유실. 단, 항목은 화면 상시 노출(company=등록 상태·dept=상시 결재상신)이라 워크플로 미정지·데이터 무손실 — 손실은 이메일 넛지뿐. 근본 수정(pledge-status 안정 ref/비회전화)은 테스트된 결재 닫기 루프 재작업이라 별도 트랙 보류. | 높음(경계) |

**교훈: 어댑터 수집 검증은 '화면이 직접 렌더하는' 전 필드를 덮어야 한다 — dept/pages 도 렌더되므로 name/document/
printedAt 만으론 반쪽(FN5-1 assetNo 동일 클래스). 인메모리 주입(importDaily·notify)은 loadFromFile 정규화를 우회하므로
수집 지점 검증이 유일 방어. 서약률은 교육 이수율과 동일 governance 오신호. AP3-3 은 count 기반 회전 닫기가 fresh-vs-
resubmit 를 구분 못 하는 근본 한계 — 항목 상시노출로 harm 이 넛지 유실에 한정(정지·손실 없음)돼, 위험한 close-loop
재작업 대신 안정-ref 재설계를 별도 트랙 보류(정직 기록).**

## 알림/스케줄러 엔진 최대강도 재감사 (5차, v1.5.84) — AP3-3 해소 포함

알림/스케줄러를 5렌즈(재상신-todo/반려방치·발화기전·sendVia기록·필터·동시성)로 5차 재감사 — **확정 3(높음·중·중)+선제,
보류(v1.5.83 AP3-3) 해소**. 코어 견고: 재진입 가드·people 스냅샷(참조교체 방어)·recordBatch 실패기록·필터 예외 없음.

| # | 상태 | 위치 | 내용 | 심각도 |
|---|:---:|------|------|:---:|
| NS5-1 | ✅ | `approvals.ts`·`pledge/dept` 부서서약 비회전 (AP3-3 해소) | 부서서약은 무상태 스냅샷이라 매 상신이 fresh 이고 재상신 전용 액션이 없어, 개수기반 회전 닫기가 무관한 부서의 반려 재상신 할일을 과다마감(반려방치 알림 영구 소실·잔여상태 없어 불가시) + 교차소유자 재상신 시 대상 뒤바뀜. ROTATING 에서 제거 + ref=부서명(안정 키)로 exact 닫기 + 페이지레벨 소유자무관 닫기(SR·변경·점검 패턴). e2e deptpledge_resign(스태시검증). 나머지 3 회전종(장애·출력물·서약현황)은 배치 가변이라 회전 유지·항목 잔여상태로 최소 가시성(문서). | 높음 |
| NS5-2 | ✅ | `notify.ts` 출력물 미등록 재직교집합 | 출력물 미등록 발송이 printout.name 을 재직 명단 교집합 없이 발송 → 퇴사 출력자 유령 독촉. 타 person 경로(미서약·재택·반려방치·확인서) 전부 적용하는 people 스냅샷 교집합을 이 경로만 누락 → 추가(미등록 출력물 자체는 관리자 화면 잔존). | 중 |
| NS5-3 | ✅ | `instrumentation.ts` 부팅 따라잡기 격리 가드 | 손상 파일 격리→시드 폴백 부팅에서 boot-catchup 이 시드 데이터 기준 컴플라이언스 메일을 발화(backupDataFile 은 __ngvPortalQuarantined 로 보류하나 notify 경로는 누락) → 같은 격리 플래그로 boot-catchup 도 보류. | 중 |

**미조치(문서): 회전 3종(장애·출력물·서약현황) fresh 상신 과다마감(항목 잔여상태로 최소 가시성·배치 가변이라 per-item
마커 필요, 별도 트랙)·personalInfo 검증(불리언 강제 렌더라 무크래시, contract 완전성만)·sendLog detail 무한장(성공 경로
slice 없음, 200 count-cap 은 있음). 교훈: 개수기반 회전 닫기는 fresh-vs-resubmit 미구분 근본한계 — 안정 per-item ref
가 있으면(부서=부서명) exact 닫기로 근본 해소, 없으면(가변 배치) 항목 잔여상태 가시성으로 완화. 재직 교집합은 전 person
발송 경로 횡단클래스(출력물로 완결). 격리 가드는 backup·notify 양쪽에 필요. 알림 코어 5차 확정 0(신규 확정은 pledge-status
회전·형제 로스터경로·격리기전).**
